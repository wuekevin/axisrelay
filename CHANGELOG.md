# Changelog

## v2.9.9 - 2026-09-20

### Features

- **Per-account forced `X-Codex-Turn-State` injection with usage-log observability.** A Codex account can now carry an operator-supplied turn-state token in its credentials (`codex_turn_state`, optional `codex_turn_state_models` model list, `codex_turn_state_set_at`), set through `PATCH /api/admin/accounts/:id/scheduler` (`null` / empty disables) or the new "Turn State 强制注入" card in the account editor with a value-age countdown (the token expires after roughly one hour upstream; the countdown resets only when the value actually changes). When the request model, or the upstream model after mapping, matches the list (comma separated, case-insensitive, trailing `*` prefix match; empty list matches everything) the value is forced onto every outbound Codex request: the HTTP header after account custom headers, and for WebSocket both the handshake header and the frame `client_metadata.x-codex-turn-state`, overriding whatever the client echoed. Every attempt in `upstream_trace` records the injected value and the value the upstream returned (HTTP response header, or the `codex.response.metadata` frame on WebSocket), persisted in new `usage_logs.injected_turn_state` / `upstream_turn_state` columns and shown, copyable, in the usage trace tooltip and error details. This does not learn or rewrite tokens automatically; an empty returned value after injection is expected, since the upstream silently accepts an echoed token without minting a new one.

- **Experimental turn-state template cache that replaces degraded tokens (#690, @sailstudio).** A system setting `codex_turn_state_template_cache_enabled` (default off, "Turn-State 模板缓存（实验性）" in Settings) captures upstream-minted `X-Codex-Turn-State` tokens of the healthy length (~292 personal / ~332 Team) into process memory keyed by account and model, and substitutes them for outbound tokens of the degraded length (~312 personal / ~356 Team) or injects one when the client sent none. Captures are parsed as strict Fernet envelopes and rejected when expired or issued in the future; acceptance keeps a 30 s TTL margin, entries are purged only on parse / TTL failure, a post-injection strike counter clears after two good responses, and `codex_turn_state_account_mode` (`personal` / `team` / `auto` by plan type, default `auto`) selects the length profile. Outbound order is cross-account echo guard → template cache → per-account forced injection, so an explicit operator value always wins; the same order applies on HTTP Responses / Chat / Compact and the WebSocket handshake, reusable WS pool slots are isolated by model because the handshake freezes the token, and the audit context is reset on every WebSocket turn. Usage logs gain `turn_state_overridden` and `turn_state_rewrite_note` (e.g. `312→292` / `inject`, never the raw token) shown beside the user agent; settings are published only after the write succeeds. `CODEX_TURN_STATE_TEMPLATE_LENGTH` / `REPLACE_LENGTH` / `INJECT_MODE` / `TTL` / `MAX_ENTRIES` / `LOG_DECISIONS` / `DRY_RUN` remain as optional tuning knobs and no longer act as the master switch.

- **Lightweight image job result endpoint for polling (#692, @Jayson1998).** `GET /v1/images/jobs/:id/result` (same API key auth and ownership check as the job detail endpoint) returns only status, error / warning, timestamps and signed download URLs with dimensions for generated assets, through a dedicated query that never loads `prompt` / `params_json`. The existing detail endpoint expanded edit inputs into data URLs, so polling an edit job could return more than 10 MB per call. Queued / running / succeeded / failed jobs are all readable (`assets: []` before output exists), `include_cache=1` adds nothing, unknown or foreign jobs return `404`, and the existing job endpoints are unchanged.

- **Antigravity Responses adapter supports freeform custom tools (#681, @frank8ai).** Codex on `wire_api = "responses"` through an Antigravity OAuth account was rejected with "does not safely support input item type custom_tool_call" as soon as `apply_patch` was involved: `type: "custom"` tool declarations were silently dropped, `custom_tool_call` / `custom_tool_call_output` input items fell into the unsupported branch, and the response side only ever emitted `function_call`. Custom declarations are now lowered to a single-`input`-string function declaration for Gemini, call and output items map onto the function call / response path, and, only for tool names the request explicitly declared as custom, the response is rebuilt as `custom_tool_call` items (`ctc_ag_` ids) with `response.custom_tool_call_input.delta` / `.done` events and the raw input unwrapped, matching the contract established in #286. Undeclared tools keep the `function_call` shape.

- **`glass` theme with a cinematic theme switcher.** A translucent glass theme joins the picker (shipped in v2.9.8, documented here), together with an animated `CinematicThemeSwitcher` transition on the theme settings page.

### Fixes

- **Account selection stops when the request is cancelled or its 30 s budget expires (#694, @Futureppo).** In large pools a request that had already been cancelled kept iterating candidates and querying Redis model cooldowns, because the cooldown read used a detached background context and the 30 s wait only started after the first synchronous scan; a regression test with 32 candidates cancelled before the scan still ran the filter 32 times (64 on the indexed path). Cancellation now propagates into the cooldown read and stale-cache cleanup, expensive filtering stops once the context is done, and the first scan, queue wait and exclusion-list retries share one 30 s budget that ends with `503` "Account selection timed out. Retry after 1 second." or the matching SSE / WebSocket error instead of a further quota scan. The budget applies to selection only, not to streaming after an account is chosen; synchronous filters already running are not preempted.

- **Strict-schema `required` entries are pruned on nodes without `properties` (#687, @eshao731).** `alignRequiredWithProperties` only reconciled `required` when the node itself carried `properties`, so a `required` list whose fields lived in `allOf` / `anyOf` / `oneOf` / `then` / `else` branches, or nowhere at all, reached the upstream unchanged and `gpt-5.5` rejected the request with "Extra required key ... supplied". Names declared in composition branches are kept and the rest removed (nothing is added, since filling under composition would change the schema's meaning); an `object` node with no source at all drops `required` entirely because it can never be satisfied under `additionalProperties: false`; nodes with `$ref` / `$dynamicRef` are left alone because their definitions are sanitized where they live.

- **Asynchronous image jobs no longer treat the HTTP 102 keepalive as the final failure (#691 @bxb1337, #693 @Jayson1998).** In-process text-to-image and edit calls ran the Images handler against `httptest.ResponseRecorder`, which commits the first `102 Processing` heartbeat as the final status, so a job that then returned a perfectly good image was marked failed and the image JSON was truncated into the 2000-character `error_message`. The internal recorder now ignores informational responses (101 Switching Protocols excepted) and keeps the complete final body and status. The two PRs landed the same fix under the same type name in different parts of the file, which merged without a conflict and briefly broke the build on `main`; the duplicate was removed.

- **Two flaky race-detector tests stabilized.** The continuous-retry affinity cleanup test asserted right after `ctx.Done()`, but the deadline timer cancels the context before running its timeout cleanups, so the assertion occasionally raced the unbind; it now waits on a sentinel cleanup registered after the bind. The Codex refresh persistence retry test dropped its fault-injection trigger from a raw SQLite connection with no busy timeout, which failed immediately with `SQLITE_BUSY` whenever the store held the WAL write lock and left the trigger in place past the retry budget; the injector now uses `busy_timeout(5000)`.

## v2.9.8 - 2026-09-16

### Features

- **Downstream HTTP / SSE and WebSocket keepalives so reverse proxies stop cutting silent streams (#673, @bxb1337).** Behind Cloudflare (125 s proxy read timeout, error 524) or Nginx (`proxy_read_timeout` 60 s), an upstream that reasons for a long time without emitting bytes let the proxy close the idle connection even though the model was still working; accounts flagged for cyber safety buffering are hit hardest because the upstream withholds the whole turn until it finishes. `/v1/responses`, `/v1/chat/completions`, `/v1/messages`, `/v1/responses/compact`, `/v1/alpha/search`, `/v1/images/generations` and `/v1/images/edits` are now kept alive across header wait, body read and SSE read. Streaming requests commit the final `200 text/event-stream` on the first keepalive tick and flush a protocol-native heartbeat (`: keepalive` SSE comment for Responses / Chat / Images, Anthropic `event: ping` for Messages); once the heartbeat has committed the stream, later upstream errors are delivered as the protocol's error event instead of an HTTP status. Non-streaming JSON requests use `102 Processing` as a best-effort signal (Cloudflare still requires the final response within 125 s of a `102`, so it cannot extend them indefinitely). Per-key model request quotas no longer commit the SSE before admission, and the heartbeat starts right after admission. Responses, Realtime and Live sideband WebSockets send empty Ping control frames for the whole downstream connection lifetime and close the connection when a ping write fails. Image upscaling keeps its own request-level keepalive loop while the blocking upscale runs. New `DOWNSTREAM_HTTP_KEEPALIVE_INTERVAL` (default `30s`) and `DOWNSTREAM_WS_KEEPALIVE_INTERVAL` (default `45s`) accept Go durations and `0` disables them; the heartbeat is a fixed period that normal model output does not reset. Claude OAuth streaming keepalive remains governed by `stream_keepalive_enabled`; video, asynchronous image jobs and `POST /v1/live` are out of scope.

- **Team / Enterprise workspace credits can override usage-window limits, workspace hard stops are enforced, and Personal Access Tokens learn their workspace (#662, @CberYellowstone).** Non-owner members of a Team workspace receive `credits: {"has_credits": true, "balance": null}` from `/backend-api/wham/usage`, and the credit-override check demanded a positive balance, so those accounts never bypassed the 5-hour / weekly windows. Credit availability is now decided as: credits valid → workspace hard stop (`spend_control.reached`, `workspace_owner|member_credits_depleted`, `workspace_owner|member_usage_limit_reached`) → upstream `overage_limit_reached` → `unlimited || has_credits`. Sparse `x-codex-credits-*` and `x-codex-rate-limit-reached-type` response headers are folded into the credit state before the window-limit decision (sparse headers only apply when both has-credits and unlimited are present and parseable, matching the official client; `rate_limit_reached_type` is per-response, so usage responses without it clear a learned hard stop), a known zero balance is not "credits available" and a hidden / empty balance is treated as unknown; drawing down credits no longer runs a row-lock transaction per request, and a failed snapshot persist rolls back the fingerprint so the next probe retries. `at-...` Personal Access Tokens are hydrated through `/backend-api/whoami` on add / import (after route dedupe, with a detached per-token timeout and a per-batch breaker), lazily before the hourly WHAM tick with a 6 h backoff, and on manual refresh, so `chatgpt_account_id`, email and plan are filled and PAT accounts with a workspace join the official usage sync; a PAT sharing a workspace with an OAuth account is no longer merged into it. The account page shows `✓` for credits-available-but-hidden balances, hides the badge on a hard stop or a known zero balance, and the usage modal explains workspace spend limits. Also fixed along the way: a monotonic sequence clock for the routing scheduler cache (Windows sub-millisecond clock resolution caused LRU eviction jitter), continuous-retry temp-file cleanup on Windows, and an SQLite connection leak in proxy risk scoring.

- **Codex egress is resolved in one place, and Resin override is visible in the UI (#679).** Resin reverse proxy, the proxy chain (account > group > pool > global `proxy_url`) and direct connection coexisted through scattered `if IsResinEnabled()` branches in the HTTP, compact, WebSocket and maintenance paths, so it was not visible which one was in effect and the proxy pool was silently skipped. `ResolveCodexEgress` / `ResolveCodexWebsocketEgress` / `CodexDialProxyURL` now decide the outbound chain with the fixed precedence Resin > proxy chain > direct, used by executors, the WS relay, WHAM / subscription / telemetry maintenance requests and the upstream audit label. Resin enablement is mirrored into the scheduler so the proxy-pool fail-closed filter no longer skips Codex accounts Resin would carry (Claude / Grok / Antigravity keep the old rule). The settings API returns a read-only `codex_egress` summary; the settings page Resin card shows active / inactive with an explanation, the proxy pool page warns when Resin overrides it, and the Codex account proxy badge gains a `Resin` state. Startup and update logs print the Resin endpoint as `scheme://host/***` instead of the full URL with its token.

- **Process-wide request memory budget (`CODEX_REQUEST_MEMORY_BUDGET_MB`) and bounded response-cache writer.** A single logical budget (default the larger of 128 MiB and the per-request body limit; an explicit value cannot go below the per-request limit) now covers read / decompressed, queued and in-flight HTTP and WS bodies including Realtime session bodies. When it is exhausted HTTP returns `503` with `Retry-After: 1` and WebSocket closes with `1013`. Shared-backend response cache writes (async and sync) are limited to 16 in-flight writes, 64 MiB in-flight logical bytes and 64 waiters, acquire their slot before copying / encoding with a 5 s wait cap, keep a 2 s I/O deadline (500 ms for shutdown flushes) and let an oversized single context take the writer exclusively rather than dropping it; L1 keeps serving after a backend write failure. `/api/admin/ops/overview` exposes `request_memory` (budget, current, high-water, rejections), `response_cache_writer` (in-flight / waiting writes, bytes, timeouts, rejections) and `response_cache.backend_write_failures`. Native Responses WS turns now also write the local replay context (except explicit `store:false`) so HTTP fallback can use a full snapshot. This is a logical body budget, not an RSS ceiling.

- **Codex background request kinds get their own transport lane.** Memory-consolidation turns (`x-codex-turn-metadata` `request_kind=memory`) and guardian classifier requests (`x-openai-subagent`) run concurrently with the user turn on the same session / thread identity and used to share its lane and preemption key, so a background request could cancel the in-flight user turn on WS ingress or queue behind it in the pool. `turn` / `prewarm` / `compaction` / unset stay on the main lane; every other kind gets its own, and subagent requests without thread metadata are laned by subagent value (header metadata wins, body `client_metadata` is the HTTP fallback; values verified against Codex CLI 0.154.0).

- **Preempted Responses WebSocket clients receive a `1013` close frame.** When a newer same-session connection replaced the active one only its context was cancelled, so the old Codex client saw a bare `1006` and could idle until its own timeout. Preemption now writes a `1013` close with reason "session preempted by a newer connection" and cancels after the frame is out or a 1 s grace; late errors on the old connection classify as preempted. Verified locally: the old connection closes within 15 ms while the new one streams.

- **WS relay waiters wake immediately on pool changes.** Waiting for a busy same-session connection or account capacity polled with 10–200 ms exponential backoff, so a released connection could sit idle up to 200 ms. A per-account broadcast channel is now fired by pending request removal, connection discard, capacity release, expiry eviction and connection removal; waiters take the signal before inspecting pool state so wake-ups are never missed, the backoff timer remains as a fallback and accumulated wait counts real elapsed time.

- **Strict first-token mode retired.** The strict / loose toggle is removed from the settings page and `first_token_mode` is normalized to `loose` in runtime defaults, database defaults and Postgres / SQLite normalization; stored `strict` values keep working and are treated as loose. The column and API field remain for compatibility, no migration is needed.

### Fixes

- **`internal_chat_message_metadata_passthrough` is stripped from Responses input items.** Codex CLI attaches this field to input items when a custom provider is named "OpenAI" and the ChatGPT backend rejects it with `400`. It is deleted from top-level input items only, never from content parts or function arguments that happen to carry the same key; the Grok path already dropped it through unknown-key rejection, the Codex passthrough path did not.

## v2.9.7 - 2026-09-13

### Features

- **HTML animation quality check ("降智检测") studio with background jobs, prompt presets and SVG export.** A new admin page at `/admin/quality-test` compares selected accounts, models and reasoning efforts on an editable HTML / SVG animation challenge (default: a pelican riding a bicycle). Tests run as server-side background jobs (`POST /api/admin/accounts/:id/quality-test` returns `202`), so switching pages or closing the tab does not cancel them; jobs are persisted in a new `quality_test_jobs` table with the account identity and subscription snapshot, model, effort, prompt, status, usage and generated content, listed through `GET /api/admin/quality-tests` and cancellable through `POST /api/admin/quality-tests/:id/cancel`. A database-backed slot limits running jobs to 3 across all admins and instances with at most 1 active job per account (`409` when full, no queueing); a single job is capped at 10 minutes and 1 MiB of output, graceful shutdown saves an `interrupted` result and crashed jobs are reaped after the deadline plus a 30 s grace. Reasoning effort is built per channel (`reasoning.effort` for Codex / Responses / Grok, native `thinking` + `output_config.effort` for Claude); the test reuses the account's proxy, credentials, usage and cooldown handling and bypasses public `/v1/*` key scheduling, billing and payload rules. Results render in an isolated iframe served by the credential-free `GET /api/quality-test/preview` container with its own CSP, with a formatted and highlighted source view, timing / first-output / token metrics, HTML download and an "export SVG snapshot" action that captures the largest visible SVG including script-generated paths and page styles (capped at 10000 elements / 8 MiB). Prompt presets come in two kinds: 7 built-in challenges shipped with the frontend (pelican, analog clock, solar system, bouncing balls, gears, city parallax, Chinese stroke order) and custom presets stored in a new `quality_test_prompts` table via `GET / POST / PATCH / DELETE /api/admin/quality-test-prompts`; a job may carry `prompt_id` to count preset usage. History opens in a dialog, `?job=<id>` deep-links a result, and a Playwright regression (`npm run test:svg`) guards the SVG export. No automatic "degradation score" is produced; metrics are aids for comparison.

- **Codex subscription status is tracked server-side with sync state, manual refresh and a renewal-date column (#670).** Subscription sync metadata (`subscription_checked_at` / `sync_state` / `source` / `error` / `last_known_status` / `renewal_detected_at` / `will_renew` / `grace_until`) is persisted in account credentials without a schema migration, and the server computes a business status (`active` / `expiring_today` / `expired` / `grace_period` / `unknown`) by business day in the configured timezone, exposed as a `subscription` object on account list and detail responses. Clearing a stale expiry when a paid plan header comes back now records a "renewed, pending confirmation" state and immediately triggers an authoritative sync instead of silently blanking the badge; grace periods are respected. The subscription endpoint is queried with the Codex CLI identity first and falls back to the browser disguise only on 403, since the browser headers are what trigger the anti-bot challenge on some exit IPs; the remaining challenge case is surfaced as a "bind a proxy and retry" hint. Refresh-token rotation no longer lets an older JWT expiry overwrite a provider-sourced value. New endpoints `GET /api/admin/accounts/:id/subscription` and `POST /api/admin/accounts/:id/subscription/refresh` (30 s per-account rate limit with `Retry-After`), a `subscription` filter on the paged account list and batch selector, and the list projection now carries the subscription keys so snapshot rows are not all judged unknown. The frontend replaces the two `ExpiryBadge` copies with a shared `SubscriptionBadge` (`YYYY-MM-DD 续费`, green > 9 days / amber 4–9 / red ≤ 3, overdue days when expired, tooltip with checked time, source, sync state, auto-renew and failure reason, refresh button), adds a "续费时间" table column and a subscription-status filter.

- **Ultra mode is recorded and filterable in usage logs (#669, reported by @yy1105384898).** Codex clients that select an "Ultra" effort send `reasoning.effort: xhigh` on the wire together with a `<multi_agent_mode>` developer message enabling proactive multi-agent delegation, so the gateway's logged effort of `xhigh` was correct. The gateway now detects that marker (last message wins, continuation frames inherit by `turn_id` within a 6 h in-process cache) and stores it in a new `usage_logs.ultra` boolean column; the usage page shows a glass "star field" badge on the model with a tooltip explaining the mode, gains an Ultra toggle under more filters (`?ultra=true|false`, treated as a dimension filter so the range cards follow it) and a `500` status chip.

- **One-click usage refresh for the whole Codex pool.** `POST /api/admin/accounts/batch-refresh-usage` (Accounts → Manage → refresh usage) refreshes the 5-hour / weekly usage snapshots of every WHAM-capable Codex account in the runtime pool regardless of pagination, search, filters or selection; accounts without an access token, Agent Identity accounts, relay accounts and other channels are skipped. It only queries `/backend-api/wham/usage`, never refreshes login tokens or falls back to the token-consuming `/responses` probe, keeps the old snapshot on failure and does not treat a lone WHAM 401 as invalid credentials. Concurrency follows `usage_probe_concurrency` with a 15 s per-query cap; `?stream=true` yields `start` / `progress` / `complete` SSE events, a concurrent run returns `409`, client disconnect cancels pending work, and list / analysis caches are invalidated before the completion event.

- **Connection tests and quality tests are recorded in usage logs with a recognizable type.** Account connection tests (`connection_test`) and quality-check jobs (`quality_test`) on Codex, Claude, Antigravity and Grok accounts now write a usage log entry projected from the test diagnostics (Claude input counted as uncached + cache read + cache write), including reasoning effort, first-content timing and the last error message; unreachable upstreams are recorded as `598` / `transport`. The usage page's Type column labels them "测试连接" / "降智测试" by `internal_reason` instead of the generic internal-request badge, and connection test dialogs show reasoning effort and first-content timing.

- **Model pricing page rebuilt around a pricing sync panel.** The page is split into a `PricingSyncPanel` for official-source synchronization (manual sync, scheduled sync toggle with interval hint that preserves custom prices, source management) and a filterable price list with source / channel filters, a rate-expression preview, inline per-model editing of standard, image and premium (long-context / priority) rates with per-field revert, an output-to-input ratio hint and a bulk-save scope notice.

- **Sixteen new themes and a redesigned theme settings page.** Eight color themes (Ocean, Forest, Lavender, Amber, Espresso, Sakura, Mint, Midnight) and eight website-inspired presets (GitHub, Vercel, Google, Apple, Notion, Stripe, Spotify, Slack) join the picker; website presets also switch surface, card, button and sidebar styling. The theme page gains a live preview, light / dark / system descriptions and per-preset UI feature notes, and the no-flash bootstrap in `index.html` is covered by tests.

- **Account card view refresh.** The Codex account list's card layout is rebuilt with dedicated styles, a `card` variant of the request-count pills, and per-card details / configure / usage actions with a quota-and-usage panel.

### Fixes

- **Antigravity Responses adapter accepts inline images in user messages and tool outputs (#667, @frank8ai).** `input_image` / `image_url` parts carrying `data:<mime>;base64,...` URIs in user messages are converted to Gemini `inlineData` parts, and images inside `function_call_output` (e.g. browser or computer-use screenshots returned by tools) are attached to `functionResponse.parts`, instead of the adapter rejecting the request as an unsupported content part. Non-user images and remote URLs are still rejected with a clear error, with unit tests for both paths.

## v2.9.6 - 2026-09-11

### Features

- **Per-image user billing for image models and a public Image Studio quota endpoint.** The model pricing page can switch an image model's user billing from token pricing to a per-successful-image price (`user_billing_mode: per_image` with `image_unit_price` in USD, via `PUT /api/admin/model-pricing`). Successful image count × unit price is written to `user_billed` and drives key cumulative quota, total spend and group / account budgets, while upstream token cost still lands in `account_billed`; failed, cancelled and internally retried images are not charged and partially successful multi-image jobs bill only the delivered part. Image APIs settle after a successful response and Studio jobs after the image is saved. Usage logs and the public key usage records gain `user_billing_mode`, `image_unit_price` and `billed_image_count`; repricing never rewrites history, and GPT Image 2.5 dated / 2K / 4K aliases share their Flare / Sunburst price. A new `GET /api/image-studio/quota` (Bearer API key, gated only by the Image Studio portal flag) returns the key's cumulative quota limit / used / remaining, expiry, `active` / `quota_exhausted` / `expired` status, a `refresh_after_seconds` hint and the user-facing image pricing without upstream cost rates. The public Studio shows the remaining quota beside the key (click for used / total / expiry), the per-image price above the generate button, and refreshes while visible, on return and after job completion; an exhausted key can still read its quota and existing jobs but not generate, edit or delete.

- **Usage statistics cards follow the active dimension filters.** The range cards at the top of the usage page now use the same account / API key / model / endpoint / fast / type / transport / retry / search parameters as the log table, so filtering to one account or model shows that slice's requests, tokens and cost instead of the global total. Cumulative fields stay global, and status-class filters (status, error kind) are ignored for statistics. Account and model cells in the log table are clickable to toggle the corresponding filter, the model dropdown keeps the selected model even when it disappears from the current page, and the dimension fingerprint is folded into the 30-second statistics cache key so different filter combinations never share a cached result.

- **Codex accounts can be bound to an IANA timezone.** Account edit and batch update accept a `timezone` field (empty clears it). For official Codex accounts the gateway rewrites `<timezone>` and `<current_date>` inside the outbound `environment_context` after payload preparation, shifting the date by the whole-day difference between the account's zone and the client's; an unbound account passes the client values through, and relay / Grok accounts ignore the field. Claude accounts keep using the same field as their identity timezone. The rewrite is covered by tests for escaped and unescaped context blocks.

- **Simulated Codex client telemetry (experimental, off by default; #666, @bxb1337).** When enabled from Settings → Codex → Client telemetry (`codex_telemetry_enabled`), ordinary Responses requests on Codex OAuth accounts asynchronously emit analytics events to `chatgpt.com/backend-api/codex/analytics-events/events` and OTLP metrics to `ab.chatgpt.com/otlp/v1/metrics`, following the selected Desktop / CLI fingerprint, the account proxy and Resin when active. Events mirror the CLI structure (thread initialized, per-turn events, hook runs, randomized tool-call / command / file-change events with accepted-line fingerprints); they are synthesized, not observed from the request. Metrics are aggregated per metric and attribute set with count / sum / min / max / unit-appropriate histogram buckets and delta temporality, startup metrics are sent once per thread and increments every 60 seconds, the fingerprint batch goes in its own request, redirects are rejected so bearer tokens cannot cross an HTTPS → HTTP downgrade, and sending uses a small worker pool with a bounded queue. `CODEX_TELEMETRY_ENABLED=false` forces the feature off at deployment level and `CODEX_STATSIG_API_KEY` overrides the public Statsig key. A separate timing probe (`codex_telemetry_timing_debug` / `CODEX_TELEMETRY_TIMING_DEBUG`) prints `[TELEMETRY-TIMING]` setup and stream-parse costs and collects nothing when off.

- **Codex OAuth refresh is guarded against double-consuming a refresh token and can run in lazy mode.** Refresh now records a persisted attempt (SHA-256 of the old refresh token, operation id and start time, no plaintext) in a new `codex_oauth_refresh_attempts` table before exchanging, updates every workspace credential that shares the same refresh token in one database transaction, and publishes to memory / cache only after the save succeeds; transient database failures retry the save rather than the OAuth exchange, and an unconfirmed attempt survives restarts and lock timeouts so a possibly-consumed token is never replayed. Concurrent forced refreshes share the first exchange's result. Account detail shows the last Codex refresh time and error (`codex_last_refresh_at`, `codex_refresh_error`), and `refresh_token_expired` / `revoked` / `invalidated` / `reused` outcomes are explained in the troubleshooting guide. A new setting `codex_oauth_keepalive_enabled` (default off) lets lazy mode keep Codex tokens renewed on the existing background interval without enabling generation probes; normal mode is unchanged.

### Fixes

- **Grok Responses accounts can serve Codex remote compaction v2 instead of being pinned to Codex accounts (#661, reported by @OneAHPro).** Streaming `POST /v1/responses` with a `compaction_trigger` input now routes to Grok accounts whose catalog uses the Responses protocol, following the compact model mapping, and is sent to the account's real `/responses` route. When the upstream explicitly rejects the trigger type with 400 / 422, the gateway issues one summary request on the same account, model and context and returns a portable `compaction` item wrapped as `axisrelay-emulated-compaction-v1:` Base64 that later turns restore as a summary message under normal scheduling; empty, incomplete or tool-calling summaries fail rather than pretending the history was compacted. Upstream-native opaque state is recorded with its producing account and returned only there (503 `compaction_upstream_unavailable` when that account is unavailable), and a rejected known-origin Grok state surfaces the error instead of silently dropping it. The standalone `/v1/responses/compact` endpoint and non-streaming triggers remain on the dedicated Codex path.

- **Public portal switches no longer flip off after saving unrelated settings (#663, reported by @gps5).** The settings update response omitted the public key-usage, Image Studio and account-portal page flags, so the admin UI's post-save merge treated them as disabled and the next auto-save persisted `false`. The update response now echoes those flags along with the Grok OAuth client id display fields, and a new test asserts that every field returned by the read endpoint is also present in the update response so the two cannot drift apart again.

## v2.9.5 - 2026-09-10

### Features

- **GPT Image 2.5 Flare / Sunburst models with image-token billing.** `gpt-image-2.5-flare` and `gpt-image-2.5-sunburst` join the built-in catalog, `/v1/models`, the Image Studio model picker and the OpenAPI / API docs, each with the gateway `-2k` / `-4k` size-tier aliases. The 2.5 family accepts the `xhigh` and `max` quality tiers in addition to `auto` / `low` / `medium` / `high`; switching back to `gpt-image-2` in the Studio downgrades those tiers to `high`. Omitting the model still defaults to `gpt-image-2`. Usage extraction now reads the upstream image-token details (`input_tokens_details.image_tokens`, `output_tokens_details.image_tokens` and the cached image subset), stores them in three new `usage_logs` columns and bills them at dedicated `image_input` / `cached_image_input` rates without double-counting the text totals. Built-in pricing carries the two 2.5 models, dated snapshots and size aliases fold to the base rate, the official pricing sync also parses the image table, the pricing page shows image-rate fields and the usage tooltip breaks out image tokens and cost.

- **The image text driver is configurable from Settings.** The Responses model that invokes the image-generation tool can now be chosen in a new "Codex → Image settings" section, persisted in `system_settings.codex_images_main_model` and applied live. Precedence is system setting → `CODEX_IMAGES_MAIN_MODEL` → built-in `gpt-5.6-luna`; an empty setting follows the deployment default, and the admin settings response reports the effective default read-only. Image-only models and malformed names are rejected. The v2.9.4 unsupported-driver fallback chain (`gpt-5.5` → `gpt-5.6-terra` → `gpt-5.6-sol` → `gpt-6-astra`) is unchanged.

- **Usage guide and rebuilt in-app documentation.** A new `docs/USAGE.md` (linked from both READMEs) walks through first-time client setup, addresses and credentials, SDK / streaming / tool-calling / structured-output guidance, image, video and advanced endpoints and an error-to-action table. The admin `/admin/docs` page is rebuilt around quick start, developer guides, endpoint reference, troubleshooting and operations sections with search, bookmarkable section URLs, a copy-as-Markdown export (placeholder keys) and per-client configuration snippets that use the selected real key. `docs/API.md` gains asynchronous image-job and token-estimation details, and the troubleshooting doc clarifies that public endpoints require an API key unless anonymous access is explicitly enabled. Documentation content and navigation are covered by tests.

### Fixes

- **Transient upstream 429s freeze the account briefly instead of burning quota cooldowns or silently retrying.** A Codex 429 that carries neither a usage-limit body nor `x-codex-primary-*` / `x-codex-secondary-*` window headers is now classified as a transient account-wide throttle. The account is frozen in memory on an escalating ladder of 15s → 30s → 60s → 120s → 240s → 300s, raised to the upstream `Retry-After` when longer and capped at 5 minutes; the ladder resets after 5 minutes without a new 429. The freeze never shortens or replaces an existing quota or unauthorized cooldown (the runtime cooldown cache merges by strength atomically, Redis included), is never persisted, and does not arm a usage probe. Pro-only / Spark 429s share the same account freeze instead of a per-model cooldown, so a model alias cannot bypass it. When every candidate is throttled, the pool error now says the accounts are temporarily throttled and carries a `Retry-After` header (embedded in the message on WebSocket), distinct from the "usage window limit reached" message that tells the client to fail over.

- **Scheduler selection is bounded and prefers less-busy accounts under bursts.** Round-robin, `remaining_quota` and `fill_first` selection scan a fixed candidate window (8, affinity 1) without per-request allocation or sorting, find the priority segment by binary search and run filters, group checks and acquisition outside the scheduler lock with a generation fence against stale candidates, so imports and updates no longer stall dispatch. Within a burst the scheduler prefers the candidate with fewer occupied requests. Admin ops scheduler metrics add scan, filter, acquire-failure, lock-wait and selection-duration buckets.

- **Scheduler wait queues are bounded and wake only the requests they can serve.** Requests waiting for a free account are now queued per API key with limits of **4096** process-wide and **256** per key (`CODEX_SCHEDULER_MAX_WAITERS`, `CODEX_SCHEDULER_MAX_WAITERS_PER_KEY`, restart-scoped). When a queue is full the gateway returns **503** with `Retry-After: 1` (Anthropic `overloaded_error`, in-stream error on committed SSE, WebSocket close 1013) instead of parking the request, and the overflow bypasses continuous retry. Account release, slot expiry and reclaim now wake waiters that can actually use that account (excluded and differently-bound waiters are skipped), lanes rotate fairly, concurrent notification-driven selections are capped at 8, and the 1-second recheck timer runs only while waiters exist. Keepalive heartbeats no longer re-queue a waiter. New wait metrics report queue depth, rejections, grants and duration buckets.

- **API key authentication is cached in two revisioned layers.** Key lookups go L1 in-process LRU (15s, 4096 entries / 16 MiB) → L2 Redis (5m) → database, with negative entries held for 2s in L1 only; raw keys and `quota_used` are never cached and oversize rows bypass the cache. A new `api_key_auth_cache_state` table plus database triggers bump a generation whenever an API key is inserted, deleted or has its name, key, enabled flag, quota limit, expiry, groups or limits updated, so every cached snapshot is validated against the database revision within 250ms; a Redis Pub/Sub invalidation channel shortens propagation further and admin edits invalidate explicitly. A failed revision check returns 503 rather than serving an unverified snapshot. `CODEX_API_KEY_AUTH_CACHE_ENABLED=false` restores the previous behaviour, and `GET /api/admin/ops/overview` exposes `api_key_auth_cache` hit / miss / revision statistics.

- **Runtime limit reads are batched and concurrent key lookups coalesce.** RPM / RPD / daily and 5h / 7d / 30d limit windows are read in one request-scoped batch (Redis `MGET` / pipelined `HGETALL`, chunked at 128), shared scope-budget buckets are pipelined, background scope-delta writers are bounded to 16 slots with synchronous fallback, and concurrent lookups of the same API key share a single database query without adding any cross-request TTL or negative caching of infrastructure errors. Redis pool statistics add wait count, wait duration, timeouts and pending requests, and the admin `usage_percent` now reflects in-use connections over pool size.

## v2.9.4 - 2026-09-09

### Features

- **Image Studio gets a complete creative workspace and collection redesign.** The admin studio now places the prompt editor and grouped controls beside a large preview canvas, keeps the generate action accessible while settings scroll, and shows recent creations in a thumbnail strip. Local artwork and editable inspiration starters help with the first prompt. The prompt library adds favorites, stable tag counts, instant search and sorting; the gallery gains grid / masonry layouts, orientation filters, image details and keyboard previous / next navigation; history groups jobs by date with status summaries and expandable prompts, errors and warnings. Gallery and history filters explicitly apply to the current page, and pagination recovers when the last page becomes empty. Desktop, mobile, dark mode and English / Chinese layouts are covered, and template searches and gallery pagination no longer reload unrelated collections.

- **Native Gemini API for Antigravity OAuth accounts (#639, proposed by @kslr; #658).** Adds `/v1beta/models`, model detail, `:generateContent`, `:streamGenerateContent` and `:countTokens` endpoints with native Gemini request / response envelopes and SSE output. Generation reuses the Cloud Code executor, scoped downstream keys, model tiers, quota handling and OAuth refresh; token counting uses the upstream count endpoint with a local estimation fallback. Multi-turn function results, inline images and thought signatures are normalized for the adapter. Gemini SDKs and aggregator gateways can authenticate with `x-goog-api-key`, alongside the existing Bearer and `x-api-key` forms; credential precedence and owner scoping are preserved, query-string keys remain rejected, and credential headers cannot be configured as upstream request-id headers.

- **Codex User-Agent client presets, version pairing and per-account identity pools (#653, reported by @bxb1337; #654).** Settings now offers TUI, Desktop, VS Code, Exec and custom client shapes, including platform / architecture choices, terminal markers and paired CLI / application versions. An optional weighted pool assigns stable combinations per account instead of generating an unrelated identity on each request. The catalog and unsaved-configuration preview use the outbound identity rules and report incompatible combinations. Client names can contain spaces, and generated `Originator` headers follow the generated User-Agent prefix across HTTP and WebSocket requests, so a Desktop identity is no longer paired with a CLI originator.

- **OpenAI Responses relay accounts can preserve downstream Codex identity (#656, @xiaomingchen).** A per-account `codex_passthrough_mode` supports `off` (default), `auto` for recognized official clients, and `always` for explicit identity forwarding. Passthrough carries the supported `x-codex-*` headers and session / thread identifiers and skips installation-metadata injection and rewrite retries for those requests. Generated relay identities now also supply `Originator` and `x-codex-app-version`. Official-client restriction errors expressed as `forbidden_error` join the existing access-restriction recovery path when metadata injection is applicable. Account add / edit, persistence, scheduler snapshots and the UI expose the setting.

- **Built-in proxy IP risk scoring with live batch progress (#593, @ifThink404).** Proxy management integrates the Scamalytics v3 adapter without requiring a separate scoring service. Administrators can configure opt-in scoring profiles with server-side credentials, timeouts, concurrency, request spacing, cached results, daily / job budgets and credit protection. Cancellable background jobs report incremental per-proxy results and retain latest / historical snapshots; the proxy table separates connectivity, risk, network features, ownership and suggested actions. Unknown values and scoring failures remain distinct from low-risk results. DNS resolution is restricted to eligible public IPv4 addresses by default, and scoring is advisory: it does not automatically disable proxies or change account bindings or routing.

- **CY evidence can assist prompt-rule drafting and attribution (#593, @ifThink404).** Learning-review candidates and incident details show their linked risk profiles, and administrators can choose the AI provider and model to draft a narrow rule from upstream CY evidence, including context-only evidence when direct prompt text is unavailable. Suggested drafts prefill the editor with attribution, validation diagnostics and evidence-match counts; saving and publishing still use the explicit review workflow. Draft validation follows the manual rule checks, identity-clause validation accepts natural-language and negated security boundaries, and the review table keeps rule text and action buttons inside their columns.

- **Prompt audit logs gain configurable retention with CY evidence protection (#593, @ifThink404).** A new retention configuration defaults to **7 days**, supports up to 365 days, and uses **0 to disable automatic cleanup**. Hourly background cleanup and the manual run action remove expired prompt-filter logs, risk events and orphaned source records in bounded batches, with last-run statistics exposed in the UI. Rows linked to an existing upstream CY incident are protected from retention and ordinary log clearing; explicitly deleting an incident also clears its linked audit logs. The retention configuration and proxy risk-scoring storage are added for SQLite and PostgreSQL. Deployments that require longer audit history should adjust the retention setting when upgrading.

### Fixes

- **Image generation recovers from an unsupported driver model.** The Responses model that invokes the image-generation tool now defaults to `gpt-5.6-luna` instead of `gpt-5.4-mini`, with `CODEX_IMAGES_MAIN_MODEL` available as an override. When an upstream rejection explicitly names the current driver as unsupported, the request tries the next fallback driver on the same account and tracks rejected candidates to avoid loops; rejection of the image tool itself does not trigger this fallback. Image-2-family `-2k` / `-4k` aliases retain their base tool model and share prompt-aware orientation, size validation and upscale planning across generation, edits and the Studio size controls.

- **Codex model listings and defaults stop selecting retired GPT-5.4 models.** `gpt-5.4` and `gpt-5.4-mini` are removed from built-in and synchronized Codex listings, and connection tests now default to `gpt-5.5`. SQLite and PostgreSQL upgrades migrate saved connection-test settings that still name either retired model. Default Claude-to-Codex mappings send Opus / Sonnet aliases to `gpt-5.5` and Haiku aliases to `gpt-5.6-luna`; fallback selection, client examples and model selectors are updated accordingly.

- **Zero-output incomplete responses enter the retry path instead of becoming empty answers (#657, @james-6-23).** A `response.incomplete` terminal event is treated as `empty_incomplete_response` only when `output_tokens` is the literal integer zero, no output items are present and the attempt has emitted no nonempty text, reasoning or tool delta and no completed output item. The failure reuses the existing pre-output retry / account-switch budget across Responses, Chat and native WebSocket handling, and exhaustion returns a 502 rather than a successful empty reply. It is classified as request-scoped and does not penalize account health. Genuine truncation with output and terminal responses with missing, null or nonliteral-zero token counts retain their existing behavior.

- **Codex tool schemas and Chat tool names are adapted without losing client-facing names (#657, @james-6-23).** Pure `oneOf` / `anyOf` unions with at least eight distinct constant branches are folded into equivalent enums only when the branch constraints and any existing enum allow it, avoiding the large-union trigger for silent upstream termination. Chat and Anthropic tool conversion explicitly preserves the non-strict default when `strict` is omitted. The Chat route sanitizes invalid function / custom-tool names, resolves collisions and enforces the upstream length limit consistently across declarations, tool choice and history, then restores the original names in streaming and non-streaming responses.

- **Antigravity tool and structured-output schemas no longer forward invalid Gemini shapes (#655, reported by @yy1105384898; #657).** Tool normalization removes orphaned `required` entries, repairs bare property maps and boolean required flags, supplies missing array item schemas, resolves local definitions and nested JSON Pointers, flattens unions and moves unsupported tool enums into descriptive hints before sending `parametersJsonSchema`. Function names are normalized and restored on responses. Native Gemini `responseJsonSchema` / `response_json_schema` is converted to `responseSchema` with local-reference cleanup, while an explicit `responseSchema` takes precedence. Escaped JSON Pointer tokens, sibling overrides and cyclic-reference fallbacks have regression coverage.

- **Claude advanced-tool-use beta is only inferred for the features that need it (#657, @james-6-23).** Ordinary tool declarations no longer automatically add `advanced-tool-use-2025-11-20`. The header is inferred for tool-search tools, deferred loading, input examples or allowed callers; explicit incoming beta headers continue through the configured allowlist. This aligns generated Claude Code headers with the actual request features.

- **Translated responses preserve thinking-token and service-tier metadata (#657, @james-6-23).** Codex-backed `/v1/messages` responses expose reasoning usage as `usage.output_tokens_details.thinking_tokens` in both terminal streaming usage and non-streaming responses. The detail is accepted only when reported as a nonnegative number, clamped to total output tokens, and omitted when absent rather than fabricated as zero; it is a breakdown of output usage, not an additional charge. Chat Completions also carries the upstream `service_tier` in non-streaming responses and terminal chunks.

## v2.9.3 - 2026-09-07

### Features

- **Antigravity catalog refresh, Gemini 3.8 Flash and folded reasoning tiers (#646, @dundunge).** Gemini 3.8 Flash is routed through the `gemini-3.8-flash-tiered` wire model with `low` / `medium` / `high` tiers expressed as a named `thinkingConfig.thinkingLevel` instead of the older numeric budget ladder, a 1M context window and a 65,536-token output cap; bare `gemini-3.8-flash` defaults to `low`. OAuth Antigravity accounts now refresh their model catalog in the background (10 s after start, then every 30 minutes, four workers, generation-guarded merge) from the list-only quota endpoint; quota groups and credits are carried over from the previous snapshot, and an empty or malformed result keeps the last successful catalog instead of withdrawing every model. The snapshot records the raw `catalog_model_ids` and provider-flagged `internal_model_ids`; internal models, `chat_*` placeholders, `tab_*` completions, `-tiered` backings of known bases and the legacy `gemini-2.5-flash*` aliases (when `gemini-3.1-flash-lite` is present) are hidden from public lists, while unrecognized text models are published and routed under their actual upstream ID without inferring tiers or modalities. `/v1/models` and the scoped Codex manifest fold the `-low` / `-medium` / `-high` variants into one base model carrying `supported_reasoning_levels` and `default_reasoning_level`; the suffixed IDs stay callable. A base-model request on the auto channel is routed to Antigravity when any dispatch-enabled account supports it, `reasoning.effort` is validated per model (400 listing the allowed values, default `low`), API key allow / deny lists match the base model as well as its variants, and locally routed base models replace stale upstream manifest entries. Scoped manifest items carry the fuller Codex shape (description, shell type, visibility, base instructions, truncation policy, reasoning-summary flag). `/health` returns `build_version` and local builds render as `本地版本 YYYY-MM-DD HH:mm` in the UI. Known limitation: older Codex menus may still list extra tiers; the server rejects unsupported values.

- **Antigravity connection tests, per-channel test settings and model redirects (#650).** Antigravity accounts gain the shared "Test connection" action: the admin test endpoint no longer rejects the channel, and single, batch and recycle-bin tests run through the native Cloud Code executor with endpoint fallback, one refresh-and-retry on 401 for OAuth accounts and 429 / 503 quota cooldown through the newly exported `proxy.ApplyAntigravityCooldown`. Antigravity is relay-style, so every generic relay branch now excludes it explicitly. Automatic model selection prefers the configured channel model and otherwise the newest Flash `low` tier, because synced catalogs keep retired versions (`gemini-3.5-flash` answers with a deprecation notice and breaks the stream). The Codex test dialog with its `codex_diagnostics` panel is extracted from the Codex page into a shared component and reused by the Antigravity page. A new `system_settings.channel_test_config` column with `GET` / `PUT /api/admin/settings/channel-tests` stores the test model, test content and batch concurrency per channel for Claude and Antigravity (concurrency `0` follows the global value and applies only to single-channel batches); the settings page shows a connectivity card at the top of both tabs whose model choices are the union of the pool's catalogs so dated Claude IDs match, and the Claude dialog and backend default selection honour it. A new `system_settings.antigravity_config` column with `GET` / `PUT /api/admin/settings/antigravity` stores model redirects: a bare logical model such as `gemini-3.8-flash` is routed to a configured fixed tier such as `gemini-3.8-flash-high` on every entry point; by default only a missing `reasoning.effort` is filled in, `redirect_overrides_effort` makes the redirect win over an explicit effort, suffixed tiers are never redirected and invalid mappings are rejected with 400. Verified on the local deployment with real Antigravity and Claude accounts.

- **Custom (free-text) tool calls across Chat Completions and Messages.** Chat Completions accepts `tools[].type="custom"` (flat or nested declaration), carries history and responses via `tool_calls[].custom.name` / `custom.input`, and streams custom input under `delta.tool_calls[].custom.input`; custom input is always opaque text even when it happens to be valid JSON, ordinary functions keep `function.arguments`, and non-stream responses rebuild tool calls from `output_item.done` when the terminal `response.output` is empty. Messages wraps free-text input as `{"input":"…"}` because `tool_use.input` must be an object; the `tool_use` id carries a reversible type tag (`toolu_c2custom_` + base64 of `[call_id, namespace]`) so later turns restore the original `custom_tool_call`, `custom_tool_call_output`, `type=custom` declarations with namespace grouping and `tool_choice` without guessing from names or field shapes. Streamed custom input is emitted as escaped `input_json_delta` fragments, `input.done` / `output_item.done` reconcile the final value, an empty terminal value falls back to the streamed content, and calls whose final value diverges from the streamed deltas or exceed the size limit end the turn as an upstream protocol error. Grok accepts a custom `tool_choice`.

- **Native Responses WebSocket continuation replays local context and fails closed.** The native `GET /v1/responses` ingress used to forward `previous_response_id` as-is and, on degrade, strip the id and send only the incremental input while caching that partial input as if it were the whole conversation, so a later fallback injected a snapshot missing the root message, tool declarations and earlier calls (upstream 400 or silent amnesia). Continuation turns now build a portable replay snapshot from the local response cache plus the current increment while still sending only the increment over a healthy upstream connection. Completed and `incomplete` responses are cached from `response.output_item.done`, so message-only turns, `phase`, tool call / result pairs and Lite `additional_tools` declarations survive; only committed responses are written under the write policy, TTL and size limits, valid L1 ancestry is pinned at turn start so TTL expiry during generation cannot break the snapshot, missing ancestry never turns a partial input into a supposedly complete snapshot, and oversize snapshots are marked rather than tail-trimmed. When a continuation cannot be restored (cache miss, oversize, non-portable encrypted state, shared-backend failure) the bound-account-excluded, `previous_response_not_found` and HTTP fallback paths return a `response_context_unavailable` (close 1008) or `service_unavailable` (close 1011) error frame and close instead of silently continuing without history; a lossy fallback is flagged separately and never written back to the replay cache. Snapshots are built lazily per attempt after `response.completed` rather than on the first-token path; shared-backend (Redis) writes run in the background through a bounded slot pool that falls back to synchronous writes under pressure (never dropped, private copies, drained on shutdown, no new background work once draining starts), while local L1 admission stays synchronous so the next frame on the same instance sees the previous turn. Under the `on_demand` write policy a root turn without an explicit `store:false` grants the session write eligibility so its ancestry exists when a later turn degrades; explicit `store:false` sessions (Codex CLI full-context) are never cached under any policy, message-only turns included. `CODEX_WS_CONTINUATION_FAIL_OPEN=true` restores the legacy strip-and-forward behaviour.

- **Structured-output schema policy for native WebSocket Responses Lite.** JSON Schema `minLength` / `maxLength` constraints are preserved for `gpt-6-astra` and `gpt-5.6-luna` when Responses Lite is explicitly enabled — via `client_metadata.ws_request_header_x_openai_internal_codex_responses_lite=true`, the `X-OpenAI-Internal-Codex-Responses-Lite: true` header, or a payload rule injecting that metadata — and only for structured outputs; tool-parameter sanitization keeps the conservative rules. Length constraints are kept through request preparation and decided once before the final outbound hop against the final model, the rule-rewritten Lite signal, the account's Lite capability and the actual transport, so other models, requests where Lite ends up off, HTTP and compact requests keep the existing sanitization and an HTTP fallback never carries constraints that do not apply. Request preparation and terminal-recovery serialization were tightened along the way. Seven live matrix cases (Luna / Astra × metadata / header, Astra without a signal, `gpt-5.5`, HTTP ingress) passed against the real upstream; the WebSocket-to-HTTP fallback re-check is covered by unit tests only.

- **Claude API Key accounts send account custom headers and can present a Claude Code client identity (#647, proposed by @zjm54321).** The v2.9.2 API Key + Base URL accounts forwarded a fixed neutral header set and ignored `credentials.custom_headers`, so gateways that inspect the caller's characteristics could not be served. `custom_headers` now apply to every API Key upstream request (Messages and `/v1/models` discovery), layered last so they override the defaults and may set `User-Agent`; the gateway-owned headers (`Authorization`, `x-api-key`, `Content-Type`, `Content-Length`, `Host`, `Accept`, `Accept-Encoding`, `Transfer-Encoding`, `Connection`) are rejected with 400 on import and scheduler update and skipped at runtime. `claude_fingerprint_mode` gains API Key semantics: empty (default) = passthrough, byte-for-byte the previous contract; `force` = always present the deterministic Claude Code CLI identity headers (`claude-cli/<version>` User-Agent, `X-App`, `X-Stainless-*`, retry / timeout and direct-browser-access headers); `preserve` = keep a real CLI client's identity and fill in the rest, non-CLI clients treated as `force`. API Key accounts never inherit the OAuth global default and only headers change — no OAuth session state, system prompt or `metadata` identity is copied. Import and export carry `custom_headers` and `claude_fingerprint_mode` for `api_key` documents (the legacy `fingerprint_headers` name is accepted), detail responses expose the operator headers and a User-Agent preview, and the add / edit dialogs gain a client-identity select and a custom-headers JSON editor. Verified against a local echo gateway; not yet exercised against a real third-party gateway.

### Fixes

- **PostgreSQL `usage_logs` request-id indexes are built online after startup.** v2.9.2 created the two request-id partial indexes inside the startup migration transaction, so upgrading a deployment with a large `usage_logs` table could scan the table under the migration lock and block writes. All three `usage_logs` indexes (account generation plus the two request-id indexes) are now built with `CREATE INDEX CONCURRENTLY` in the background after startup, serialized across instances by a database advisory lock; `INVALID` leftovers from an interrupted build are dropped and rebuilt online, and a failed build only affects query performance and is retried at the next start. A dedicated PostgreSQL 18 CI job now runs the trace, index, capability and legacy daily-usage migration test.

- **Rejected encrypted-content memory now actually matches later requests, and its batch semantics are documented.** Under the default isolation mode the v2.9.2 memory keyed its scope on a per-request random outbound UUID, so a remembered digest never matched a follow-up request; it now uses the stable downstream session identity, extracted the same way for every supported auth form. The upstream rarely identifies which item it rejected, so the memory records every encrypted item in the rejected request rather than "only that item" as the v2.9.2 notes claimed; the docs state this limit and the gateway does not guess ownership from unknown ciphertext formats.

- **Claude bare refresh-token import no longer rotates an existing account's token before the duplicate check (#643 follow-up).** Re-pasting an already imported refresh token refreshed upstream first — rotating and invalidating the stored token — and then hit 409 and discarded the new one, and a 403 profile fetch could insert a duplicate row. Dedupe now runs before any refresh, the import reuses the background refresh lease, and duplicates make zero refresh calls. Token refresh no longer sends `scope`, inheriting the original grant. Plan inference no longer overrides a known OAuth plan (Max multipliers included) from model-call evidence, while Setup Token accounts can still be corrected. A model dropped after `credits_required` can be explicitly re-probed; a successful probe restores it to the persisted whitelist and clears the cooldown.

- **Claude API Key accounts: model discovery filters non-`claude-*` IDs, normalization re-reviews only changed prompts, and normalization errors use SSE-safe frames.** A gateway catalog mixing other vendors' models exported a list the importer then rejected; discovery now keeps only `claude-*` IDs and the mixed-catalog round trip has regression coverage. The prompt filter re-runs only when per-account normalization actually changed the prompt content, retries with the same normalized body are not reviewed twice, and a normalization failure after the SSE stream has started is reported through the committed-stream error guard. The import dialog and API docs now state that Claude Code / OAuth policies do not apply to API Key accounts while the generic prompt filter still does.

- **Codex connection diagnostics on WebSocket transport are taken from the turn, not the handshake.** Forced-WS tests reported the handshake's request id, `cf-ray` and headers timing; they now read request id and CF Ray from the `codex.response.metadata` frame, list the first frame separately as `first_frame_ms`, and update response headers by name. Short credentials are redacted on whole-word boundaries so IDs and timestamps containing the same substring are no longer mangled; standalone credentials are still masked.

- **Cycle projection no longer breaks the official usage response at exactly 0.5% used.** The confidence interval divided by zero at the 0.5% boundary and produced `+Inf`, which made the whole daily-usage response unserializable (200 with an empty body); the boundary is handled and the response stays serializable. SQLite and standalone PostgreSQL tests upgrade from the pre-breakdown table shape and check idempotence, defaults and preserved counts.

- **Model capability snapshots merge per field within a generation, are purged with the account, and the scoped manifest stops hard-coding Fast / Ultrafast.** A partial manifest from an older client could erase capabilities of newer models under the same credential generation; snapshots now merge per model and field, still bounded by model count and byte caps. Single-account and bulk purge explicitly delete `model_capability_snapshots` rows instead of relying on SQLite foreign-key cascades that are not enabled. Unknown capabilities stay conservative, `input_modalities` falls back to `text`, other accounts' numeric limits are not treated as a safe ceiling for an unknown account, and the scoped Codex manifest no longer hard-codes `gpt-5.6-sol` Fast / Ultrafast service tiers but uses the learned capability intersection.

- **Pricing overrides that only set Astra long-context fields return 400 instead of storing an empty entry.** The empty-override check ran before normalization stripped the long-context fields, so such an edit reported success and left a zombie row; validation now runs after normalization, an existing valid override is kept, and the store drops normalized-empty entries.

- **Request tracing: `X-AxisRelay-Request-ID` is exposed to browsers, compaction sub-requests correlate with their parent, and `/v1/live` records the trace.** The retrievable gateway id is now in `Access-Control-Expose-Headers` alongside the legacy `X-Request-ID`, whose contract is unchanged and documented; overflow-compaction sub-requests prefer the parent's gateway trace id; live connections snapshot the trace at connect and write it at settlement; proxy labels that embed URL credentials are redacted. Usage page: the merged client-equals-upstream User-Agent row keeps the inline request id, the green badge only shows with an actual UA audit, and Claude reset times use the configured display timezone.

- **Response cache shared payload pool releases map buckets when empty**, with added coverage for partial-overlap eviction, duplicates, same-key replacement, re-admission and old-reader ownership; the logical byte budget remains a cache-layer measure, not process RSS.

## v2.9.2 - 2026-09-06

### Features

- **Claude accounts accept a plain API key with a custom Base URL (#645, proposed by @zjm54321).** Anthropic-compatible relays and direct `sk-ant-api…` keys could not be used as Claude upstreams: the importer rejected any `auth_kind` other than `oauth`, and the outbound path hard-coded the official host with Bearer auth and the Claude Code identity. `POST /api/admin/accounts/claude/import` now takes `{"auth_kind":"api_key","api_key":"…","base_url":"https://host[/v1]"}`; the key is stored in the access-token slot so it inherits redaction and export protection, the address in `credentials.claude_base_url`, and no schema migration is needed. A Base URL whose last segment is `/v1` only gets the resource name appended, anything else gets `/v1/<resource>`. Requests to such an account send `x-api-key`, keep the client's body byte-for-byte (system, metadata and thinking signatures included), drop `oauth-*` / `claude-code-*` betas, never add `?beta=true`, the Claude Code preamble, fingerprint headers or a CLI User-Agent, and do not follow redirects. Every OAuth-only path is gated off for the kind — token refresh, usage probing, fingerprint generation, client platform/version policy, plan inference — while scheduling, proxies, groups, connection tests, cooldowns and export/import (which round-trips `base_url`) keep working. When the pool holds both kinds for a `/v1/messages` request, the raw body is kept for API-key accounts and only OAuth accounts get the per-account normalizer and prompt-filter rerun; API-key accounts skip the thinking-signature retry and the client-compatibility gate. Model discovery calls `<base_url>/v1/models` and the catalog is isolated per account. Known limits: the Base URL cannot be edited in the account dialog yet (delete and re-import), model ids must still be `claude-*`, and the path has not yet been exercised against a live third-party upstream.
- **Claude Setup Token and sessionKey onboarding, with a hosted callback page (#643).** Claude accounts carry `credentials.claude_auth_kind` (`oauth` | `setup_token`; rows without the key are `oauth`). `POST …/claude/oauth/auth-url` takes `mode=setup_token` to mint the official `claude setup-token` long-lived credential (`sk-ant-oat01-…`, `user:inference` only, one year, no refresh token); the default redirect is the hosted `https://platform.claude.com/oauth/code/callback` page, which shows `code#state` to paste, and exchange-code accepts `code#state`, a bare code or the whole callback URL. `POST …/claude/oauth/exchange-session-key` turns a claude.ai `sessionKey` cookie into OAuth credentials or a Setup Token server-side (organization lookup, PKCE authorize and token exchange through the account proxy with the browser-fingerprint client); the sessionKey is never stored or echoed. Import accepts `auth_kind=setup_token` and OAuth documents carrying only a `refresh_token` (the access token is refreshed before insert so duplicates and dead tokens surface immediately), and `POST …/claude/import-tokens` (old `/import-setup-tokens`) bulk-pastes up to 200 mixed `sk-ant-oat01-` / `sk-ant-ort01-` tokens with `<prefix>-N` naming, per-token pool proxies and 409 on duplicates. Setup Token accounts skip the OAuth usage endpoint and sample usage through the native Messages probe. Plan inference now reacts to upstream evidence: a `credits_required` rejection on a placeholder or Max plan downgrades the account to Pro, and a successful gated-model call from tests, probes or live traffic upgrades placeholder / Pro to Max. The Claude page gains a four-tab add dialog (OAuth / Setup Token / sessionKey / import), an `auth_kind` filter with `summary.setup_token`, and an inline Setup Token badge. These flows have not yet been exercised against live Anthropic authorization.
- **Codex connection tests emit structured `codex_diagnostics` and a diagnostics panel.** Following the Claude test dialog, Codex OAuth, AgentIdentity, OpenAI Responses and Grok relay tests now stream a `diagnostics` SSE event carrying `codex_diagnostics`: upstream HTTP status, time to headers, first content time, total duration, request and response model, transport (`http` / `websocket`), request id (the account's `upstream_request_id_header` first, then the common `x-request-id` family), `response.id`, `cf-ray`, `x-codex-plan-type`, the `x-codex-safety-buffering-*` pair (with a `safety_buffered` flag that is only true when a `safety_buffering: true` event actually appeared), last `response.status`, incomplete reason and error type/code, the `x-codex-primary-*` / `x-codex-secondary-*` usage windows, terminal token usage with cached and reasoning splits, a whitelisted header set and a redacted 64 KiB body preview. Two frames are sent — a header-only frame as soon as headers arrive and a final frame with `duration_ms` — and a request that fails before headers attaches the diagnostics to the `error` event. Forced-WebSocket accounts read the windows from the `codex.rate_limits` frame and safety buffering from `codex.response.metadata`. The wire key is separate from Claude's `diagnostics` so a mixed recycle-bin test never renders the wrong shape; the Codex accounts page shows it in the test modal with copy and retry.
- **Official daily usage is broken down by model and speed, backfills 84 days, and projects the current billing cycle.** The account usage dialog's official statistics now sync ChatGPT's daily token breakdown in parallel with the daily counts: per-day rows gain `breakdown[{model, speed, share, credits, usd}]` and `surfaces[]` (cli / desktop_app / exec / web), stored in three new `account_daily_usage` columns written independently of the counts upsert so whichever endpoint answers first cannot overwrite the other. The upstream percentage is a per-fetch peak scale, so it is only used as the day's relative share: `share` is the row percent over the day total and `credits` / `usd` are share × the day's official credits, so fast rows carry their own cost and the model mix is visible per day. Each endpoint decides its own backfill window: the first sync in a process, or a local coverage start later than 84 days ago, pulls 84 days, otherwise 7; the response reports `retention_days=84`, a `breakdown_refresh_error` when only the breakdown fetch failed, and only rows with counts evidence are shown or summed. Today's row is shown with a `*` once it carries tokens. Three new cycle cards use the account's 7d window snapshot: used cost since the cycle start, live used percent, and an estimated full-cycle allowance with a ±0.5% range that is flagged unreliable below 10% used or when no window / credits exist. Free-plan accounts with zero credits show the average daily share instead of cost.
- **Unavailable continuation ids are recovered in every transport, `service_tier=ultrafast` is preserved, and explicitly rejected encrypted content is remembered (#641).** `previous_response_id is not available for this user` is now recognized in HTTP error bodies and in SSE / WebSocket error envelopes and enters the existing single-shot continuation fallback as a deterministic 400 instead of a retryable failure; matching is limited to the error fields. `ultrafast` survives Responses, Chat translation, HTTP/WS forwarding and `x-codex-routing-hint`, the config generator offers it, usage logs show requested / actual / billing tiers, and billing defaults to the gateway's Fast rule (priority price override, otherwise the Fast multiplier) — a compatibility policy, not a statement of official Ultrafast pricing; tier availability and actual billing remain unverified with live traffic (see #582). When upstream explicitly rejects a reasoning or compaction `encrypted_content` item, its SHA-256 digest is remembered per downstream identity, session, account and credential generation (30-minute TTL, 4096 sessions × 256 digests, in-process only); a later request carrying the same item drops only that item before sending, other history stays intact, and "missing encrypted_content" is not treated as a rejection. The error observation is a bounded side read of HTTP and in-stream errors that never changes the response bytes.
- **Every request carries a gateway id and records the upstream id and proxy in the usage log (#641).** HTTP responses return `X-AxisRelay-Request-ID` and each WebSocket turn gets its own id; retries keep the gateway id and re-record the upstream id and proxy snapshot per attempt, hidden continue-thinking turns included. `usage_logs` gains `request_id`, `upstream_request_id`, `upstream_proxy_id` and `upstream_proxy_name` (both ids indexed; proxy names are captured at request time and never contain URLs or credentials). The upstream id is read from `X-Request-ID`, `Request-ID` or `X-Goog-Request-ID`, or from a per-account `upstream_request_id_header` set in the quick-config sheet or via the scheduler update endpoint (validated as a header name, credential headers refused, never sent outbound; WebSocket handshake ids are not mistaken for turn ids). The usage page shows both ids and the proxy, and the admin query accepts exact `request_id` / `upstream_request_id` filters and matches them in `q`.
- **Codex model capability snapshots survive restarts and are merged conservatively on manifest fallback (#641).** A successful `/models` manifest fetch persists the allowed capability fields to the new `model_capability_snapshots` table per account and credential generation; partial manifests keep the account's previous valid fields, invalid or unavailable manifests never clear a snapshot, and stale credentials or older async tasks cannot overwrite newer state. Manifest fallback merges only the accounts the calling API key can reach: booleans by intersection, context limits by minimum, support lists by intersection, unknown or conflicting capabilities treated conservatively. Instructions and other non-capability content are never stored. The Responses Lite forwarding decision is restored from the snapshot on restart instead of waiting for the next manifest.
- **Response cache replay shares immutable history payloads (#641).** Identical history items are shared between local cache entries and request rebuilds copy only the sequence structure; mutable read paths still return isolated copies, and eviction releases holds without breaking in-flight replays. Logical byte budget, entry cap, TTL, owner isolation and the Redis format are unchanged; the ops API adds `response_cache.shared_payload_bytes` next to the logical `current_bytes`. In the 128-turn replay microbenchmark cumulative allocation dropped from about 180 MB to 2.2 MB (about 98.8%), with a regression test pinning the same scenario under 12 MiB — a cache-layer measurement, not end-to-end latency or process RSS (see `docs/PROXY_COMPATIBILITY_IMPROVEMENTS.md`).
- **Account lists and the usage log table tightened across channels (#643).** Claude rows merge sampling state and last-sample time into one line, show the usage reset time inline with the bar (clock icon, `HH:mm` on the same day), use a flexible usage column, unified 13px semibold headers and tabular time columns, cutting row height from about five lines to three. Grok headers use icon sort indicators with `aria-sort`, inline reset time resolved in the display timezone and tighter row padding; Antigravity adopts the shared table shell, whole-row detail opening, a Grok-style auth-kind chip and a single-line updated time. The usage log shows cache read/write badges side by side, collapses the user-agent cell to one line when client and upstream agents match, mutes the date and the `/v1/` prefix, highlights the minority `sync` badge in amber and quiets empty-cell placeholders.

### Fixes

- **`gpt-6-astra` is no longer surcharged above 272K input tokens (#640).** v2.9.0 shipped the long-context rates ($20 / $2 / $75) for Astra, but Codex applies its long-context exception to this model and bills the same rates at every context length, so long sessions were overcharged. The long-context fields are removed from the built-in table and `pricing.json`, stripped when pricing overrides are loaded or updated so legacy custom prices and later official / flat-JSON / models.dev syncs cannot restore them, and hidden on the pricing page for Astra and its aliases (resolved via the canonical model). Standard custom prices, service-tier multipliers and other models' long-context pricing are preserved; regression coverage includes the 272K boundary, 1M-token inputs, cached input and the Fast / Priority / Flex tiers.
- **Antigravity accounts are excluded from the ChatGPT daily-usage probe.** The wham daily-usage probe and manual refresh admitted every non-relay account, so each probe round sent a Google-issued token to chatgpt.com and logged a 401 against every Antigravity account; the channel gate now rejects Antigravity alongside relay, Grok and Claude accounts on both the probe candidate list and the manual refresh, and the frontend stops offering official statistics for them.
- **Encrypted-content namespaces never hash the raw credential (CodeQL `go/weak-sensitive-data-hashing`).** The rejected-content memory derived its scope by digesting the `Authorization` header; it now reuses the existing non-secret per-API-key identity, carries owner, key identity, account id and credential generation as plain map-key fields and digests only the downstream session id, so static keys still get distinct namespaces without a credential in the hash.
- **Two admin test flakes are deterministic.** The snapshot-prune and mutation-invalidation tests raced the async request-stats refresh that bumps the cache generation and could skip caching entirely; they now pre-warm the stats cache. The Grok batch-import revive test's post-import probe refreshed fake credentials against the real `auth.x.ai`, whose permanent `invalid_grant` wrote `status=error` under the revive assertion; the token endpoint now points at a local TLS stub so the refresh only ever fails transiently and never touches the network.

## v2.9.1 - 2026-09-05

### Features

- **Per-model weekly request quotas on API keys (#637, proposed by @viktorcao).** Models such as `gpt-6` are metered upstream by weekly request count, so a key sharing an upstream account could burn the whole week's allowance and the existing RPM/RPD and 5h/7d cost/token limits could not stop it. `limits.model_request_limits` now accepts rules of `{model, window: "week", max_requests, timezone, reset_weekday, reset_time}` with exact ids or `*` wildcards, matched against the effective model after global and per-account mapping so aliases cannot bypass it; several matching rules all need budget and each counts once. Windows are fixed calendar weeks in the rule's IANA timezone (DST-aware, default `Asia/Shanghai`, Monday 00:00). Counting is one external HTTP request or one WebSocket `response.create`: the gateway deducts atomically right before the upstream send, and internal failovers, transport fallbacks and retries reuse the same request identity so a rule is charged at most once; a retry that maps to a different model must also have budget there. Exhaustion returns `429 rate_limit_reached` with `Retry-After` and a usage snapshot in `error.details`, is never treated as an upstream failure, and a counter store outage returns 503 instead of silently allowing. Counters and the idempotency ledger live in two new tables (`api_key_model_request_counters`, `api_key_model_request_ledger`) shared by all PostgreSQL instances; restarts, cache clears, usage-log deletion and cost resets do not zero them. Rule ids are server-generated and only `max_requests` and ordering can be edited in place; changing the model or reset schedule means a new rule starting from zero. The key editor's advanced limits, `GET /api/admin/keys/:id/model-request-usage`, and the public `/key-usage` summary (`model_request_usage`) show used/remaining/reset for the current week regardless of the report range.
- **Claude connection tests emit structured diagnostics and a dedicated result dialog.** Manual native Messages tests now stream a `diagnostics` SSE event carrying upstream HTTP status, time-to-headers, first content/thinking time, total duration, request and response model, the fingerprint mode actually used, request/organization/message ids, stop reason and error type, native token usage with cache read/creation and 5m/1h split (accumulated by latest value, never double-added), a whitelisted set of response headers (rate-limit and request ids only, never cookies or auth), and a redacted JSON/SSE body preview capped at 64 KiB with `body_truncated`. The Claude accounts page shows this in a new test modal with copy-to-clipboard and retry. Probe and test output budget defaults to 4096 tokens (capped by a positive `max_output_tokens`, `0` disables the cap), and a complete response containing only thinking passes.
- **Claude accounts page list area matches the Codex page.** The filter toolbar, active-filter chips, sticky batch bar, card and table shell, clickable rows, sortable headers (`sortKey`/`sortDir`, import time maps to `created_at`), inline header action menus and column visibility settings are rebuilt on the shared Codex styling; a hidden-by-default tags column, batch delete, healthy-only export and clear-search were added. The header action menu and column settings menu were extracted into shared components and the Codex, Grok and Antigravity pages adopt the same column-settings hook.
- **Provider visibility and a settings page layout pass.** The General tab gains a provider visibility picker: hidden channels disappear from the accounts switcher (hidden routes fall back to `/accounts`), dashboard channel breakdown and the usage channel filter; Codex is the fallback and always visible, default is all visible, stored in `system_settings.visible_channels_config` via `GET/PUT /api/admin/settings/visible-channels` as a separate column outside the main settings UPSERT. The tab bar becomes an in-flow sticky bar with a frosted section index that follows scroll, the header shows an auto-saving / N unsaved / saved status pill backed by a persisted snapshot, the bottom save bar only appears for dirty manual-save fields and offers discard, auto-save merges only its own keys, three single-toggle Codex compatibility cards merge into one row-list card, and the API endpoints reference is always expanded. Guard tests cover the section index, dirty tracking and every visibility consumer.
- **Codex model sync prunes stale rows, hides internal variants, and sorts discovered models newest-first.** Official model page parsing now only accepts ids in catalog card attributes (`data-model-slug`, `slug`/`name` props), so usage-example placeholders like a bare `gpt-5.6` are no longer learned; if a page redesign yields zero ids the sync errors out and keeps the local list. Rows sourced from the official docs that no longer appear on the page are removed on sync (reported as `removed`), giving the registry a delete path for earlier parsing noise; manifest-learned, manual and built-in rows are untouched. `-wm` internal variants (e.g. `gpt-5.6-sol-wm`) are rejected on admission and no longer exposed even if a stale row exists. Synced/learned models are listed ahead of built-ins ordered by version newest-first instead of being buried alphabetically after `gpt-image-2-4k`.
- **Bundled Codex CLI version raised to 0.153.3, with a "use synced version" shortcut for the minimum CLI version (#634).** `gpt-6` is version-gated upstream and required the minimum client version to be at least 0.153.3. The builtin and `system_settings.codex_min_cli_version` default move to 0.153.3, the settings response exposes `codex_effective_cli_version` (max of builtin and synced), and the Codex tab offers a one-click "use synced version" button for the minimum CLI version field.

### Fixes

- **`/v1/messages` and `/v1/chat/completions` SSE streams stay alive during upstream silence (#623, reported by @BlueOriginAI).** The translated Messages and Chat Completions paths only emitted a downstream heartbeat when request-level continuous retry was active; with default settings a Codex account serving the Anthropic or Chat protocol went silent for the entire reasoning phase after the first content frame, so idle-timeout intermediaries closed healthy streams as broken pipe. Both paths now use the same `startDownstreamSSEKeepalive` ticker as `/v1/responses`, serialized with translated writes behind the downstream mutex, never writing a comment before the first real body byte (pre-content transparent retry and real error statuses are preserved), and skipped when buffered continuous retry owns the writer — which also fixes `/v1/responses` writing comments into the private attempt buffer. On Messages a failed heartbeat records the write error and cancels the upstream read instead of blocking on silence. Live verified on the PostgreSQL + Redis deployment with 10s heartbeats landing on frame boundaries.
- **Codex Desktop clients are recognized and get the correct outbound `Version` (#638, by @luannanxian).** Codex Desktop sends `User-Agent: Codex Desktop/0.153.3 (...)` and may omit the `Version` header; the client-version parser did not accept the spaced `Codex Desktop/` token, so the gateway fell back to its builtin version and version-gated models such as `gpt-6-astra` rejected the request as an outdated client. The parser now accepts the desktop token; current desktop clients keep their UA with the right version, below-minimum ones are lifted to the minimum with compatible headers, both covered by regression tests.
- **Claude native requests present the real Claude Code CLI identity and report usage consistently.** Outbound requests now align with a Claude Code CLI 2.1.259 capture: `metadata.user_id` is injected as the JSON string `{device_id, account_uuid, session_id}` with `device_id` derived deterministically per account (overridable via `credentials.custom_headers.claude_device_id`, case-insensitive, preserved through export/import and timezone fingerprint rebuilds, never sent as an HTTP header) and `account_uuid` from the account; `X-Claude-Code-Session-Id` and the metadata `session_id` share one request-level UUIDv7 resolved from the dedicated header, then `metadata.user_id.session_id`, then the generic session sources, still isolated per API key and stable across failover retries. `anthropic-beta` is built in capture order (core `claude-code-20250219` except for haiku + `oauth-2025-04-20`, then body-driven pairs for thinking/context_management/tools/effort/1h cache/cache scope, then downstream betas filtered by a whitelist that defaults to the real CLI's beta registry instead of dropping everything unlisted); `context_management` is no longer stripped; fixed SDK headers (`X-Stainless-Retry-Count`, `X-Stainless-Timeout`, `anthropic-dangerous-direct-browser-access`) are set when absent; the system prefix is reordered as billing block, CLI declaration, then original blocks with their attributes; the billing block's `cc_version` is aligned to the final outbound UA in `preserve` mode; third-party client UAs (opencode, curl) are treated as `force` so the OAuth credential's CLI fingerprint stays consistent. Usage-log `input_tokens` for Claude use the total-input convention (including cache read/write) while the admin detail table's `↓` shows uncached input with a tooltip breakdown; `499` (client cancel) is excluded from the account list's 7d failure, retry-failure and error-code counts.
- **Capacity-overflow scheduler logs no longer print the raw affinity key.** CodeQL flagged clear-text logging of the affinity key, which is derived from downstream headers/body (`session_id`, `prompt_cache_key`, `X-Claude-Code-Session-Id`); the account ids already identify the binding and the borrowed account, so the key is dropped from both log lines.

## v2.9.0 - 2026-09-05

### Features

- **`gpt-6-astra` is now a built-in model with official pricing, pricing-page placement, and `max` reasoning effort.** The official docs sync and upstream manifest learning already admitted the id (#626), but a deployment that had never synced or served a manifest-carrying request could not call it, so the registry now seeds it at cold start — pinned to the top of the built-in order, with API-key auth marked unavailable like the rest of the flagship tier. Billing previously fell through every `gpt-5*` rule to the $1/$2 default and undercounted Astra usage by an order of magnitude; the pricing table now carries the official rates ($10 input / $1 cached / $50 output per 1M tokens, $20 / $2 / $75 above 272K, fast tier 2×), unknown `gpt-6` variants fall back to Astra instead of the default, `pricing.json` ships the same numbers for pricing sync, and the pricing page pins Astra ahead of the 5.6 family. `reasoning.effort=max` was clamped to `xhigh` for major-only ids because the version check required a minor number; Astra is documented with a Max level, so any `gpt-<major>` with major above 5 now passes `max` through. Verified against the live official pricing and models pages — the models page now 308-redirects to `learn.chatgpt.com/docs/models`, and the sync client follows it and still parses `gpt-6-astra`.
- **Claude Code OAuth accounts are a first-class upstream channel (#596–#601 landed via #607, by @ifThink404).** Accounts are added through the Claude Code OAuth/PKCE flow or by importing tokens, and requests to `claude-*` models on `/v1/messages` are routed natively to Anthropic with streaming, proxy binding, account groups, per-account model whitelists learned from upstream model discovery, usage probing (5h / 7d windows from `anthropic-ratelimit` headers plus zero-spend OAuth usage sync), connection tests, cooldowns and 429 handling, Anthropic pricing with official pricing sync, portable credential export/import with refresh-token dedup, and a dedicated Claude accounts page mirrored across dashboards, usage, proxies, scheduler, API keys and the model catalog. Credentials can optionally be encrypted at rest with `CODEX_CRED_ENCRYPTION_KEY`. Merging the six-stage stack exposed two regressions on the shared `/v1/messages` entry that were fixed before landing: the Claude request normalizer ran for every request and stripped `speed`, the carrier of the priority tier for Codex-routed Messages requests, and the text sanitizer deleted zero-width joiners and applied NFC, corrupting emoji sequences and NFD file paths — both are now scoped to requests actually routed to a Claude account, and the routing decision is memoized per request so a policy-stripped body can never reach a Codex account.
- **Claude client platform and version policies, with CLI version sync and fingerprint alignment (#610, #614, by @ifThink404).** Global and per-account policies can require Claude Code CLI clients, pin a fixed outbound version, or enforce a minimum version; Anthropic client-compatibility 400s are classified without cooldown or ban state changes. A production incident showed `fingerprint_mode: force` replaying the CLI version baked into each fingerprint at import time, so Anthropic rejected `claude-fable-5-1` with "version 2.1.251 or newer is required" even though the real client was newer. The gateway now ships a builtin CLI version and syncs the latest release from GitHub (npm dist-tags fallback) into `system_settings.claude_synced_cli_version`; the effective version is `max(builtin, synced)` so a bad remote value can never downgrade, generated fingerprints pin their UA to it, every sync and startup rewrites only the version segment of existing accounts' `User-Agent` (CAS-guarded), and the final outbound UA is aligned with the required version or fails closed with a local 426 before any upstream call. Admin probes bypass the policy gate so a CLI-only policy cannot mark the whole Claude pool as errored, and the pre-scheduler global gate that also hit Antigravity, relay and Codex-mapped `claude-*` requests was dropped in favor of the per-account gate. `CLAUDE_DISABLE_CLI_VERSION_SYNC=1` disables the network sync.
- **Claude first-token timeout, pre-first-token SSE keepalive and latency logs (#620, by @ifThink404).** Upstream occasionally sent `message_start` and then nothing for 4–9 minutes on `xhigh` effort with large contexts; with the global first-token timeout at 0 these requests held a concurrency slot until the client gave up, and downstream gateways that saw no bytes retried and multiplied the load. `ClaudeConfig` gains `first_token_timeout_seconds` (default 120, explicit 0 follows the global value, clamped to 600) and `stream_keepalive_enabled` (default on): a watchdog cancel before the first visible frame is normalized into a retryable first-token-timeout outcome, `: keepalive` comments are written every 15s until the first frame, and watchdog timeouts, pre-first-token disconnects and first-token waits of 60s or more are logged with account, model, effort and wait time.
- **Every channel can be refreshed from the model catalog button, with plan-group sampling and live SSE progress (#627, by @ifThink404).** The catalog dialog's refresh only called the Claude endpoint, so new Codex, Grok and Antigravity models never appeared there. `POST /api/admin/models/refresh-all` now refreshes all four channels in parallel under a 120s budget and writes what it finds back to each channel's source of truth. Probing every account does not scale, so each channel groups schedulable accounts by subscription plan and probes one random account per group: Codex runs the official docs sync plus one `/models` manifest per plan learned into the registry, Claude writes one `FetchModels` result back to every account in the group, Grok and Antigravity resync the sampled account. `?stream=1` streams `start` / `progress` / `complete` events and the dialog shows a per-channel progress panel with newly learned models as chips. The admin remote-model probe also learns new manifest slugs into the registry, add-only (#631).
- **Antigravity: automatic project onboarding, structured Google quota errors, streamed text and per-session ids (#609).** A Google account that had never used Antigravity returned an empty `cloudaicompanionProject`, so it was admitted, showed as healthy and was never scheduled; `fetchIdentityContext` now calls `onboardUser` and polls the long-running operation, surfacing failures as an entitlement warning. 429/503 bodies are parsed as `google.rpc.Status`: `RATE_LIMIT_EXCEEDED` under 3s retries in place, longer delays cool the exact (account, model) pair for the upstream-provided duration, `QUOTA_EXHAUSTED` also cools the shared Gemini family, and `MODEL_CAPACITY_EXHAUSTED` retries with a short cooldown without counting as an account failure. Text is streamed per frame as `output_text.delta` instead of being buffered until the terminal frame, with same-frame refusals still blocked and late refusals ending as `response.failed`. The hard-coded global `sessionId` — itself a pool fingerprint — is now derived from the strongest session identifier on the request.
- **Scheduling usage sort key is derived per upstream channel.** `remaining_quota`, `fill_first` and the `round_robin` healthy bucket sorted by the 7d usage percentage, which only Codex and Claude populate, so Grok and Antigravity degraded to dispatch-score order. Grok now uses the free-quota exhaustion snapshot and per-request `x-ratelimit-*` remaining tokens; Antigravity uses the persisted quota projection; stale observations count as 0 so recovered accounts are not pinned to the back of the queue.
- **System settings split into channel tabs.** The single scroll page mixing four vendors' options is now six `?tab=` panels — Codex, Claude, Antigravity, Grok, Appearance, General — with legacy anchors mapped onto tabs, a "Codex client & fingerprint" card split out of runtime optimization, and channel-scope badges on cross-channel cards stating which upstreams each option actually applies to (verified against backend consumers). A guard test locks tab structure, card placement and locale keys.
- **One proxy field for all four channels, with a portaled pool dropdown (#625, by @james-6-23).** Codex, Grok and Antigravity each had their own proxy input and Antigravity lacked pool selection; `ProxyField` (manual entry, test with IP geolocation, pick from pool) is now the only proxy field, and a value matching a pool node shows the linkage hint — the backend has always associated accounts with pool nodes by URL, cascading address edits and unbinding on delete, it was just invisible. The pool list renders through a portal with fixed positioning so it is no longer clipped by the modal body or buried under the footer, and pressing Escape with a portaled dropdown open now closes only the dropdown instead of the whole modal.
- **Account list proxy badge with a quick editor (#606), Claude account page polish (#611), and an invite-page clear button.** Every channel's list shows the account's effective egress as a badge: bound pool node with latency, inherited group / pool / global / direct, and — the real motivation — a red badge when an account is pinned to a disabled, failed or deleted managed proxy (fail-closed out of scheduling) or when the pool is on with no usable entry and no global proxy. The Claude page persists analysis-panel visibility (default collapsed, skipping the initial fetch), uses branded empty-state cards, shows the OAuth link before opening it, and all four channel pages move the toolbar below the title so the channel switcher stops truncating; proxy inputs gain an inline clear button.

### Fixes

- **Claude prompt-cache reads and writes are billed with Anthropic usage semantics, and cache-write tokens are persisted (#615, by @ifThink404).** Anthropic's `input_tokens` excludes cache reads and writes, but the gateway stored it as the prompt total and clamped `cached` to it, so cache reads were priced at about zero and cache writes were never recorded: a typical Claude Code turn billed $0.0069 instead of about $0.125. Native Claude usage is converted to total-input semantics, `cache_creation_input_tokens` and its 5m/1h breakdown are stored in the new `usage_logs.cache_write_5m_tokens` / `cache_write_1h_tokens` columns, cache writes are priced from the model pricing table (default 1.25× / 2× input), streamed cache writes are no longer double counted, and the usage page shows write tokens and cost. The Claude Code system preamble is injected with `ttl: "1h"` when the client's first `cache_control` block is 1h, since Anthropic rejects a 1h block after a 5m one.
- **Claude accounts stop routing models their plan does not include (#616, by @ifThink404).** A Pro account kept being selected for Claude Fable 5.1 every few minutes because the whitelist comes from upstream model discovery, not plan entitlement, and the 30-minute `credits_required` cooldown was overwritten by the generic 4-second adaptive 429 cooldown. The billing rejection now drops the model from the account's explicit whitelist, and `MarkModelCooldownWithBackoff` — shared by every channel's 429 handling — never shortens a still-active longer cooldown.
- **Claude requests with invalid thinking signatures or `thinking.type=disabled` are recovered instead of failing (#613, #617, by @ifThink404).** Clients that persist transcripts without signatures replay `thinking` blocks Anthropic rejects on every follow-up; the gateway drops blocks with empty or truncated signatures before sending and, if Anthropic still rejects, strips all thinking blocks and retries once on the same account — verified live that the bad signature comes from the client, so rotating accounts cannot fix it. Claude Fable / Mythos run with thinking always on and reject `disabled`, so the parameter is omitted for those models and, for any other model that rejects it (including the Opus 5 effort-vs-disabled phrasing), dropped and retried once. The system preamble is injected without `cache_control` when the client already uses the maximum of four blocks.
- **Claude 401 handling: first offence cools for 6 hours, not 24, and the state is described as unauthorized rather than banned (#604, by @ifThink404).** `ReportRequestFailure` wrote `LastUnauthorizedAt` before the cooldown decision, so every first 401 looked like a repeat and got the 24h tier; the upstream error detail is now stored so admins can tell revoked tokens from other auth problems, and every label rendering the unauthorized status or the banned health tier was swept across zh / en / zh-TW.
- **Same-timezone Claude account saves no longer fail on header casing (#621, by @ifThink404).** Freshly generated fingerprint headers (`X-Stainless-OS`) merged with stored canonical-cased ones (`X-Stainless-Os`) tripped the case-duplicate check about two saves out of three; keys are canonicalized before merging and existing identity headers win on same-timezone saves.
- **Re-importing an errored or unauthorized account revives it instead of leaving it stuck (#618, by @james-6-23).** After re-authorization a bare refresh-token import was merged into the old account by email + workspace identity, but the old row kept `status=error` and its unauthorized cooldown while the new row was soft-deleted, so the user saw the new account vanish and the old one stay unauthorized. Merges, identity upserts and re-authorization now clear the error / unauthorized state and the cross-instance cooldown cache (rate-limit cooldowns are kept), byte-identical duplicates revive an errored holder and count as `updated`, and a revive is not reported when the runtime reload fails.
- **Non-versioned Codex model aliases such as `gpt-daybreak-blue-latest` are admitted (#624, by @james-6-23).** Trusted Access for Cyber accounts list the alias in their manifest but the registry rejected any `gpt-*` id whose first segment was not numeric, so `/v1/models` never showed it and `/responses` rejected it. Letter-led codename families are now admitted, `daybreak` ids bill as `gpt-5.6-sol` (blue is sol, red is 5.6-cyber with no published price) and support `max` effort; digit-led ids that do not parse (`gpt-4o`) and punctuation-led ids stay rejected, and versioned ids like `gpt-5.4-daybreak` follow their own version rules.
- **The official Codex model page parser only accepts quoted ids or `codex -m` commands (#626, by @ifThink404).** Admitting major-only versions made the broad regex learn `gpt-6` from nav copy, `gpt-6-astra-in-enterprise` from an anchor and `gpt-6-astra-texture` from an image name; every real model on the page appears in a quoted form, so nothing legitimate is lost.
- **Message-only context overflow is classified as 400, not 500.** Relay upstreams report overflow as `{"code":null,"type":"upstream_error","message":"Your input exceeds the context window..."}`, which missed every keyword match and fell through to a retryable 500 — the gateway rotated through the whole pool and recorded a failure on each healthy account. The existing message-field overflow check is now wired into the shared status mapping, so HTTP and WebSocket ingress are fixed together; verified live with a 3.6MB request returning 400 on a single account.
- **Image generation with no image output surfaces the model's reply and recognizes natural-language refusals (#589, by @james-6-23).** When `response.completed` carried only model text, the gateway discarded it and returned a fixed "did not execute image generation" message, and refusals phrased in plain Chinese were not recognized as policy refusals, so every account was tried in turn and each left a 502. The error now includes a redacted preview of the model reply, a refusal-wording-plus-policy-topic heuristic terminates as 400 `content_policy_violation`, and the no-image outcome is logged with account, model and text.
- **CI `test-race` runs in minutes instead of ~13.** Every SQLite-backed test replayed the full schema migration, which costs about 1.2s per open under the race detector; a per-process schema template is copied in about 1ms instead, CPU-bound oversized prompt-filter fixtures skip under the race tag, and the proxy shard is split in two.

## v2.8.9 - 2026-09-01

### Features

- **Account export and import can carry proxy bindings, making a pool migratable in one piece.** Export accepts `include_proxy=1` to append `proxy_url`, `proxy_label`, and `proxy_enabled`; import accepts `import_proxy` to adopt the file-carried proxy and register it into the proxy table. Proxies are written and synced into the in-memory pool *before* any account row is written, and a failed reload aborts the whole import — reversing that order would pin accounts to a URL that is registered as managed but not yet enabled, which fail-closes them out of scheduling. Invalid entries are skipped and fall back to the form proxy, more than 500 proxies registers none at all, pre-existing rows are never revived by `ON CONFLICT DO NOTHING`, source-disabled proxies import as enabled with an explicit warning rather than silently killing every account bound to them, and new rows are labeled `imported-<YYYYMMDD-HHmm>` for batch cleanup. A file-carried proxy fills an empty binding but never overwrites an existing one, since the target may already have a deliberate assignment; a form-entered proxy keeps its existing overwrite semantics. Grok reads the proxy from raw file JSON because its credential parser has no proxy fields, and preserves the existing binding on the recycle-bin revive path. Antigravity already honored the file-carried proxy but never registered it, leaving accounts bound to URLs the proxy page and rotation could not see; its switch therefore only controls table registration, and its export keeps `include_proxy` defaulting on because it has always emitted `proxy_url`. Verified end to end against PostgreSQL, including `ON CONFLICT` idempotency, preserve-on-revive, and the export → import → export round trip.
- **Antigravity accounts are now admitted through `/v1/chat/completions` and `/v1/messages` (#595, reported by @maikaken).** Both endpoints already translate their inbound body into a Responses payload, which is exactly what the `v1internal` adapter consumes, so all three transports now share one admission gate and one adapter instead of excluding the channel outright. The Responses-to-Chat return translation only consumes SSE, so the upstream call is always streamed and non-streaming clients are aggregated gateway-side. An upstream 401 on an OAuth account refreshes the token and retries the same account once, matching `/v1/responses`, instead of discarding a usable account as an auth failure.

### Fixes

- **Antigravity function tools are bridged by default instead of being dropped (#595).** Silently discarding the tools was worse than not supporting them: the upstream still received the system instruction describing those tools, answered with a call it was never allowed to declare, and terminated the turn as `MALFORMED_FUNCTION_CALL`. Responses function tools now map to Gemini `functionDeclarations`, and previous `reasoning` items echoed back as conversation history are skipped rather than rejected, so multi-turn tool conversations survive. Built-in Codex tools with no `v1internal` equivalent are still ignored, and `ANTIGRAVITY_FUNCTION_TOOLS_ENABLED=false` pins the bridge off for diagnostics.
- **Re-importing a deleted Grok account no longer dead-ends on its own recycle-bin record (#602, reported by @a1073185067).** The credential identity fence includes recycled accounts by design, so re-importing a previously deleted `refresh_token` failed with "credential identity already exists (account N)" while the account list showed nothing — no visible way out. SSO, refresh-token, and `auth.json` file imports now merge the credentials into the holder and revive it from the recycle bin; a manual add that collides returns a 409 naming the holder and pointing at the recycle bin. The reauthorization overlay no longer falls back to the subject as an email, so a token without an email claim cannot clobber the account's real address, and import results carry `updated`/`revived` flags so the detail dialog can explain why no new account appeared.
- **Codex sibling subagents no longer preempt each other on a shared WebSocket connection.** A Codex session tree shares one session-id while each parent and child agent carries its own thread-id, so independent subagents were collapsed onto a single transport lane and cancelled one another. Genuine child threads now get their own local connection lane, derived from raw downstream identity before any fingerprint convergence — fingerprint policy governs what the upstream sees, not whether independent local streams must serialize. When convergence is off or device-scoped, the client's self-reported Thread-Id is preserved instead of being flattened into a single-thread shape.
- **The Messages-to-Responses streaming adapter no longer emits misaligned or partial output items.** Empty text and thinking blocks reserved an `output_index` but were skipped in the terminal object, so accumulators and the final response disagreed. Start-only blocks are now emitted before done, a second `content_block_start` without a stop begins a fresh message lifecycle instead of manufacturing done events for the abandoned item, a `content_block_stop` arriving mid tool-call JSON after a `max_tokens` cut is never announced as completed, and `input_json_delta` is treated as authoritative over the provisional start input.
- **Synthetic Responses objects keep a stable `created_at` across conversions.** Reading a complete upstream body can block until generation finishes, so the creation time is now frozen before that work starts, and stream-break frames reuse the timestamp observed on the stream instead of stamping the moment of failure.
- **Spark 429s stay scoped to the Spark model, and image capability cooldowns require structured evidence.** Spark has an independent quota window, so its `usage_limit` metadata must not rewrite the account plan or the main 5h/7d snapshots; only explicit quota evidence drives the Spark window, while transient rejections keep the ordinary short model cooldown. Separately, a `response.completed` frame containing plain model text is prompt-dependent and no longer persists an image-generation capability cooldown on the account/model pair.

## v2.8.8 - 2026-08-31

### Fixes

- **WebSocket connections now accept `socks5h://` proxies.** Managed proxy validation and HTTP probes already accepted `socks5h`, but the Gorilla WebSocket dialer only recognizes `socks5`, so forced-WebSocket requests failed locally with `proxy: unknown scheme: socks5h` even when the same proxy tested healthy. The WebSocket path now reuses the lenient proxy parser and normalizes only the connection-local scheme to `socks5`, preserving credentials and proxy-side DNS resolution; a fake SOCKS5 regression test verifies the domain-form CONNECT target, and a PostgreSQL + Redis 2004 live request confirmed the bound `socks5h` proxy completes a real upstream WebSocket response.

## v2.8.7 - 2026-08-31

### Features

- **Codex invite recipients are now protected by a durable global at-most-once ledger.** Recipient emails are normalized case-insensitively and atomically reserved before the upstream request, successful sends and historical tracking records remain blocked across restarts and sender accounts, definitive failures release their reservation, and ambiguous transport outcomes stay conservatively unavailable instead of risking a duplicate email. The invite page batch-checks both pasted emails and account-list candidates, keeps previously invited accounts visible with a disabled “Invited” marker, blocks manual duplicates, and clears the recipient input only after a successful send.

### Fixes

- **Disabled, unauthorized, errored, and banned accounts no longer appear in Codex invite selectors.** The sender and recipient dropdowns now share the same eligibility filter while retaining locked and temporarily rate-limited accounts whose referral credentials remain usable; recipient emails are still deduplicated and the selected sender is excluded.

## v2.8.6 - 2026-08-31

### Features

- **New Codex account imports can open an invite-earnings guide and reuse a richer invite workspace.** The default-on guide probes at most 50 eligible OAuth accounts through the existing adaptive import-probe gate, polls cached eligibility without delaying the import response, ranks accounts by referrer-only reward value and remaining rewarded sends, and can allocate a limited recipient-email budget to the highest-value accounts first. Successful eligibility and 90-day tracking observations are cached in memory/Redis and durable credential-generation-fenced snapshots so repeated page visits do not hammer the Cloudflare-protected endpoints; challenges and failures are never persisted as ineligibility. The invite page now shows monthly send capacity separately from 90-day sent/accepted/pending counts, supports explicitly probing another 50 accounts with tracking, and can select deduplicated recipient emails from the Codex account list while excluding the sender. Administrators can disable the post-import guide from Settings or the modal without disabling the invite page itself.
- **Grok reasoning streams gain an opt-in quality guard for degraded accounts (#570 and #587, reported by @a1073185067).** For streaming Grok 4+ requests whose reasoning is actually observable, the gateway can hold a bounded prefix and discard an answer with enough visible output but no real thinking delta, encrypted reasoning, or signature evidence, then rotate accounts before anything reaches the client. The guard defaults off and skips explicit no-reasoning requests, compaction turns, short replies, unobservable native streams, and replay-unsafe hosted tools. Defaults are six quality attempts, a 30-second hold ceiling, fail-closed exhaustion, and a 12-hour first-offense cooldown; a second offense after cooldown disables the account, while later healthy reasoning resets the penalty ladder. Empty streams use a separate 15-minute cooldown, usage from discarded attempts is retained, and fail-open remains available as an operator-selected escape hatch.
- **Response-context cache writes can be limited to downstream keys that actually use `previous_response_id`.** The new `on_demand` policy grants each API-key owner a sliding one-hour write window after any continuation lookup, then writes both local and Redis context only for that owner; deployments whose clients always resend full context can therefore eliminate unused tool-context write amplification. The compatibility default remains `always`, invalid values are rejected, the policy is persisted independently with the existing response-cache generation, and cache telemetry exposes skipped writes plus the number of currently eligible owners.
- **Database-backed API keys can be enabled or disabled without deleting or rotating them.** Existing and newly created rows migrate as enabled, the admin table exposes a guarded enable/disable action and a distinct disabled status, and a disabled key is rejected by the normal authentication middleware with a masked security-audit event. Environment-configured keys retain their existing behavior.
- **WebSocket connection logs expose the real upstream `permessage-deflate` negotiation result (#586, reported by @huyachigege).** The dialer already offered compression; the gateway now records whether the upstream accepted it, including negotiated parameters, once per distinct outcome bitmask so direct and relay routes remain observable without producing one log line per connection. Real-upstream verification observed `server_no_context_takeover` and `client_no_context_takeover` negotiation.

### Fixes

- **The experimental server-side Plus-to-Pro subscription-upgrade surface is removed (PR #591 by @Cd1s).** The undocumented ChatGPT Web billing path was frequently blocked by Cloudflare challenges and a successful payment could invalidate the OAuth credential family, making a server-side paid mutation too unreliable to keep. Quote, payment, verification and operation-history routes are no longer registered; their admin UI, settings switch, environment default, proxy client and documentation are removed, while unrelated read-only subscription-expiry synchronization remains. Existing installations keep any old nullable setting column and operation-journal table because the migration deliberately performs no destructive schema cleanup.
- **Antigravity sync failures preserve actionable Google account-recovery details.** Structured `VALIDATION_REQUIRED` and `TOS_VIOLATION` responses are parsed from the untruncated upstream body, rendered as persistent verification or Terms-of-Service diagnostics instead of generic transient warnings, and surface the supplied verification or appeal URL as a safe clickable action. Ordinary transient failures still keep their upstream message, partial quota sync remains non-fatal, and raw JSON blobs or truncated one-time links are no longer dumped into the UI.

## v2.8.5 - 2026-08-27

### Features

- **Administrators can run guarded Plus-to-Pro subscription upgrades (PR #583 by @Cd1s, disabled by default).** Quote-first admin APIs enforce exact amount caps, keep a durable idempotency journal across the paid path, submit payment at most once per account, and expose reauthentication readiness before any money moves. Hardening on top of the contribution: the enable switch moves out of the startup environment into the admin settings page (a stored value is authoritative and the environment can no longer re-enable it), the per-account lock is taken before quote resolution so two requests sharing a quote but carrying different idempotency keys cannot double-pay, post-payment journal writes and verification detach from the request context so a client disconnect cannot strand the one record that matters, only 401 counts as credential invalidation (a Cloudflare 403 falls to `verification_pending` instead of a misleading "payment accepted, credentials revoked"), a partial unique index on submitting rows guards against cross-instance double submission, the minor-unit factor is derived from the currency so zero-decimal currencies such as JPY are not displayed 100x off, and a strictly read-only `/verify` reconciliation endpoint releases operations stranded mid-submission without ever repeating the paid POST.
- **Antigravity OAuth clients are managed from the system settings page.** Clients persist in a dedicated settings column read and written by its own small UPDATE, so an unrelated settings save can never clobber them. Environment-provided clients stay read-only and win on key collision, the built-in official Desktop client remains the fallback when nothing is configured, client secrets are never echoed back to the browser, and submitting an empty secret keeps the stored one.

### Fixes

- **Active sessions no longer rotate accounts mid-conversation, preserving the upstream prompt cache (#584, reported by @a1073185067).** Bounded session affinity forced a re-pick after 50 requests or 5 minutes since first bind — and the clock never reset while the conversation stayed active — so long agent tasks lost their bound account mid-task and every follow-up message after a completed task landed on a different account, re-ingesting the entire context at the uncached rate. Rotation now happens only when the bound account turns unhealthy or the session has been idle past `CODEX_SESSION_AFFINITY_IDLE_ESCAPE` (default 10 minutes), by which point the upstream prompt cache has already expired and rotation is free. Sticky and continuation hits refresh the idle clock, and capacity-spillover borrows are logged since they imply an expected cache miss for that single request.

## v2.8.4 - 2026-08-27

### Features

- **Upstream failures can opt into configurable continuous full-attempt recovery (PR #549 by @Establishmentarian).** The default-off selective mode matches transport failures, 429, 4xx/5xx classes, stream failures, `response.failed`, context errors, exact HTTP statuses, or exact upstream error codes. A separate default-off catch-all super switch retries genuine upstream failures without relying on a known-code list, including unknown future failures and permanent quota, authentication, balance, invalid-request, or non-CYB structured safety-policy errors. An explicit upstream `cyber_policy` is always a hard stop: it is never replayed on another account and retains its signed decision, conversation lock, and verified-user cooldown. Streaming attempts are retained privately and discarded on retryable failure; only an HTTP 200 response with a normal protocol terminal is replayed to the client, while SSE comments or Responses WebSocket Ping keep the downstream alive. This intentionally trades real-time token delivery for transparent mid-stream recovery. Account rotation uses valid `Retry-After`, configured delay, and capped exponential backoff with jitter. Client cancellation, local validation/policy/spool failures, downstream write failure, WebSocket disconnects, and explicit CYB stop immediately. The UI warns that deterministic failures and media creation can repeatedly consume tokens, requests, balance, account quota, memory, disk, and duplicate charges.
- **Continuous retry now has a request wall-clock ceiling.** `max_duration_seconds` defaults to 600 seconds and starts only when a request first enters the unlimited retry budget; later attempts cannot reset it. Expiry cancels drainable upstream work immediately, releases request concurrency, returns the latest upstream failure through HTTP/SSE/WebSocket (falling back to `upstream_timeout` when none exists), and closes Responses WebSocket turns with 1013. Non-streaming Grok media keeps its JSON protocol and emits best-effort HTTP 102 Processing informational heartbeats during active retry waits and upstream I/O without committing the final status; intermediaries may still drop 1xx, so the wall-clock ceiling remains authoritative.
- **Antigravity becomes a dedicated Google upstream channel with OAuth account management, browser authorization, credential import, scheduling, scoped models, API-key channel restrictions, usage attribution, and an account UI.** OAuth traffic uses the Cloud Code `v1internal` adapter and can refresh once after an upstream 401. Antigravity account groups and model backing remain isolated from Codex and Grok.
- **Google API Key accounts can declare Gemini models and model mappings and target the Generative Language `v1beta/interactions` endpoint.** This path is explicitly experimental: local tests cover endpoint/header/body construction, but the complete Google request, SSE/JSON response, tool-call, usage, and error wire contract still requires authoritative real-upstream verification.
- **Antigravity gains dedicated admin credential export and a sanitized management plane.** One account downloads as secret-bearing JSON and multiple accounts as a sanitized-member ZIP; state reads are side-effect free, OAuth sync refreshes Google control-plane snapshots, API-key sync remains explicitly local/unverified, and bounded capability probes persist generation-fenced protocol observations.
- **An opt-in real Google Interactions integration test now exercises the production executor.** It is gated by `ANTIGRAVITY_INTERACTIONS_TEST_API_KEY`, supports safe model/input/SSE controls, validates native status/content types and bounded envelopes, and skips explicitly when no key is supplied. Production certification still requires a successful real run.
- **The routing scheduler gains a staged engine rollout: `legacy`, `shadow`, and fully `indexed` selection.** Indexed mode selects from priority/health buckets with sparse API-key sub-pools, deterministic affinity offsets, and event-driven availability waits; shadow mode keeps legacy authoritative while sampling one in 64 requests for candidate-parity comparison. `CODEX_SCHEDULER_ENGINE` pins an instance for a canary or emergency rollback, `FAST_SCHEDULER_ENABLED=true` remains a compatibility alias for indexed, and `/api/admin/ops/overview` reports the active engine, indexed/legacy selections, scan volume, event waiters, routing-cache state, shadow parity, and outbox lag. Grok account state updates move to scheduled maintenance jobs so probe results land without waiting on request traffic.
- **Sessions can briefly hold their account slot after a successful request (PR #555 by @ekti123456).** The default-off session slot buffer reserves the concurrency slot for a configurable duration (default 10 seconds) after a turn completes, so a fast follow-up turn keeps its account under concurrency pressure instead of losing the slot to an unrelated request; occupancy displays count buffered slots as occupied.
- **The model pricing page previews billing for every model, exact aliases get independent pricing controls, and accounts show upstream API balances and per-model average first-token latency (PR #578 by @ifThink404).** The pricing page renders a per-model cost preview from the effective layered price, exact alias entries (such as `codex-auto-review`) become independently editable, the accounts page can display each account's upstream API balance behind a badge switch, today's usage details add a per-model average first-token duration, and the frontend recovers cleanly from stale chunks after a redeploy by clearing runtime caches before retrying. Unsigned CY locks are also scoped properly: risk-profile listings gain upstream-CY-only and activity-state filters, and a lock created by a local rule is no longer presented as an upstream CYB lock.
- **A 5-hour window auto-activation keeps Team accounts rolling (default off).** Some Team accounts do not start the next 5-hour window until a real request is sent, so a pool that went quiet over a reset wastes the whole window. When enabled, the gateway sends one minimal request after a detected reset for accounts whose upstream usage still reports a 5h window and reset time. This consumes a small amount of quota and stays disabled by default.
- **Codex HTTP request bodies are zstd-compressed like the real Codex CLI (default on).** The real client compresses `/responses` requests by default, so an uncompressed gateway stood out; a settings switch can turn it off.
- **Proxy connectivity tests understand SOCKS5 and IPv6.** Probes now speak the SOCKS5 handshake directly against IPv4 and IPv6 targets with stricter response parsing and clean fallback for unreachable targets.
- **Official usage USD totals cover every retained snapshot instead of only the last seven days, and model-list reads are size-capped.** `official_usd_7d` becomes `official_usd` and includes older snapshots, so the account page total matches the persisted settlement history; a new `models_list_read_max_bytes` limit rejects oversized upstream model list responses. Codex AT accounts are excluded from official usage statistics and backfill, and the official usage tab only appears for eligible accounts.
- **`gpt-reserve` joins the built-in model list (PR #562 by @bGlzdGRlcg).**

### Fixes

- **Status-less `ErrUpstream(0, ..., cause)` transport failures keep their transport classification after continuous retry landed.** Executors wrap `http.Client.Do` EOF/reset as a non-legacy-retryable structured error; the classifier now unwraps that cause so sticky same-account transport retries still bind the original relay account.
- **Antigravity API Key accounts no longer enter OAuth unauthorized-refresh recovery, and API Key rotation rejects a duplicate credential family.** Account responses expose only the credential kind and never return the key itself.
- **The OAuth Responses-to-Gemini converter rejects empty unsupported input, converts OpenAI function declarations into Gemini `functionDeclarations`, and rejects unsupported tool types instead of forwarding malformed objects.**
- **Long Responses streams stay alive through downstream idle timeouts (PR #561 by @dundunge).** SSE keepalive comments are emitted toward the client during long upstream silences, so reverse proxies and clients no longer cut a healthy long-running stream and log it as a 499.
- **Cross-channel reasoning history no longer 400s on Codex (#565, reported by @a1073185067).** Planning on grok-4.6 and then switching to gpt-5.6 replayed Grok-produced reasoning items into Codex, which rejects foreign encrypted content and reasoning `status` fields. Response preparation now drops reasoning items whose encrypted content does not carry the Codex prefix and strips `status` from the ones it keeps.
- **Grok tool schemas with non-object roots are normalized, and string-form upstream error bodies surface (PR #580 by @ifThink404).** Union roots (`anyOf`/`oneOf`/`allOf`), boolean schemas, and `type` arrays are collapsed into the object root Grok requires instead of dying on the upstream 400. Follow-ups close the remaining shapes: merged union roots preserve sibling keys such as `$defs` so `$ref` pointers stay resolvable, same-name properties across branches nest into `anyOf`/`allOf` instead of first-wins merging (a `const`-discriminated union no longer makes later branches uncallable), bare local `$ref` branches are resolved before classification with a cycle-safe depth cap, and `type:["object","null"]` roots get the same required-relaxation as the equivalent `anyOf`-null form. Reserved-name renames (`tool_search` → `tool_search_fn`) now also apply to history `function_call` items, so a genuinely user-declared reserved name no longer diverges between its declaration and its call history from the second turn on.
- **Capacity spillover no longer steals a session's durable binding, and restored Grok tool-call IDs match their type.** When the slot buffer spills a busy session onto a sibling account, the original binding is preserved for later turns instead of being overwritten — while a filter-rejected account still migrates the binding as before. Relay `fc_*` item IDs restored as `custom_tool_call` or `tool_search_call` are re-prefixed (`ctc_`/`tsc_`) so strict Responses validators accept the replayed history.
- **Exact alias pricing keeps the documented custom > synced order and normalized key lookups (follow-ups to PR #578).** A synced alias entry no longer silently overrides an admin-entered custom canonical price; alias fields now layer over the effective canonical price instead of falling back to code defaults; and underscore or space spellings resolve to the stored hyphenated key. Traditional Chinese strings for the new pricing preview and first-token labels are backfilled.
- **The prompt-guard fingerprint replay check stays off the hot path and can actually match every endpoint (follow-ups to PR #578).** A global existence gate skips fingerprint envelope construction entirely while no active fingerprint lock exists (cached with a 10-second TTL), and the real endpoint and model are threaded into fingerprint derivation so cooldown locks created from images or search requests are no longer unmatchable. CY attribution also honors an explicitly configured Moderations review adapter instead of sending Chat Completions to it.
- **codex-local conversation locks can be found, unlocked, and audited again (PR #573 by @zzzhouuu).** The lock path hashed the session through the fallback-signal fingerprint chain while the unbound audit path hashed only the raw session ID, so the resulting `session_hash` values never matched: the risk profile could not attach the lock and the administrator could not unlock it. Both paths now share one fingerprint chain (covering `Session-ID` and `X-Codex-Window-Id` fallback identities) in the existing lock-table format. The local audit view also includes local blocks that were already reviewed by the external model, log search matches persisted session hashes, and local log cleanup is scoped to `source=local_filter` so other audit sections stay intact.
- **Spark admission honors `DispatchPaused`.** The Spark scheduler snapshot only consulted lock-protected state, so an account paused by an operator or by the Codex overload breaker kept receiving Spark traffic while standard traffic skipped it.
- **The payload-rule model mapping dropdown includes the `max` reasoning effort (#575, reported by @jyeric).**
- **The self-service account review entry no longer vanishes (#576, reported by @ExoticSkywing).** The pending-review panel stayed hidden on empty or stale list snapshots, so admins could not find `/account-portal` submissions; the panel now stays visible and the account list cache is invalidated on submit.
- **CI stays green honestly.** The admin race shard is split alphabetically to stop 12-minute test timeouts, and a flaky audit-queue drain assertion waits for the workers' completion signal instead of racing close's bounded early return.

### Security

- **Antigravity documentation now calls out plaintext API Key persistence, database/admin protection requirements, Google terms and suspension risk, preview pricing uncertainty, and the experimental Interactions contract.**
- **Antigravity credential downloads are admin-authenticated attachment responses with no-store/no-cache and nosniff headers.** Filenames and ZIP members are sanitized, while ordinary list/state responses remain secret-free.
- **Custom balance query URLs are restricted to the account's base URL host.** The balance probe sends the account's stored API key (`Authorization: Bearer`) and custom headers — a key that is designed to never reach the browser — so accepting an arbitrary absolute URL was both a key-exfiltration channel and an SSRF probe surface. Absolute URLs must now match the base URL host and must not carry userinfo; relative paths are unchanged, and a cross-host URL is rejected before any request leaves the gateway.

## v2.8.3 - 2026-08-21

### Features

- **Grok accounts can expose exact GPT-compatible aliases across the three normal HTTP text APIs (PR #547 by @Establishmentarian).** The Grok account editor now manages per-account mappings such as `gpt-5.5` to `grok-4.5`; routing validates each target against the account's visible catalog, conservative pre-sync defaults, and any explicit whitelist, and scoped model discovery advertises only routeable aliases. Responses, Chat Completions, and Messages share the mapping path, including the existing Codex function/namespace/custom/deferred-tool and `tool_search` bridge. Responses WebSocket and `/responses/compact` remain excluded, and provider-hosted tools still depend on the concrete Grok backend.
- **Spark usage is tracked independently of the account's ordinary 5h/7d windows.** Account responses and snapshots carry their own Spark usage percentage and reset time, and dispatch reads the Spark counters rather than inferring them from the standard windows, so a Spark-eligible request is no longer admitted or rejected on the wrong budget.
- **Liveness fails after a sustained account-store lock stall.** The non-blocking `/health` used to keep a deadlocked instance in service forever: the previous blocking handler would hang on the store lock, time out the container healthcheck and get the process recycled — an accidental but real self-healing valve that the `TryRLock` rework removed. A single failed `TryRLock` is still ordinary contention and returns 200, but when every probe fails continuously for 30 seconds `/health` returns 503 with `status=unavailable` and `blocked_seconds`, so orchestrator healthchecks recycle the instance without flapping under transient load.
- **Session, thread and window identifiers are derived as UUIDv7.** The identifiers are now time-ordered and deterministically derived from a seed plus timestamp instead of random v4 values, which keeps them unique while making a client session traceable in order. Installation identifiers stay v4.

### Fixes

- **The account store and the fast scheduler could deadlock the whole gateway (PR #553 by @ImogeneOctaviap794).** The two components could take their locks in opposite orders: request dispatch held `FastScheduler.mu` and then reached `Store.mu` through the egress filter's proxy resolution, while account add/remove held `Store.mu` and then called into the scheduler. A second, independent deadlock existed in the fallback, lazy, candidate-check and fresh-affinity paths, which ran account filters while already holding `Store.mu.RLock` — and Go's `RWMutex` blocks new readers once a writer is waiting, so a goroutine could end up waiting on its own nested `RLock`. Under load this froze every request that needed the account store, while static files and lightweight health checks kept returning 200, which made the process look healthy and prevented automatic recovery. Account-set mutations are now serialized by a dedicated mutex, `Store.mu` only covers the account slice and ID index, scheduler updates happen after that lock is released, and every filter runs against an account snapshot taken outside the read lock. Seven deterministic regression tests cover both lock classes.
- **A top-level envelope `type` field reached HTTP upstreams and produced 400s (#548, reported by @viktorcao).** After a Responses WebSocket connection hit a 1009 and fell back to HTTP, the request still carried the WebSocket envelope's top-level `type`, which the HTTP upstream rejects. The field is now stripped on every HTTP path — including the WebSocket fallback, continuation replays and forced-HTTP requests such as image generation and Agent Identity accounts — while nested `type` values are preserved.
- **`response.incomplete` is treated as a terminal state.** Upstream sends `response.incomplete`, not `response.completed`, when a request hits `max_output_tokens`, and that event still carries the full output and usage. Every terminal check only matched completed/failed, so an ordinary truncation was classified as a stream break: the gateway appended a synthetic failure terminal, discarded the real usage in favour of an estimate, and penalised the account with a 598. The damage was worst on `/v1/messages`, where the Anthropic translator had no case for the event at all.
- **Compaction triggers are normalized to the final input item, including a direct trigger object (PR #546 and PR #550 by @ImogeneOctaviap794).** Upstream rejects a `compaction_trigger` followed by any other input item. The gateway now keeps at most one direct trigger and moves it behind every history, message and tool item. A top-level `input: {"type":"compaction_trigger"}` object — which the request classifier already treated as a compact request — is wrapped into the same one-item array shape instead of being forwarded as an object, and non-canonical trigger type spellings are rewritten to the canonical wire value.
- **CYB conversation locks are auditable, and a Chinese credential-theft false positive is fixed (PR #552 by @ifThink404).** Risk profiles that are actively locked or cooling down are prioritized before pagination and can be filtered on their own, lock details expose the audit reference and decision id with a deep link to the original review log, and a lock created by a local terminal rule is no longer described as an upstream CYB lock. The `credential_theft` pattern no longer matches across separate Chinese clauses, so a benign request such as generating a login page and an export page is not treated as credential exfiltration; a follow-up keeps brand-named browser phrasings (`导出 Chrome 保存的密码`) inside the terminal rule.
- **Grok capability probes accepted truncated terminals, and native routes now run the preflight.** The probe body caps output at one token, so a reasoning model always finishes with `response.incomplete`. Because the Responses branch only counted `response.completed` as success, every reachable Responses endpoint was recorded as unavailable with `http_status=200`, which permanently disabled native passthrough for Codex→Grok and reported a live protocol as dead. The native branch also skipped the Grok preflight entirely, so Codex-only tool shapes (custom, namespace, `tool_search`, `additional_tools`) would have gone upstream raw; both halves are fixed together.
- **Dispatch-state reconciliation no longer runs on the request path (PR #544 by @ImogeneOctaviap794).** Reconciliation moves to a shared background pass, and a request that misses re-enters the full selection loop — including the availability wait — once that pass completes, instead of getting a single immediate re-check, so a repaired pool no longer drops the rest of a concurrent burst. Re-entries are capped, a canceled context exits the loop promptly, waiters are tied to the active reconciliation, and health counts are aligned with the reconciled state.
- **Codex tools are bridged across protocols for Grok (PR #543 by @Establishmentarian)** and **account state overlays render correctly in tables (PR #545 by @Establishmentarian)**, the latter fixing an overlay scope problem visible in Safari.
- **Live call records no longer race.** The record aliased by a live session was mutated under the store mutex while several paths read it with no lock at all. Reads now take a snapshot, the controller is promoted to observer before the session is published, and the lease-refresh loop reads under the store mutex.
- **The unpatched `lib/pq` driver is replaced with `pgx` v5.10.0.** `govulncheck` failed on seven `lib/pq` protocol advisories that have no fixed release. The public Postgres driver name is preserved, with identifier quoting, int8 arrays and SQLSTATE classes mapped onto pgx stdlib.
- **CI runs the race detector.** `test-race` is sharded so admin, database, proxy and promptfilter no longer share one two-core runner, frontend tests and job timeouts are added, docs-only workflows are skipped, `govulncheck` is pinned on PR and push, and checkout/setup actions move off Node 20. A database perf gate is relaxed under the race detector.

## v2.8.2 - 2026-08-18

### Features

- **Claude Code on Grok can drop follow-up reasoning effort without touching the first user turn.** A settings switch (`follow_up_effort_enabled`, default off) keeps the client's or default effort for the opening question, then caps tool-result follow-ups and tiny no-tools requests at operator-chosen `low`/`medium`/`high` (defaults medium / low). A client value that is already lower is left alone. grok-4.6 still has `xhigh`; grok-4.5 tops out at `high`.
- **Grok tool tables keep their first-seen order, and Claude system prefixes stay split so Grok can reuse the static cache.** Claude Code often reorders tools or inserts MCP tools mid-session, which punched through Grok's prefix cache. The gateway now remembers the first-seen tool order per conversation — keyed by the static system text plus the first user turn, not the tool list itself — and only appends new names. `cache_control` is stripped from tools forwarded to Grok. System blocks that carry `cache_control` stay as separate developer messages instead of being concatenated, so the static CLAUDE.md prefix can be reused while the dynamic git-dirty tail changes.

### Fixes

- **Claude Code stays on catalog Responses, so grok-4.6 no longer 400s on `tool_result` roles.** A fresh Messages capability probe used to override the catalog `apiBackend`. grok-4.6's Responses path does not accept Anthropic `tool_result` roles; catalog now wins, downstream SSE stays Claude-shaped, and same-protocol non-native routes still force upstream streaming so the canonical SSE pipeline can consume them.
- **`unknown provider for model` is treated as an account/model mismatch and rotated (PR #539 by @dundunge).** Relay 400s of the form `unknown provider for model gpt-5.6-sol` were passed through because the detector only recognized official Codex wording. HTTP and `response.failed` paths now reuse the existing model-unsupported cooldown and account-rotation retry; ordinary 400s stay unretried.
- **Streaming Codex Remote Compact no longer pins to official OAuth accounts (#540, reported by @wxxsfxyzm).** Newer Codex clients send Remote Compact as `POST /responses` + `stream=true` + `compaction_trigger`. The old body-signal path excluded working OpenAI Responses relays whenever any official account was still in the pool, so a relay-only (or official-but-wrong-model) deployment returned an immediate 503. Streaming Remote Compact now uses the ordinary `/responses` scheduler; non-streaming body-signal compact still lifts to the dedicated compact path.

## v2.8.1 - 2026-08-17

### Features

- **ChatGPT Live calls are reachable through `/v1/live`, behind a per-key switch that stays off by default.** API key limits gain `allow_live`; keys without it keep their previous behaviour and never reach the new endpoints. Live call creation, session lookup by `call_id`, sideband handling and session attestation are implemented, with attestation available on darwin and a stub elsewhere.
- **Prompt-filter model review can save reusable provider profiles (PR #535 by @ifThink404).** Operators no longer have to retype a review endpoint's base URL, key and model on every change: profiles are persisted, listed and selectable from the review UI.
- **Local Prompt Filter blocks can show an operator-defined message.** The advanced enforcement settings accept a trimmed, 2,000-character message and use it consistently for direct OpenAI, Anthropic, text/image, signed NewAPI-local, and Responses WebSocket blocks. Empty values preserve the existing default, while upstream policy, conversation-lock, and cooldown messages keep their dedicated diagnostics.
- **Database and Redis pool ceilings rise from 500 to 5,000, and bulk imports can skip the immediate refresh.** The settings validators no longer cap large deployments at 500 connections, and account creation accepts `skip_refresh` so a bulk import does not fire a refresh per imported account.

### Fixes

- **Codex fingerprint convergence no longer forwards the client's original thread identifier, and stops adding a header real clients never send (#536, reported by @Yonch404).** Packet captures of codex-tui 0.147.0 show `X-Client-Request-Id` is not a per-request random id — it equals `thread-id` in every observed handshake, including the one where session and thread genuinely differ. Because the allow-list forwards the header verbatim and convergence never touched it, the downstream client's original thread id reached upstream while the metadata beside it had already been rewritten. It now converges when it reuses the client's own session or thread id and is left alone otherwise, so a genuinely per-request value is not turned into a constant. `X-Codex-Installation-Id` and `X-Codex-Window-Id` become override-only: the real client never sends the installation header at all (`installation_id` lives only inside the turn metadata JSON), so adding it unconditionally produced a header no genuine client carries.
- **Turn metadata no longer forwards each downstream user's workspace identity.** `workspaces` is keyed by an absolute local path containing the OS username and carries git remote URLs plus the latest commit hash, all passed through untouched — so a single shared account exposed a pile of unrelated users' home directories and repositories upstream. Paths become account-scoped derived placeholders, remote URLs are dropped, and the commit hash (a globally unique repository marker that resolves straight back to a public repository and its author) is replaced with a same-length derived value. Entry count, distinctness and non-identifying fields are preserved, and re-keying is idempotent so a retried payload is not re-hashed. Regression tests drive the real outbound header assembly on both the HTTP and WebSocket paths, then scan the finished request for any surviving original identifier.
- **Large account imports no longer spawn a goroutine per probe or ignore runtime load.** Import probes move to a queue-based concurrency model, and database plus probe concurrency adapt to current system metrics instead of running at a fixed ceiling. Usage probes also handle revoked tokens correctly under load.
- **Compaction provenance fails open during cache outages and stays off the hot path for non-compaction traffic.** A runtime-cache read failure while resolving compaction affinity now falls back to normal scheduling instead of returning 503, so conversations keep flowing while Redis is loading or unavailable; conflicting known provenance still returns 400. Request bodies and stream frames are pre-screened with a cheap byte scan for both `"encrypted_content"` and a compaction item type before any full JSON walk, keeping large conversations and reasoning-only stream frames out of the provenance parser.
- **Encrypted compaction state stays with the upstream that created it.** Responses HTTP, compact, and WebSocket paths record a SHA-256 digest plus the producer account and compatibility domain, prefer that producer on reuse, and permit failover only inside the same native or relay domain. Unknown pre-deployment state keeps legacy scheduling; conflicting known sources return 400, while a known source with no compatible account returns an explicit 503. `CODEX_COMPACTION_AFFINITY_TTL` controls the rolling seven-day default without storing opaque encrypted content.

## v2.8.0 - 2026-08-16

### Features

- **Large Codex and Grok account pools keep the accounts page responsive while seven-day usage statistics warm in the background.** Pools above 500 accounts now aggregate request and today counters in 100-account batches with resumable progress and a size-aware timeout instead of repeatedly attempting one full-table scan. Usage-backed sorts are temporarily disabled only during the first cold aggregation and automatically return when the cache is complete; pools of at least 2,000 accounts reuse their lightweight list snapshot for 30 seconds. Deleting or cleaning accounts prunes warm server and browser snapshots in place rather than forcing a blocking full-pool reload. Regression coverage exercises batching, resume-after-timeout/error, sort gating, non-blocking cold starts, cache pruning, and both Codex and Grok frontend sort behavior.
- **Account cleanup reports per-account progress across Codex and Grok pools.** Banned, rate-limited, error, and Grok cleanup actions can stream start/progress/complete events, update deleted/failed totals without stale “already cleaned” messages during the run, and quietly reconcile the visible page when rows disappear. Team/K12/education plan badges also expose the effective workspace ID in a tooltip so operators can identify the workspace a seat actually routes through.

### Fixes

- **Imported OAuth accounts recover from transient refresh failures without being stranded in `error`, while permanently dead credentials reach a bounded terminal state.** A newly imported refresh token now retries timeout, proxy, upstream 5xx, and refresh-lock failures up to three times without occupying an import worker while waiting; invalidated/reused refresh tokens and ended sessions enter the existing unauthorized recovery cadence, then become terminal `error` after three consecutive permanent failures so the recovery probe no longer retries a dead credential forever. A successful refresh or manual cooldown clear resets the counter.
- **Responses probes and connection tests no longer treat an HTTP 200 stream with a failed terminal event as success.** `response.failed`, failed/incomplete `response.completed`, and `error` events are classified explicitly; embedded `usage_limit_reached` errors apply the authoritative account cooldown, other failures remain failures, and a missing terminal event is rejected. Runtime status now keeps hard account errors ahead of usage badges and uses the same fresh-dispatch usage-window rule as the scheduler.
- **Deactivated Team, K12, and education workspaces isolate every native seat and avoid redundant batch-test handshakes.** Workspace linkage now recognizes seats whose native account IDs differ but whose effective workspace and plan identify the same organization, preserves the triggering account for the 60-second deduplication window, and short-circuits sibling tests locally. Header-only workspace overrides on personal accounts remain excluded.
- **SQLite account-group changes, soft deletes, and usage-log flushes share one serialized writer gate (PR #529 by @ifThink404).** These write paths previously opened transactions outside the existing SQLite write semaphore, allowing concurrent mutations to contend until `SQLITE_BUSY` or starve buffered usage logs. Top-level SQLite transactions now enter one lock while PostgreSQL keeps its normal transaction behavior; regression tests cover group mutations, single/batch soft delete, and flush contention.
- **Prompt-risk reconciliation and admin aggregation scale to production-sized datasets (PR #530 by @ifThink404).** Request-ID and fingerprint reconciliation now use separate index-friendly queries backed by targeted partial indexes, avoiding the cross-branch `OR` plan that degraded as `prompt_risk_events` grew. Dashboard aggregation gets a bounded 15-second budget and the 30-day risk-profile list gets 20 seconds instead of failing at five; schema, migration, query-plan, API-timeout, and race tests cover PostgreSQL and SQLite.

## v2.7.9 - 2026-08-16

### Features

- **Grok Imagine gains a full media path: image models plus async video generation, edit and extend (covered by `proxy/grok_media_test.go`).** The gateway now exposes Grok media models (`grok-imagine-image*`, `grok-imagine-video*`) independently of the text-model whitelist, and adds `/v1/videos/generations`, `/v1/videos/edits`, `/v1/videos/extensions` plus status and `/content` download (Range/206, signed-URL rewrite through the gateway). Video jobs are async (`request_id` then poll); paid Grok accounts are preferred over free ones because free plans 403 on media. Codex-channel API keys stay blocked from Grok media. Grok OAuth reauthorization updates an existing account in place instead of creating a duplicate.
- **Grok batch model whitelists survive catalog sync.** Operators can pin grok-4.5/4.6 (or any other set) on selected accounts; a later catalog refresh keeps the declared whitelist instead of replacing it with whatever the upstream catalog currently shows.
- **The accounts page becomes a usage console: today's per-model counts, success/error pills, and live quota-sampling progress.** Page stats now include today's requests/successes by model and a 7-day success-model breakdown; the 7-day request column becomes success/error capsules with a status-code tooltip; disabled Grok accounts get the same frost overlay as Codex; quota analysis excludes `error` accounts so dead seats no longer flatten the distribution; official-settlement auto-refresh skips accounts younger than 24 hours (upstream almost never has numbers on import day); and the quota chart surfaces in-flight sampling progress instead of looking idle.
- **GitHub access, account filters and WebSocket slot count become operator-tunable (#522, requested by @huyachigege).** Shared-IP GitHub anonymous quotas (60 req/h) were breaking CLI-version sync, pricing sync and online update. Settings now take a PAT (sent only to `api.github.com`, 5000 req/h) and a GitHub-only outbound proxy, decoupled from the global proxy so API calls can use a dirty IP + token while release downloads stay off a bandwidth-capped clean node. The accounts table filters OAuth vs Responses API-key accounts, and `codex_ws_stateless_slots` (1–32, default 8) hot-reloads the per-(account, cache key) stateless slot count. Wide-screen account and usage layouts stop forcing horizontal scroll.
- **Codex turn-state stays on the account that minted it, and session-level beta features default to `remote_compaction_v2`.** `X-Codex-Turn-State` is an opaque blob bound to the minting account's outbound identity; after failover the client can still echo the old blob, which official Codex never does. The gateway records provenance per affinity key and strips known cross-account echoes on the next attempt (HTTP and WebSocket), without injecting a blob of its own. Session-level `X-Codex-Beta-Features` now defaults to `remote_compaction_v2` when the client omitted it, matching official Codex handshakes; an explicit client value is left alone.
- **A deactivated ChatGPT workspace isolates every native seat in that workspace.** Team deactivation used to mark only the account that hit `deactivated_workspace`, so the scheduler kept dialing siblings and burning handshake budget. The trigger account is marked error and other Codex accounts whose native `AccountID` matches the effective workspace are isolated in the same pass (Grok / OpenAI Responses accounts are left alone; header-only workspace overrides do not take the whole account down). Deduped for 60 seconds. Handshake 401/402 are promoted from transport errors to real HTTP responses so usage logs, unauthorized cooldown and this workspace mark actually fire — a 402 that stayed a dial error left the account "available" forever.
- **Codex accounts pause themselves when upstream overload crosses a window threshold.** `server_is_overloaded` is capacity shedding, not account standing, but a sticky overloaded account still wasted the pool. A sliding-window breaker (default 20% of ≥5 requests in 5 minutes) now cools the account as `overload_paused` for 30 minutes with a resume overlay; recovering no longer wipes the usage snapshot, so the quota bar stays visible. Codex channel only; off by a settings switch.
- **Bulk account import accepts large JSON dumps without loading the whole body at once.** The import endpoint is raised to 200 MB (`CODEX_MAX_IMPORT_BODY_SIZE_MB`), files over the limit are rejected individually so a batch can still proceed, and JSON is parsed as a stream. The admin UI shows the size cap and reports oversized files.
- **Prompt-filter model review can fetch the review-model list from the operator-supplied API key and base URL.** The review UI no longer depends on a static catalog when the configured review endpoint exposes its own models.

### Fixes

- **Responses dispatch reconciles a stale runtime pool and pins active turns instead of returning a misleading 503 (PR #526 by @Torrekie).** Adding or enabling a healthy endpoint while the in-memory scheduler had no candidate left requests looping on "无可用账号". A scheduling miss now reconciles active accounts, enablement, routing metadata and Responses endpoint identity; credential changes clear only stale identity-derived cooldowns. `ignore_usage_limit_status` is narrowed to a bound Codex turn that carries turn-state evidence — fresh turns at WHAM 100% return 429, and an actual `/responses` `usage_limit_reached` is persisted as `responses_rate_limited` and never rerouted.
- **Official-client-gated Responses model discovery no longer goes out as the Go default User-Agent (PR #528 by @Torrekie).** `GET /v1/models` for an OpenAI Responses account only set `Authorization` and `Accept`, so official-client-gated endpoints answered `unauthorized client detected` before they even looked at the token. Discovery now sends the Codex User-Agent, `Version`, `Originator` and a stable `X-Codex-Installation-Id`, and reapplies saved custom headers last so explicit overrides still win.
- **Usage-log error summaries keep the upstream diagnostic instead of a generic gateway phrase (#524, reported by @zzzhouuu).** Stream failures such as HTTP/2 `INTERNAL_ERROR` were visible in process logs but the admin usage table showed an empty or generic summary. Failure messages now prefer the upstream body/code path and fall back to the gateway-generated text, so the usage page matches what operators already see in Docker logs.
- **Go 1.26.6 and `golang.org/x/image` v0.45.0 close the stdlib and VP8L advisories that failed `govulncheck`.** The Docker builder image tracks 1.26.6; the image bump covers the `image.DecodeConfig` path used by upscale.

## v2.7.8 - 2026-08-14

### Features

- **Custom image sizes land on the exact requested canvas, including sizes upstream rejects as not a multiple of 16 (PR #519 by @ifThink404).** A requested `1920x1080` used to miss the promised pixels: GPT Image 2 requires both sides to be multiples of 16, so the job either failed or came back at a nearby size. Strict sizing now treats the requested canvas as authoritative — the upstream request is rounded up to a legal size (for example `1920x1088`) and the result is locally resized back to the exact canvas, padding by default or covering when opted in. `strict_size=false` restores the previous upscale-tier behaviour. Cache keys include fit/strict so mixed sizes no longer collide. Verified live: `2048x1152` returned `2048x1152`; `1920x1080` generated upstream at `1920x1088` and cropped locally to `1920x1080`.
- **Official settlement pills stop spinning when upstream has nothing to show.** Accounts whose official 7-day snapshot synced successfully but came back empty — official stats lag, or the account has no official-client usage — used to look like they were still loading, and the page kept retrying the backfill. The API now reports `official_usage_synced`; the capsule shows a static "no data yet" placeholder and the page stops polling. Failed backfills cool down for 15 minutes instead of retrying every two, leaving recovery to the hourly probe.
- **The admin console layout holds together on smaller viewports.** Usage, accounts, operations and the shared chrome (pagination, segmented tabs, stat cards, time-range selector, layout shell) get denser grids, wrapping filters, overflow-safe dropdowns and mobile cards that honour the column visibility settings instead of overflowing or leaving empty chrome.

### Fixes

- **WebSocket fallback connections no longer crowd out the reusable slot pool (#520, reported by @huyachigege).** When all eight stateless slots were busy, the overflow connection was pooled under a per-request unique key that no later request can ever hit. Pure LRU then kept these just-used zombies while evicting older but reusable slot connections, so reuse dropped and later requests paid a 10–20s handshake. Capacity eviction now drops one-shot connections first; on clean stream completion an unbound one-shot is discarded instead of released (bindings with a live `previous_response_id` stay pooled so chain affinity still works); `CODEX_WS_STATELESS_ONESHOT` now actually destroys the connection on release.

## v2.7.7 - 2026-08-13

### Features

- **Grok accounts gain a full control plane: plan and quota tracking, model-catalog and protocol-capability probing, and multi-protocol adaptation (PR #513 by @Futureppo, probe isolation in PR #518 by @dundunge).** The account pool now probes each Grok account's plan, quota, model catalog and per-protocol capabilities and persists the observations; Responses, Chat Completions and Messages conversion, streaming passthrough and error handling were completed; and OAuth refresh gained concurrency isolation with clearer admin status display. Post-merge hardening closed the launch defects: default model ID lists are unified into a single source (a diverged copy rejected models the catalog had registered), routine access/refresh-token rotation carries generation-scoped catalog and capability observations forward instead of forcing a full real-inference reprobe of every model, catalog-selected same-protocol routes without native verification force upstream streaming so the canonical SSE pipeline can consume them (every non-streaming request on such routes previously failed deterministically and penalized the account), streaming usage is read from the protocol-correct paths so native passthrough and chat conversion stop logging zero tokens, and capability probes are isolated from user traffic — probe rows are excluded from account statistics and health buckets so a probe error can no longer drop an account's health tier.
- **Local prompt-guard blocks now kill risky prompts before they reach upstream, with strike accrual and an operator-controlled authorization switch (PR #515 by @ifThink404).** A local block previously let the attacker simply rephrase: the next variant went straight upstream and produced a real cyber-policy event at the provider. A local block now locks the conversation immediately across all entry points — OpenAI, text, Anthropic, and the Responses WebSocket path, which held a separate copy of the logic that checked existing locks but never created them — so one hit seals the session against unknown paraphrases. Deployments without NewAPI can finally lock at all: identity falls back to the downstream API key plus Codex session identifiers, with a dual-driver rolling migration adding `identity_kind` to the lock table. Highest-confidence local severe violations can accrue NewAPI CYB strikes (`LocalSevereStrikeEnabled`, default on) so pre-upstream killing no longer exempts malicious users from automatic banning — conversation-lock re-blocks are explicitly excluded so retries cannot stack strikes. Self-declared authorization ("I have written permission") no longer disarms the targeted-intrusion terminal rules by default; a new operator switch (`AuthorizedPentestAllowed`, default off) decides whether such unverifiable claims are honored. English and Chinese preposition-style bare targets ("against 1.2.3.4") are now recognized, with new false-positive guards proving defensive and ops phrasing stays unblocked.
- **Official OpenAI and xAI model pricing syncs from the official catalogs (PR #512 by @ifThink404).** The model-pricing page keeps official prices in sync, the settings-page Grok whitelist batch import that wrote a legacy credentials path was removed, and corrupt pricing JSON now fails closed so a poller cannot wipe existing overrides. Reasoning-effort mapping becomes model-aware, allowing `xhigh` for grok-4.6 and later while keeping older models unchanged.
- **The accounts table shows today's usage, and official settlement snapshots backfill on demand.** A daily aggregation reports requests, tokens and billing since server midnight alongside historical stats, and accounts missing official usage snapshots are backfilled asynchronously when the account page requests stats, so settlement history stops having gaps for accounts imported late.
- **`/v1/responses/compact` works again via body-signal compaction.** Upstream removed the dedicated `/responses/compact` endpoint (it now returns 404). A new system setting `compact_via_responses_enabled` (default off) rewrites compact requests on official Codex OAuth accounts as `/responses` calls carrying a `compaction_trigger` input item and aggregates the SSE stream back into the single-JSON compact response; relay accounts keep calling their own `/responses/compact`.
- **Newly imported accounts can default to a configured Codex fingerprint mode.** A system setting applies the chosen `off`/`device`/`session`/`full` policy to accounts as they are imported instead of requiring a per-account edit afterwards.
- **Deleting or editing a proxy immediately unbinds the accounts that reference it.** Deleting a proxy previously left accounts pointing at a dead proxy URL; deletion and URL changes now update account bindings and runtime state at once so no traffic is routed through a non-existent proxy.

### Fixes

- **Grok refresh-token rotation survives caller cancellation and concurrent cooldowns.** The token exchange and credential CAS ran on the admin caller's cancellable context, so a request timeout mid-exchange could abandon an already-rotated RT and permanently brick the account; the refresh lease now switches to a detached critical-section context once RT consumption starts. Cooldown state was also published from a snapshot taken at entry, silently clearing a rate-limit cooldown set during the exchange and sending a 429'd account straight back into dispatch — the live account state now decides at publish time.
- **The `usage_logs` generation index builds online instead of inside the startup migration.** The non-concurrent `CREATE INDEX` on the largest production table ran inside the single 10-second migration transaction: large deployments blocked log writes on the share lock, timed out, rolled back and crash-looped at startup. The index now builds in a background task with `CREATE INDEX CONCURRENTLY` under a 60-minute budget, cleaning up invalid leftovers first; failure only degrades query performance and retries on the next startup.
- **Codex custom and dynamic tools work on Grok routes (#516).** The protocol bridge now carries Codex's custom-tool and dynamic-tool declarations, including injection of searched tools.
- **The user-level CY cooldown duration is configurable (PR #509 by @ifThink404).** The 30-minute cooldown applied after a genuine upstream cyber-policy event was hardcoded; operators can now tune it from the prompt-filter settings.
- **The account quick-configuration sheet can no longer wipe workspace routing.** The sheet filled from the list row and kept Save enabled, so a slow or failed detail fetch submitted `custom_headers` as null. It now loads details first, blocks save until they arrive, and surfaces fetch errors with a retry.
- **JSON-schema sanitization aligns `required` with `properties` and normalizes additional tools more robustly.** Extraneous required keys are removed and missing ones added in sorted order; additional-tool normalization fills missing types and flattens nested functions, with recursive checks for unsupported keys under draft-07 and draft 2020-12.
- **The accounts page live-state poll stops re-rendering unchanged rows.** The 1-second poll replaced every account row object on each tick, defeating row memoization and recomputing every accounts-page computation at 1 Hz; merging now preserves object identity for unchanged rows and returns the original array when nothing changed.

## v2.7.6 - 2026-08-12

### Features

- **Shared Codex OAuth accounts can converge downstream device fingerprints at an explicit per-account level.** A new `off` / `device` / `session` / `full` policy deterministically rewrites only identifiers already present in `X-Codex-Turn-Metadata` and `client_metadata`, keeping values stable across restarts without storing new state. The outbound `Session_id` header and prompt-cache isolation remain independent, relay/Grok accounts stay off, and existing accounts default to unchanged passthrough behavior. The policy is available in single- and batch-account editing, while a new quick-configuration sheet brings fingerprinting, dispatch score, concurrency, scheduler priority, warm-tier handling, proxy, custom headers, tags and groups into one focused workflow.
- **Official settlement usage refreshes on a status-aware cadence instead of a six-hour full sweep.** The scheduler now checks due accounts every 30 minutes, refreshes healthy OAuth accounts approximately hourly, and keeps rate-limited or exhausted accounts on a six-hour cadence because their usage cannot move in the short term. Attempts are bounded to four concurrent probes, deleted accounts are removed from the in-memory schedule, and a manual refresh immediately resynchronizes the account page's stats.
- **The administration console gets a broader interaction and visual polish pass.** Dashboard summaries and charts, settings, account configuration, API keys, usage, operations, proxies and model pricing now use clearer grouping, denser responsive layouts, stronger state feedback and more consistent controls. English and Chinese copy was expanded for the new auto-refresh, pricing and proxy-management actions.

### Fixes

- **Account request statistics converge promptly and no longer scan the other upstream channel.** Request-count aggregation and caches are split by Codex/Grok channel, a completed refresh expires the corresponding list snapshot, and account pages use bounded catch-up polling while statistics are warming or stale. This clears the persistent “statistics refreshing” state introduced when silent-refresh backoff was added, keeps manual official-usage refreshes in sync, and replaces Grok's raw `warming` / `stale` enum with localized labels.

## v2.7.5 - 2026-08-12

### Features

- **Each account's usage modal now shows what upstream actually billed, not just what the gateway estimated.** The official wham analytics endpoint is probed per account and its settlement data lands in a daily-usage breakdown: credit-billed clients (1 $ = 25 credits) and token-billed clients are split out per client, with raw credit counts shown alongside the converted cost. Upstream only retains seven days of analytics, so the gateway persists each day's snapshot to the local database and the history keeps accumulating past that horizon. The accounts table splits the billed column into a gateway-computed pill and an official-settlement pill (clicking the official pill jumps straight to its breakdown in the modal), the usage source is attributed explicitly so the two numbers can't be confused, and the modal honors the global full-number display setting.
- **Verified users get a bounded safety cooldown after a genuine upstream CY event, closing the session-churn loophole (PR #502 by @ifThink404).** The exact-conversation lock keyed on the session fingerprint, so a platform user who triggered upstream cyber-policy enforcement could immediately re-send the same content by rotating to a fresh session. A 30-minute user-level cooldown now rides alongside the (longer) exact-conversation lock: during the window, new requests from the same verified platform identity are rejected locally with a distinct non-strike `user_cyber_cooldown` decision — propagated through HTTP, Anthropic-shaped errors, NewAPI policy metadata and the Responses WebSocket — so blocked retries neither reach upstream nor stack additional penalties. Both restrictions resolve in a single database lookup backed by a new composite index. The local rule set also gains a strict pattern for role-framed direct intrusion requests, and the external model review carries an immutable boundary so role-play framing cannot establish authorization.
- **CY restrictions became legible and operable: blocked requests explain themselves, and admins can release a user's cooldown in one click (PR #505 by @ifThink404).** Rejections used to be a generic block; they now carry the error code, restriction scope, remaining time, `Retry-After`/expiry headers and the manual-unlock location across all four response paths (HTTP, signed NewAPI, Anthropic envelope, Responses WebSocket). Risk profiles for platform users surface the active cooldown with its expiry and remaining duration, and a new `scope=user_cooldown` unlock releases every active CY lock for a verified user at once — the existing exact-conversation unlock is unchanged and remains the default. The change also fixes a coverage gap: verified users whose requests carry no session fingerprint previously escaped the cooldown entirely because the lock input demanded a 32-character fingerprint; sessionless identities now persist a deterministic per-user lock, with a 64-way concurrency regression proving repeated retries add no duplicate incidents.

### Fixes

- **Account group memberships survive single-account hot reloads (PR #504 by @ImogeneOctaviap794).** The full startup path loads group memberships after all accounts are built, but the single-account reload path rebuilt the account from its credential row alone. When an imported refresh token was refreshed asynchronously and merged into an existing OAuth account, the surviving runtime account came back with an empty group list — the admin API reported no groups and the scheduler treated the account as ungrouped until the next restart or a manual group edit. Memberships are now fetched before the rebuilt account is added, with a regression test covering the refresh/merge path.
- **The account management page stops re-rendering the world on every interaction.** A profiling pass found four compounding costs: hidden views kept their data queries and polling machinery live (the Grok view ran a second full copy even when Codex was displayed), every row re-rendered on any table state change because the monolithic component memoized nothing, silent background reloads re-fired at a fixed cadence even when consecutive refreshes changed nothing, and each search keystroke re-rendered the full table. Queries are now gated on the active view, rows are memoized on their own data, background reloads back off when nothing changes, and search input state is isolated from the table until it actually filters.

## v2.7.4 - 2026-08-11

### Features

- **A `fill_first` scheduler mode drains one account at a time instead of spreading load (#501, requested by @sunjinqu).** Within the same scheduler priority, accounts sort by 7-day usage descending — the account with the least remaining quota stays at the head of the queue and absorbs all traffic until it is exhausted, rate limited or cooled down, then the next one takes over (A → drained → B → drained → C). A recovered account rejoins ordered by its fresh usage, so a quota reset sends it to the back of the line instead of interrupting the account currently being drained; accounts without usage data fall back to a deterministic ID order. Verified live against a real pool: consecutive requests stick to one account under `fill_first` and rotate again the moment the mode switches back. Scheduler modes only apply while the fast scheduler is enabled (an existing constraint shared with `remaining_quota`), and the settings UI now says so — the mode select dims with an amber hint while the fast-scheduler gate is off, instead of silently doing nothing.
- **The Codex egress identity gets two fidelity upgrades: an upstream routing hint and self-consistent User-Agent versions.** Official Codex clients declare routing intent via `x-codex-routing-hint`; the gateway now synthesizes the header for official Codex accounts (inbound spellings are stripped first, relay/Grok accounts stay strip-only), deriving the value from the final outbound body after model mapping and tier sanitisation. Separately, a copied `raw_user_agent` override used to pin the version segments forever and silently bypass CLI version sync — official-shaped UA strings now rebuild their version segments from the effective synced version while keeping the rest of the fingerprint, and the wham probes plus models-manifest fetch stop hardcoding the built-in UA constant that contradicted the synced version they sent elsewhere.
- **The admin console gets a full design-system pass: one tile language, theme-token colors, and two pages rebuilt.** A source-level audit of all 22 pages against the house style landed as a series of focused changes. Dark mode stops breaking: the version popover's light-only amber boxes, the API reference's hardcoded zinc code blocks (now the shared syntax-highlighted CodeBlock), PromptFilter's solid `text-white` risk badges, unpaired light-mode counters and inline rgba badges are all on theme tokens now. Stat tiles consolidate from six per-page forks into shared `StatTile`/`CompactStat` components with tabular numerals — refreshing values stop shifting width — and segmented controls collapse into one sliding-thumb `SegmentedTabs` primitive with the focus rings the ad-hoc copies lacked. GrokAccounts (83 raw palette hits) and PromptFilter (~100) route state colors through the semantic tokens; the Proxies page — the last one bypassing the shared scaffold — adopts PageHeader/Button/Table/Switch primitives, stable toolbars and content-shaped skeletons; the model-pricing page loses its permanently-dark marketing hero for the house tile grid while keeping click-to-filter behavior. Page content gains a shared max-width on wide monitors, numeric log columns right-align, the scheduling-strategy settings card regroups by concern (connectivity-test settings move to their own card), and non-interactive metric cards stop lifting on hover and stop badging "Normal" on every tile.
- **Theme switching is smooth on heavy pages.** The circular theme wipe animates over the live new document, and flipping `.dark` used to start thousands of 200ms per-element color transitions that repainted the layer on every frame of the sweep — a visible stutter as the circle crossed chart-dense regions. Element transitions are now frozen for the duration of the view transition (the WAAPI clip-path animation is unaffected), turning the wipe into plain compositing of two static layers.

### Fixes

- **Conversation continuations survive losing their account instead of failing the turn (PR #500 by @bunizao, hardened in follow-up).** `previous_response_id` turns now stay pinned to the OAuth account that created the response, even after bounded affinity reaches its escape thresholds, and the binding survives retryable failures. When the bound account genuinely cannot serve — rate limited, cooled down, excluded — or the upstream no longer knows the response id, a one-shot degrade path rebuilds the request without the continuation reference and fails over, instead of waiting 30s and surfacing a raw 400.
- **Upstream capacity shedding no longer walks the whole pool's health down.** `server_is_overloaded`/`slow_down` fell through to a generic 500, so every shed event bumped the failure streak and dropped the account a health tier — but shedding is bucketed by client identity and model capacity, not account standing, so during a busy period the streaks benched healthy accounts for a signal that says nothing about them. These events are now classified explicitly and skipped in health reporting, while the silent failover path still rotates away.
- **Images returned by tools reach the vision model.** `function_call_output` carries text only, so tool-returned images were dropped or stringified into the prompt as raw base64 — screenshot-style tools (routine under Claude Code) never actually showed the model anything. Images are lifted out of the tool output into a synthesized user message with `input_image` parts and an attribution line; text-only tool outputs keep their original bytes so the prompt-cache prefix is preserved.
- **Two retry-path payload repairs stop poisoned histories from failing every subsequent turn.** A stale `encrypted_content` rejection now drops compaction items whole instead of leaving a bare `{"type":"compaction"}` shell that upstream rejects again as missing a required parameter after the single retry is spent. And explicitly-null `parameters.type` schemas (Codex Desktop sends one on `automation_update`) are repaired beyond top-level function tools — non-function tools and Responses Lite's `additional_tools` carrier are now walked by the same sanitiser, closing a blast radius that grew with the conversation.
- **The Responses Lite signal is gated by what the effective model actually supports.** Upstream accepts the lite signal only for models whose manifest declares it; the gateway's model mapping could rewrite a supported model into an unsupported one while keeping the signal, producing a guaranteed 400. A slug→capability table (seeded, then continuously learned from manifest passthrough/sync) strips the signal for known-unsupported effective models and passes it through otherwise.
- **Model cooldown backoff escalates on sparse traffic too.** Backoff only stepped up when a failure landed while the previous cooldown was still running; on low-traffic deployments the gap between failures routinely exceeded the cooldown, so the level never advanced. The streak is now judged by the time since the last failure (within an hour keeps escalating), while success or a lapsed TTL returns to the first step.
- **Redeeming a reset credit can no longer be double-spent by a closed tab.** The non-refundable consumption call hung off the client's request context, so a user closing the tab mid-flight aborted a credit that may have been spent, reported failure, and invited a retry that spent a second one. Consumption now detaches from client cancellation and answers only to its own timeout; the read-only query path is unchanged.
- **Calendar-day cost/token limits reset at the configured timezone's midnight (#498, reported by @rgzn-aiyun).** `time.Local` was lazily locked by the startup log line before `.env` loaded, so a TZ that only lived in `.env` never took effect and daily limits reset at host-default midnight. The timezone is now applied explicitly after `.env` loads, IANA tzdata is embedded for Windows/minimal images, and the startup banner prints the effective zone. Process-level TZ still wins over `.env`.
- **Account cost columns no longer degrade to "-" after silent list refreshes (#499, reported by @OneAHPro).** Page stats were merged into the account rows in place, so any silent reload replaced the merged rows with cost-less base rows and the stats effect never refetched for unchanged pages. Stats now live in dedicated state merged at render time, so base-row refreshes cannot wipe them.
- **Sorting the account list by scheduler priority works (#493 follow-up).** The paged list sorts against a lightweight projection that never selected `scheduler_priority`, so every row compared as 0 and the sort degraded to the ID tie-break while badges showed real values. The field is projected on both PostgreSQL and SQLite, with an end-to-end regression test.
- **The accounts page no longer renders the raw stats-state enum ("warming"/"stale") to users.**

## v2.7.3 - 2026-08-10

### Features

- **Prompt-policy model review survives key outages: per-credential quarantine, single-probe recovery, bounded conversation-lock expiry, and a read-only audit-chain health panel (PR #497 by @ifThink404).** A dead review key (401/402/403) used to stay in the rotation and burn a round trip on every request; unhealthy keys are now quarantined by credential fingerprint — authorization/billing failures cool down for 15 minutes, rate limits and transient/network failures for the short circuit-breaker window — and when every key is cooling the request fails fast with a retry-after instead of hammering the upstream. A key whose cooldown expired re-enters through exactly one recovery probe per call, so a recovering provider is not stampeded; a probe answered with HTTP 2xx but an unparseable body clears the key's cooldown outright, since transport success proves the credential is healthy even when the verdict is unusable. Conversation CY locks gain a bounded automatic expiry (`conversation_lock_ttl_hours`, default 168, clamped 1-720) with lazy race-safe unlocking and cache TTLs capped to the lock's remaining life, so a mistaken lock self-heals instead of blocking a session forever. A new read-only `GET /api/admin/prompt-policy/incidents/health` endpoint and admin panel surface storage, audit-queue, review-pool, fallback-policy and conversation-lock state without exposing secrets — and only genuine faults (dropped/failed audit writes, review enabled with an empty or fully cooling pool) report degraded, not intentionally disabled features. Prompt logs now label the final decision source (model review / local rules / combined / conversation lock), so a local score of 0 is no longer misread as model enforcement.
- **Upstream CY evidence survives every write order, low-risk sessions skip redundant model reviews, and the risk-profile API scales to hundreds of thousands of events (PR #492 by @ifThink404).** Upstream cyber-policy failures now persist a durable, redacted evidence bundle with a stable quarantine candidate even when the original prompt cannot be recovered, and asynchronous shadow evidence reconciles against incidents regardless of which side lands first; AI learning refuses to fabricate rules from insufficient evidence. Adaptive review coalesces concurrent low-risk reviews per subject behind a short lease so a burst performs one remote call instead of many — local high-risk rules stay authoritative, and a review-provider failure is never blamed on the user. The risk-profile listing stops materializing the full 30-day event working set in PostgreSQL, restoring the production API from timeout/500 to 200 on real datasets. Post-merge hardening closes three confirmed defects: JSON HTML-escaping could inflate evidence up to 6x past the 64KiB validation budget and silently drop the whole incident from the audit trail — an attacker could suppress their own abuse evidence with escape-heavy prompts; metadata now shrinks progressively by encoded size so persistence never fails on size. The legacy-preview fallback no longer upgrades quarantined insufficient-evidence rows into learnable material, and the guarded-auto identity gate deduplicates replayed prompts by fingerprint. The review lease releases on holder failure with a 10-minute sweep, and the admin review-key draft editor no longer deletes the wrong key after consecutive removals.
- **Review API keys are managed individually with clearer moderation risk labels and reconciled usage totals (PR #488 by @ifThink404).** Review keys become individually addable, testable and removable instead of one opaque blob — failed keys can be synced out of the pool in one click — moderation risk labels distinguish what was actually flagged, and API-key group usage totals reconcile correctly. Follow-up hardening: full-settings saves from a stale snapshot can no longer resurrect a deleted review key; the delete CAS compares against the raw stored value so a padded legacy key no longer 409s forever; keys are masked on rune boundaries with prefix/suffix only revealed for 12+ character keys (short keys were reconstructible from the overlapping fragments); the DELETE route is audit-logged and rate-limit-sensitive; the api-key-accounts cache namespace bumps to v2 so pre-deploy Redis entries are not served as zeros; zh-TW strings are backfilled.

### Fixes

- **Mid-stream upstream breaks are attributed at the transport layer, and an isolated break no longer penalizes a healthy account (#491, reported by @zzzhouuu).** Internal 598s turned out to be upstream peer `RST_STREAM (INTERNAL_ERROR)` and Envoy-shaped 503s — failures the gateway only observes — but three gateway-side gaps made them harder to diagnose and more damaging than necessary. Break messages now carry downstream write-blocked time, upstream frame count and the gap since the last frame, separating "slow client stalled the HTTP/2 flow-control window" from "upstream reset on its own". Pooled HTTP/2 clients rotate at a max age (default 30 min, `CODEX_HTTP_CLIENT_MAX_AGE`) so a continuously busy account stops carrying hundreds of streams on one aging connection; in-flight requests finish on the old connection. The hardcoded 15s HTTP/2 keepalive becomes configurable (`CODEX_HTTP2_READ_IDLE_TIMEOUT`/`CODEX_HTTP2_PING_TIMEOUT`) for high-latency egress where one late PING tears down every multiplexed stream. And a single transport break no longer halves an account's concurrency — the demotion now requires 3 consecutive transport failures, reset by any success, so background upstream noise stops benching healthy accounts.
- **Upstream capacity-shed errors before first output fail over to another account instead of killing the Codex CLI session.** The real shed sequence is `event: error` followed by `event: response.failed`, but the error frame was not in the pre-first-byte buffering whitelist — it was written out as the first client byte, which permanently disqualified the silent account-switch path, and the CLI treats `server_is_overloaded`/`slow_down` as fatal ("Selected model is at capacity"). Retriable error frames are now buffered like lifecycle frames, restoring the existing failover chain across the Responses relay, `/v1/responses` and WebSocket paths; when a mid-stream error must be passed through, `server_is_overloaded`/`slow_down` are rewritten to `server_error` so the CLI's built-in backoff retries instead of terminating — the message text is preserved and all other codes are untouched. Cooldowns, billing and logs are based on the original pre-rewrite event.
- **The usage page labels the actual service tier as upstream-reported, matching confirmed upstream semantics (#494, reported by @jieChris).** On the ChatGPT OAuth path, priority processing is applied by upstream server-side routing and `response.service_tier` is not an end-to-end verifiable field — upstream reports `default` even when the speed-up is real (openai/codex#14204; interleaved A/B testing in #494 measured ~1.5x throughput with `actual=default`). The "Actual Tier" column is relabeled "Upstream-reported Tier", post-split rows stop falling back to the intent-preferring legacy column when upstream reported nothing, and the requested/actual/billing triple is documented. Stored values are unchanged.
- **Append payload rules are idempotent when the rule engine re-enters.** `ApplyPayloadRulesToBody` appended the configured text on every invocation, so a caller re-feeding an already-rewritten body would stack the append block repeatedly, growing the payload and shifting prompt semantics. Appending is now skipped when the text already exists as a complete `\n\n`-delimited block, including the multi-rule middle-segment case.
- **Account sorting and cost visibility return to small pools, and re-authorization clears a stale error status (#493, reported by @sunjinqu).** The v2.7.2 paged rework auto-switched pools under 10 accounts to the personal card layout, silently removing the sortable table — the switch is now announced with a toast explaining how to return to pool mode, and a compact sort control (requests/usage/import time) covers personal mode, grid view and mobile cards. Re-authorizing an existing OAuth account now clears its error/401 state immediately, mirroring the re-import merge path, instead of leaving it marked abnormal until an async probe happens to succeed; rate-limit cooldowns are deliberately preserved. Request-count refresh failures are logged instead of leaving stats warming forever.
- **Dashboard tiles reflect account mutations immediately.** The paged accounts response served summary tiles and filter counts from a 5-second snapshot cache that was never invalidated on mutations, so the reload right after deleting or banning accounts always showed pre-mutation counts. Mutations now invalidate the snapshot.
- **Frontend dependencies nanoid (3.3.18) and react-router (7.18.2) are bumped for GHSA-2v37-7h3g-55p8 and GHSA-qwww-vcr4-c8h2.**

## v2.7.2 - 2026-08-07

### Features

- **Fresh affinity keys can hash-spread across accounts instead of piling onto the top-scored one (#484, reported by @huyachigege).** Affinity was schedule-then-bind: a new `X-AxisRelay-Affinity-Key` with no existing binding asked the scheduler for its top pick, and on a mostly idle pool the tiny floating-point differences in dispatch score made every new key independently choose the same first-place account — ten downstream identities, one account taking 90% of the traffic, with the bounded-mode escape (50 requests / 5 minutes) re-picking that same account on every cycle. In-flight load only tie-breaks exactly equal scores, so it never spread anything. A new `session_affinity_spread` setting (default off, hot-reloadable) switches fresh-key selection to rendezvous hashing within the top scheduler-priority and health tier: the same key always maps to the same account (true idempotent binding), different keys spread evenly, an unavailable pick falls through deterministically in hash order, and multi-replica deployments converge on the same mapping with no coordination. Binding reuse, bounded escape and Grok-specific affinity modes are untouched.
- **A ChatGPT login shared across workspaces can keep one account route per workspace (PR #485 by @ImogeneOctaviap794).** One OAuth login can sit in a personal space and several Team spaces — same AT/RT, with `Chatgpt-Account-Id` deciding which space serves the request and each space metering its own quota. Deduplication previously looked only at the token's `workspace_id`, so adding the same credential twice with different target workspaces merged the second route away. Dedupe now keys on the effective workspace (`Chatgpt-Account-Id` override, else token workspace) across AT/RT/ST adds, OAuth callbacks and file imports; the add-account flow accepts a target workspace ID directly; the account list shows token, override and effective workspace; editing a workspace header checks for route conflicts; and multiple routes sharing one RT share a refresh lock — a token rotation updates every route's credentials while leaving each route's headers, groups, proxy, usage and cooldowns alone.
- **The account management page paginates server-side and survives pools of tens of thousands (PR #486 by @Futureppo).** The page previously fetched every account and did filtering, sorting and per-account stat enrichment in the browser, so load time grew linearly with pool size. `GET /api/admin/accounts` gains a `view=page` mode with channel/search/status/plan/auth-type/tag/email-domain/group filters and stable sorting; light snapshot and request-stat caches (5s/10s, stale-while-revalidate with mutex refresh) keep summaries warm without blocking first paint; only the current page's accounts are fully hydrated; PostgreSQL health stats aggregate in-database and the list projection extracts public fields from JSONB once, with supporting indexes. Measured on a real 44,768-account PostgreSQL deployment: a 20-row page response is ~46 KB with no credential fields, ~7.3 ms hot, and cold load drops from ~8.9 s to ~2.0 s. The legacy full-list API is unchanged for existing callers.
- **Grok accounts get the same group workflow as Codex, and groups are now channel-isolated (#487, asked by @yyhhjjj).** A Grok account imported without a group had no way to join one afterwards — the group chips are now clickable on both the table and card views (accounts without groups show a dashed "set groups" affordance), opening the same quick-group dialog as the Codex page, with a manage-groups button that deep-links to the group manager (`/accounts?groupManager=1`). Alongside the entries, groups gain a `channel` (codex/grok): Codex and Grok accounts can no longer join the same group — every membership write path (single edit, batch edit, import binding) validates the account platform against the group channel with a named error, a group with members refuses channel changes, and each page's selectors only offer its own channel. A one-time migration classifies existing all-Grok groups as grok channel; mixed groups keep their members untouched and only new writes are constrained.

### Fixes

- **Mid-stream upstream failures are signaled as errors instead of masquerading as successful responses (#473).** Internal 598 stream breaks had two fake-200 shapes: a break after the first byte left the client with a truncated 200 stream and no terminal event, and a break before any output returned an empty 200 body. The first now synthesizes a standardized `response.failed`/error terminal event into the stream so clients can distinguish "finished" from "died mid-flight"; the second returns a real 502. Mid-content transparent resumption stays deliberately rejected — splicing two upstream attempts into one response risks silent corruption.

## v2.7.1 - 2026-08-06

### Features

- **Account groups can carry their own proxy list, with sticky per-account selection (#479, requested by @ZhuAndiliba).** Free-tier pools add and remove accounts constantly, so per-account `proxy_url` maintenance doesn't scale — an account pulled into a group now automatically egresses through the group's proxies on its next request, no per-account setup. The resolution order becomes account proxy > group proxy > sticky proxy pool > global proxy > direct, so an explicitly configured account proxy still wins. A group can hold multiple proxies (one per line in the group editor, http/https/socks5): accounts stick to one entry by account ID so the account↔exit-IP mapping stays stable instead of drifting per request, which is exactly what ban-wary free pools need. Changes apply hot — resolution happens per request, and session-affinity bindings detect a changed group proxy and rebind on next reuse. With multiple group memberships, the first group that has proxies wins.
- **Streaming CYB strikes actually reach NewAPI, gated by an opt-in counting toggle (PR #480 by @ekti123456).** The first upstream CYB locked the conversation correctly, but its signed policy decision was never delivered to NewAPI on the streaming path; every subsequent request was then intercepted by the local conversation lock as a non-counting `conversation_cyber_locked` event, so NewAPI's cumulative strike count stayed at zero and auto-ban never triggered. Streamed policy failures now preserve audit details, incident metadata and the signed decision across translated error responses, and a new advanced-protection toggle chooses whether upstream cyber-policy decisions count toward strike/suspension limits — disabled by default, so operators can observe before enforcing.
- **Prompt inspection gains a per-API-key scope, and policy auditing is unified end to end (PR #478 by @ifThink404).** Bound API keys can now choose `inherit` (local GuardPipeline plus configured remote review), `local_only` (keep deterministic local rules, session correlation, audit and risk profiling, but skip remote model review — for trusted integrations that shouldn't pay the review latency on every request), or `off` (skip prompt inspection entirely, with an explicit UI confirmation). The scope is server-side only — a request cannot select or override it through headers or body fields — applies consistently across HTTP, SSE and WebSocket flows including hot refresh for logical WS frames, and existing bindings migrate to `inherit`. The same PR lands durable prompt-policy incidents with exact request/attempt correlation, nullable scoring so "not evaluated" stays distinct from a real zero, and searchable CY-incident/model-review/audit/learning histories.
- **Moderation thresholds are configurable per category, only CY verdicts count toward bans, and audit-style models bypass terminal rules (PR #475 by @ekti123456).** The `omni-moderation-latest` integration now intercepts on locally configurable per-category thresholds instead of trusting the upstream's own verdict wholesale; ban counting only accumulates on genuine CY confirmations, so a moderation-model false positive can no longer contribute to auto-banning an account. Terminal strict-rule interception can exclude designated models — `codex-auto-review`'s built-in prompts are full of audit vocabulary that near-guaranteed terminal hits. Triggering CYB now locks the current conversation so continued turns can't dig the account deeper, and the previous version's undeletable-extra-rules bug is fixed.
- **The request log page filters by status, error type, retries and transport, and can search error text (PR #483 by @ImogeneOctaviap794).** Usage logs can be narrowed by status family or exact status code, error type, retry attempts and transport, and searched across error messages, models, endpoints, accounts and API keys — filtering 4xx or "all errors" now includes 499 so cancelled requests stop slipping through. An error-distribution summary with clickable counters and structured error-detail columns lands alongside, so chasing a batch of 401/429/5xx/499 no longer means paging through raw logs. Account-info search resolves matching account IDs first instead of re-parsing credentials on every log row.
- **Relay and OAuth accounts get separate model-cooldown policies (PR #482 by @ImogeneOctaviap794).** A transient 429 from a direct API upstream used to trigger the Codex OAuth model cooldown — persisting for minutes and benching a relay upstream that was perfectly retryable. Relay accounts now default to no persistent model cooldown, surfacing the HTTP 429 with a `Retry-After` once retries are exhausted, while OAuth keeps its 300-second adaptive cooldown and unchanged 5h/7d window handling. Both sides support `off`/`fixed`/`adaptive` with system-wide defaults and per-account overrides stored in credentials; the account detail sheet shows the effective policy and can clear single-model or all-model cooldowns, and resetting an account's status clears them too. Grok keeps its existing independent rate-limit logic.

### Fixes

- **`gpt-image-2-2k`/`-4k` on the public Images API now return the physical target size instead of the upstream base resolution (#477, reported by @OneAHPro).** The admin image studio upscaled these aliases to their promised size, but the OpenAI-compatible `/v1/images/generations` and `/v1/images/edits` only passed the size hint upstream and returned the ~1.57MP base image (a 3840x2160 request came back 1254x1254). The upscale pipeline moved from the admin layer into a shared package used by both surfaces — local Catmull-Rom or the external `IMAGE_UPSCALER_ENDPOINT`, behind the existing LRU result cache and bounded-concurrency gate — and the public API resolves the authoritative target from the request (an explicit user size is exact and never overridden by the alias default), upscaling final images on both streaming and non-streaming paths while partial-image previews stay untouched. A failed upscale degrades to the base-resolution image rather than failing a request whose image is perfectly usable, and usage logs record the final delivered dimensions. One deliberate semantic: a square input to a 16:9 4K edit yields 2160x2160 — the upstream returns a square base image and the shared fit-inside rule never crops content to force an aspect ratio. Image scheduling is also restricted to Codex accounts: Grok and relay accounts occasionally drew image requests they can never serve, answering upstream 401s that marked healthy accounts unauthorized.
- **Prompt-policy review fails fast when the external model degrades, and risk scoring is bounded (PR #474 by @ifThink404).** External model review gains a bounded circuit breaker so a degraded provider no longer stalls the request pool; timeout/unreviewed local blocks are treated as unverified evidence with deduplicated fingerprints and a capped standalone risk contribution, and successful model clears reconcile against matching unverified events in either write order while preserving confirmed CY, model and terminal-rule evidence.
- **The Codex account page's export no longer sweeps Grok accounts into the file.** "Export healthy accounts" listed every platform, so a codex-named export could contain Grok credentials (and the TXT variant emitted blank lines for their nonexistent refresh tokens). The export endpoint accepts a `channel` filter that the Codex page always passes; leaving it unset preserves the all-platform semantics remote migration depends on, and the Grok page keeps its dedicated exporter.
- **Retry attempts are only counted when a retry actually happened, and the credits toggle updates optimistically in the UI.**

## v2.7.0 - 2026-08-03

### Features

- **Prompt interception becomes an auditable policy subsystem: incident records, per-subject trust, and keyed NewAPI bindings (PR #468/#469/#470/#472 by @ifThink404).** Interception previously produced logs and nothing else — there was no record of how a given subject had behaved historically, and no way to reconcile upstream CY incidents with local verdicts. Five tables (`prompt_policy_incidents`, `prompt_risk_events`, `prompt_risk_event_sources`, `prompt_risk_trust_policies`, `prompt_filter_newapi_bindings`) and their endpoints land together: incidents are queryable by ID, risk profiles aggregate by subject type and key with grantable/revocable trust policies (carrying provenance and a model-review counter), and NewAPI auditing moves to one-to-one per-key bindings with safe secret rotation and runtime revocation. The old global passthrough secret, its duplicate endpoints and the compatibility fallback are removed; interception mode and review tier are now owned solely by GuardPipeline. `usage_logs` and `prompt_rule_candidate_evidence` gain `prompt_policy_incident_id` so usage and evidence link back to the incident.
- **Whole requests can be reviewed by a model, with the verdict recorded in the audit log (by @ifThink404).** Alongside rule matching, a request can now be handed to a model for review. `prompt_filter_logs` gains `reviewed`, `review_confidence`, `review_threshold`, `review_reason`, `review_endpoint`, `review_latency_ms` and `review_request_mode`, so the audit trail can answer whether a decision came from a rule or a model, at what confidence, and how long it took. A review connectivity-test endpoint is added, review concurrency is bounded without changing realtime timeouts, and `request_correlation_id` plus `session_hash` let multiple records from one session be tied together. Audit logs are filterable by scope.
- **Rule candidates enter an AI-assisted learning loop with auditable apply and rollback (by @ifThink404).** Asynchronous shadow evidence is reconciled against upstream CY incidents into rule candidates, which an AI provider can analyse to suggest identity attribution. Candidates expose their evidence and can be drafted, published or dismissed; identity updates apply and roll back individually. The AI provider is selectable, review concurrency is bounded, and behaviour stays consistent across replicas.
- **Prompt interception V1 hardening: custom-pattern admission, raw/effective config round-tripping, and non-persisting rule tests (by @ifThink404).** Custom rules now pass an admission gate so an overly broad rule cannot go live unnoticed; configuration distinguishes `RawConfig` from `EffectiveConfig` and round-trips between them; a rule test evaluation runs without writing to the database, so a rule can be validated without touching production. The PromptFilter admin UI is collapsed into a tighter layout with resources for all three languages. The stable-cohort rollout mechanism is retired in the same pass.
- **Image Studio gains batch jobs and RealESRGAN upscaling.** A single request can produce multiple outputs tracked as one job, upscaling is backed by RealESRGAN, and callers can select `upscale=none` to take the upstream image as-is — the admin dropdown separates that from the auto default.
- **Accounts with credits keep serving traffic after their usage window is exhausted, and the account list says so.** The `credit_skip_usage_window` toggle bypassed the 5h/7d usage-window penalties unconditionally — it never looked at the credit balance, so once credits ran out the scheduler kept feeding requests to an account that was guaranteed to 429. The bypass is now gated on credits actually being spendable (`credits_valid && !overage_limit_reached && (unlimited || has_credits && balance > 0)`), with an unprobed balance treated as no credits: waiting out one window is cheaper than hammering a wall. Two gaps behind the toggle close as well. Credit accounts previously only avoided *entering* a usage-window cooldown; an account already cooling down was never released, so the most natural workflow — noticing an account is rate limited and only then flipping the toggle — did nothing until the window reset. Enabling the toggle, or a balance top-up landing via the usage probe, now releases the locally derived usage cooldown, distinguished from a genuine upstream 429 by whether the penalty deadline lines up with the 7d reset instant; upstream cooldowns are left intact. The status badge deliberately keeps saying "rate limited" — the window really is exhausted — while a separate `using_credits` signal renders a compact credits icon between the status badge and the countdown, and `IsAvailable` keeps the account in rotation. Because such an account now *displays* as rate limited, both "clean rate-limited accounts" paths skip it, so a one-click cleanup can no longer delete an account that is working fine.
- **Referral invites show the remaining monthly capacity and the invites already sent.** The invite view could only send; there was no way to know how many invites were left or what happened to earlier ones, and the two upstream limits are not the same number — 10 sends per month, but only the first 3 earn rewards, so everything past the third was silently unpaid. The eligibility and tracking endpoints are now wired through, surfacing both capacities, the upstream offer copy, and the 90-day invite list with redemption status and expiry. Remaining capacities are carried as pointers so "upstream said zero" stays distinguishable from "upstream omitted the field" — only the former blocks sending.

### Fixes

- **`malicious_persistence_c2` no longer terminates a request over a bare mention of persistence — roughly 1128 false hits per day in production (by @ifThink404).** The Strict, terminal rule matched bare "persistence" / "持久化", so ordinary software-engineering text (a persistence layer, persisting a task id) was blocked outright. Strong terms (malicious persistence, C2 server, botnet, backdoor service) still match on their own; the weak wording now requires co-occurring malicious context. A pre-existing gap where a Chinese verb plus English "C2 server" went unmatched is fixed in the same change, with production-sample regression tests.
- **`operational_exploit_request` no longer fires on bare payload/poc vocabulary (by @ifThink404).** The Chinese branch matched unqualified "payload" and "poc", so ordinary developer text was terminally flagged: a git/API request payload ("使用生成的 payload 创建 pr") and the "poc" substring inside identifiers ("mattpocock/skills"). Bare payload is dropped — exploit-qualified 载荷 and "exploit payload" are retained — and poc is word-bounded, so genuine exploit/POC requests still terminate while developer vocabulary does not. The English branch is unchanged.
- **gpt-5.6 `encrypted_content` is recognised instead of flooding the audit as unclassified — about 35% of `/v1/responses` adapter_unclassified traffic, 3526 records a day (by @ifThink404).** It is an opaque server-side encrypted continuation payload from `store=false` reasoning and carries no scannable plaintext, so it is now recognised and skipped in both the top-level item and nested content-block paths. Co-located user text is still scanned. The noise disappears with zero detection impact.
- **`adapter_unclassified` audits record which type was unrecognised (by @ifThink404).** The audit previously reported only that one occurred. The request envelope now carries bounded, deduplicated type names, surfaced into the audit `match_context` as `unclassified_types: <name>` — schema names only, never user content — so operators can see which new Codex type needs an explicit adapter mapping.
- **Hidden continue-thinking rounds no longer hit a reverse proxy's idle timeout (#458).** With `codex_continue_thinking_enabled` on, hidden continuation rounds forward nothing downstream while opening the next round and while that round produces no forwardable reasoning events. Behind a reverse proxy with a 60-125s idle read timeout the proxy cancels the request with 524/context-canceled mid-fold, the CLI retries the whole outer request, and every retry re-enters the fold — a reconnect loop that multiplies latency and real upstream spend. Once the fold confirms continuation, a ticker writes a standard SSE comment (`: keepalive`) every 15 seconds; parsers ignore comments but every hop's idle timer resets. The callback stays inert until the first real body byte is written, so pre-first-token error semantics (real HTTP status for early `response.failed`, account rotation) are untouched. The pass-through path starts no ticker and stays byte-identical.
- **Image job warnings are separated from errors, and upscaling no longer crops by default.** A batch that degraded but still produced images was marked succeeded with the notice written to `error_message`; the external job API returns the record verbatim, so a caller treating a non-empty `error_message` as failure reported a successful batch as failed. The notice now has its own warning field and `error_message` means the job failed (no schema change — the two share a column on disk). The external upscaler also forced `fit=cover` whenever the requested size matched exactly, overriding the operator's `IMAGE_UPSCALER_FIT` and cropping any source with a different aspect ratio, while the local backend never crops — the same request produced different images depending on whether an endpoint was configured. The default is now to fit inside the box, identical to cover when the ratios match, with cropping still available to operators who ask for it.
- **Image outputs honour the requested size, and partially successful batches survive.** The local upscaler never received the requested size and resampled to the tier long side instead: asking for 2048x2048 produced 2560x2560, and 2880x2880 produced 3840x3840 — both first-class 1:1 sizes behind the `gpt-image-2-2k`/`-4k` aliases, so the default backend inflated every square output. The target box is now resolved once and shared by both backends, and `DoUpscaleTo` lets the local path hit an exact target without cropping a mismatched aspect ratio. An undecodable upscaler response previously failed the whole job while outputs saved before it stayed on disk and in the database, already billed but unreachable behind a failed job; it is now degraded like a transport failure, and both job runners report a save-stage failure as a warning whenever earlier outputs survived.
- **Image jobs regain admission control, and timed-out jobs no longer stick in "running".** `MaxConcurrency` stopped gating image jobs: the slot used to be reserved at enqueue and held until the background job finished, which is what made it admission control, but batching moved the reservation to each upstream call — so every request was accepted and the ones that could not run turned into failed job rows instead of a synchronous 429. Reserving at enqueue again is only safe if the job's own upstream calls stop reserving a second slot for the same key, so the existing concurrency-inherited signal is passed down through the admin image calls; with `MaxConcurrency=1` the job would otherwise deadlock against itself. Job deadlines now scale with the requested output count, since a batch can issue one upstream call per output and repeat the whole batch through the JPEG fallback. Terminal status writes run on their own context — the job context is routinely past its deadline by then, so the write failed and the row stayed "running" until the next restart with the error discarded. Upscaler responses outside `image/*` are rejected, and a fallback to a per-process random signing key is logged.
- **Referral invites work again — the upstream endpoint had moved and every send was failing.** `POST /backend-api/wham/referrals/invite` now returns a route-level 404: the `wham/` prefix was dropped and the request body's single `referral_key` field was split into `program_id` + `entrypoint`, confirmed against the upstream's own 422 validation response. The client was updated to the current contract, and the send-response parser accepts both `invites[]` and `items[]` since the success payload shape is not yet observed.
- **Cloudflare bot challenges are no longer reported as "this account has no referral eligibility".** These endpoints sit behind Cloudflare bot management, where Go's default TLS fingerprint had roughly half of all requests thrown into a managed challenge — the challenge page borrows a 403, and the UI mapped every 403 to an eligibility failure. Requests now use the repository's existing uTLS Chrome fingerprint, reusing the pooled maintenance client so no HTTP/2 connections leak, with a per-account cookie jar so `__cf_bm` is reused, taking a measured 2-of-10 success rate to 12-of-12. Challenge pages are detected explicitly — the whole body must be scanned, since the markers sit past 7KB of inline SVG — and reported as retryable instead of as an eligibility verdict. The polymorphic `detail` field is parsed in both shapes, so a recipient-level rejection reports the upstream reason and the exact failed addresses rather than blaming the account.
- **The Codex invite view has its own route.** It was local component state, so a refresh dropped back to the account list, the page could not be linked, and the browser back button left the accounts page entirely. It now lives at `/accounts/invite`, matching the existing `/accounts/grok` pattern; deep-linking while the account list is still loading no longer reports "no accounts available for invites".
- **Resetting an account's quota refreshes its status and usage bars immediately.** After clicking reset, the account kept showing its old usage and status until the operator ran a connection test and refreshed by hand. The reset succeeded and a usage probe was dispatched, but only as a background goroutine — the handler returned 200 immediately while the probe (a wham round trip, normally 1-3 seconds) was still in flight, so the client refetched stale data. The manual reset now waits for the probe to land before responding, bounded at 15 seconds; the response carries `usage_refreshed` and the refreshed status so the client can schedule one late refetch if the probe timed out, and an unavailable probe returns immediately instead of blocking the request.
- **gpt-5.6 encrypted content input items are accepted by `/v1/responses` (by @ifThink404).**

## v2.6.9 - 2026-08-01

### Features

- **Grok account credentials can be exported as auth.json files.** Grok account management had four import paths and no export. `GET /api/admin/accounts/grok/export` returns credentials in the Grok CLI single-credential layout — a single account downloads bare JSON, multiple accounts download a ZIP with one `<email>.json` per account, with sanitized filenames and numeric suffixes on collisions (API Key accounts all share the same placeholder email). The file is a deliberate superset of the CLI layout: the CLI format omits `client_id`, which the importer can otherwise only recover from the access token's JWT claims, so a CLI-shaped file becomes unimportable the moment its access token expires — export also writes `client_id`, `oidc_issuer`, `principal_type`, `principal_id`, `plan_type` and `auth_mode`, keeping the file usable indefinitely via its refresh token and letting API Key credentials round-trip correctly. Usage and billing fields are deliberately excluded: export moves credentials, not accounting state.

### Fixes

- **Long-running WebSocket streams are no longer killed at 120 seconds of upstream silence — and a genuinely stalled turn can no longer pin a stream forever.** The WS read path enforced a hard 120s timeout between business frames, so a long reasoning turn or a priority-queue wait that legitimately produced no incremental frames for two minutes was misjudged as a dead connection and cut off mid-response. Business-frame silence is now only a liveness checkpoint: recent inbound activity (data frames, peer pings, heartbeat pongs) proves the transport is alive, and an otherwise-silent connection gets a correlated Ping probe with a unique payload — a frame arriving mid-probe wins over probe failure via a queue-sequence guard, so no response is lost at the checkpoint boundary. The follow-up hardening closes the opposite gap the removal opened: liveness proves the transport is alive, not that upstream is still working the request, so a wedged upstream worker behind healthy heartbeats could otherwise pin the request, its read lease and the pooled connection indefinitely. Business-frame silence is now capped at 15 minutes (`CODEX_WS_MAX_TURN_SILENCE`, Go duration format, `"0"` disables), both failure paths report as timeouts in failure attribution again instead of generic transport errors, probe rescues and silence-cap kills leave attribution logs, and the never-consumed 2-minute pending-request timeout that other code cited as a backstop was retired. The probe-failure rescue branch is covered by a mutant-verified regression test.
- **Manual API key quota resets restart the enforced 5-hour and 7-day windows, and bulk resets are concurrency-safe (PR #463 by @gejifeng).** Resetting a key's quota previously left the sliding 5h/7d cost and token windows untouched, so a freshly reset key could still answer 429 until the old window decayed. Resets now restart the enforced windows while preserving usage history and unrelated windows, flush buffered usage logs and evict the relevant runtime caches so the reset takes effect immediately; the bulk reset-all path additionally guards against concurrent resets racing each other.
- **Overflow auto-compact summary requests are attributed to the API key that triggered them.** The experimental context-overflow auto-compaction issues an internal summarization turn; its usage previously landed unattributed. `usage_logs` gains `internal_reason` and `parent_request_id` columns (added automatically on startup) so the summary turn is billed to the originating key and linked to the request that triggered it.
- **gpt-5.6 Terra and Luna default pricing matches OpenAI's July 30 price cut.** Terra is now $2/$12 (−20%) and Luna $0.20/$1.20 (−80%), including the long-context and priority tiers, so deployments that never ran a models.dev sync stop overbilling.
- **Proxy auto-balance only rebinds genuinely refreshable OAuth accounts.** The v2.6.8 auto-balance treated every account as a candidate; API Key, relay-API, AT-only, Session Token and Agent Identity accounts should not have their egress reassigned automatically. They are now excluded as balance targets while their existing manual bindings still count toward per-proxy baseline load.
- **Grok request preflight is a single pass — 68× fewer allocations.** The upstream body was rewritten in three full passes (map decode/re-encode, up to five whole-body sjson mutations, a second `input[]` walk); since the official CLI replays full history every turn, a turn-five body is already ~800KB and the chain cost 5.9ms / 7.9MB / 21k allocations per request. `prepareGrokUpstreamBody` now rewrites top-level keys in place and splices history items as raw bytes, costing 1.4ms / 1.67MB / 308 allocations for the same body, with a differential test over a 50+ case corpus keeping the old chain as the reference oracle. The differential test also surfaced and fixed a latent bug: the old fast-path gate skipped normalization entirely for a `[compaction, user message]` input — exactly the first turn after a compaction.

## v2.6.8 - 2026-07-30

### Features

- **API keys gain calendar-day cost/token limits, and the key-usage page shows when every window resets (#460, requested by @viktorcao).** The existing 5h/7d/30d limits are sliding windows, so there was no way to give a shared key a plain "at most X dollars per day" budget. `cost_limit_daily` / `token_limit_daily` are enforced against the server's calendar day and fully recover at midnight; the 429 message names the exact reset time, and the limit-check cache key embeds the date so yesterday's usage can't leak past midnight. The self-service key-usage report now returns server-computed window metadata — a new "today" window plus `window_kind` with `reset_at` for the fixed daily window and `decay_at` (oldest entry + window length) for the sliding ones — so the portal shows when quota refreshes instead of clients guessing, and the admin key list gains a daily budget progress bar.
- **The response context cache gets real budgets, end-to-end metrics, and hot-reloadable settings (#456).** The in-process response cache previously grew without bound and its behavior was invisible. Memory and entry budgets are now configurable system settings applied without a restart, Redis-backed deployments refill the local cache from Redis after a restart, logical context bounds are preserved when trimming, per-key isolation is enforced, and the operations page reports cache size, entry counts and an aggregate hit/miss ratio measured end-to-end rather than per-layer. Cache hits are cloned outside the lock to keep hot paths from serializing on large payloads.
- **API key quotas can be reset manually — one key or all configured keys at once (#454, PR #461 by @gejifeng).** Dedicated endpoints and admin UI actions renew `quota_used` while preserving cumulative usage history and reset counters, and evict the proxy-side quota cache so exhausted keys become usable immediately instead of waiting out the cache TTL.
- **Proxies can be auto-balanced across accounts, and the proxy page survives large pools.** Too many Grok accounts egressing from one IP triggers upstream 402s, but hand-binding hundreds of accounts froze the page: the bind dialog rendered every account at once (re-rendering all of them per checkbox toggle) on top of a full account fetch with usage enrichment. `POST /proxies/auto-balance` now spreads accounts across enabled, reachable proxies least-bound-first with an optional per-proxy cap — existing valid bindings are kept to minimize IP churn, baseline load counts bindings from other channels, and the assignment algorithm is deterministic and unit-tested. The page reads a server-aggregated `bound_count` from `ListProxies`, the bind dialog lazy-loads a new `GET /accounts?view=lite` payload (identity/binding fields only, ~8x smaller), renders at most 100 rows with memoized row components, and `FetchGrokModelIDs` follows the same proxy fallback chain (explicit binding > sticky pool > global) as the main request path instead of always dialing direct.
- **Imported Grok accounts show their plan and usage automatically — no manual refresh.** Imported auth.json files usually carry an expired access token; the post-import probe only refreshed missing tokens, so the billing call 401'd with the stale token and could mark a healthy account unauthorized for 24h. The probe is now a three-step chain — force-refresh the token when absent or expired, run the billing probe (plan / weekly quota), then a connectivity test that writes the account status and brings back `x-ratelimit-*` headers to light up the usage bar — capped at 4 concurrent accounts so bulk imports don't stampede the upstream. The frontend silently re-fetches the list at 2.5s/7s/15s/30s after any import so the async probe results appear on their own.
- **Grok usage bars survive restarts.** The `x-ratelimit` snapshot backing the account-list progress bar was runtime-only, blanking every bar on container restart until the next request. Snapshots now mark a dirty flag; the store's background loop flushes dirty snapshots to credentials once a minute (per-request writes would hammer the DB), flushes once more on shutdown, and account loading restores them without re-marking dirty.
- **Grok file imports drive the same top-right progress toast as Codex batch operations.** auth.json / sso.txt / refreshtoken.txt imports report per-chunk progress (200 entries per chunk) with success/failure counters in the shared progress overlay; the detail modal now only opens when some entries actually failed. The Grok page also stops double-rendering: the desktop table and mobile card grid were both mounted with one merely CSS-hidden — a media query now mounts only the visible one, and rows are memoized with ref-forwarded handlers so a checkbox toggle repaints one row instead of every row twice.
- **Grok subscription tiers are first-class.** A `GrokPlan` model resolves the tier from the access token's claims (with alias handling and stored-credential fallback), billing reflects the resolved plan, and the Grok account page displays plan badges and can filter accounts by tier.
- **Proxy connectivity results are persisted with a one-click cleanup for dead proxies (PR #455 by @Futureppo).** The `proxies` table gains a `test_status` column kept in sync by single and batch tests, the page shows an untested count, and a clean-error action removes failing proxies while unbinding affected accounts.
- **Usage logs expose the context-compaction trigger and history, in a tidier table.** Compaction badges land in the usage log detail, the first-token and total-duration columns merge into one color-coded timing column, and the table gains styling polish plus a refresh button.
- **Base concurrency overrides are no longer capped at 50.** Global `max_concurrency`, per-account and per-group overrides accept any integer ≥ 1, matching deployments that front large account pools; validation, API docs and admin UI inputs updated together.

### Fixes

- **Response cache hit/miss statistics are measured end-to-end.** The aggregate counters previously mixed per-layer probes, over- or under-counting deployments with both local and Redis tiers; hits and misses are now attributed once per request.

## v2.6.7 - 2026-07-28

### Features

- **Account batch tests and refreshes can optionally show structured per-account results.** Large account-pool operations previously ended with only aggregate toast counts, so operators could see that some accounts failed without knowing which accounts, why they failed, or which upstream HTTP status was involved. The streaming admin events now carry a normalized result status and HTTP status per account, and the Codex/Grok account pages can retain those events in an opt-in results dialog with summary cards, status filters, pagination, account labels, and persisted visibility preference. Batch-test IDs are also constrained to the active Codex or Grok channel before submission, preventing an “all accounts” action from silently testing the other tab’s pool; the default remains the existing lightweight progress/toast flow when detailed results are disabled.

### Fixes

- **Weak-network WebSocket mode avoids reusing stale VPN, residential-proxy, and NAT connections (#442, reported by @huyachigege).** From v2.6.0 onward, longer-lived upstream WS pooling could repeatedly select a socket that an unstable intermediate network had silently invalidated, producing frequent internal 598 stream failures even though short-lived connections were reliable. A new opt-in `codex_ws_weak_network_mode` setting limits idle reuse to 15 seconds, rotates every connection after 3 minutes, and requires a real Ping/Pong round trip before every reuse instead of trusting recent inbound activity. Idle keepalive is paused in this mode so it cannot extend stale connections, while heartbeats remain active for in-flight responses; the mode is disabled by default and exposed in Runtime Settings.
- **Provider-qualified OpenAI Responses model IDs are accepted end to end.** The shared validator allowed only short `A-Z`, digit, dot, underscore, and dash identifiers, so valid custom upstream names such as `openrouter/gpt-5:free` were rejected before they could reach an OpenAI Responses relay. Model validation now allows provider separators and Unicode identifiers up to 255 characters while retaining a constrained character set, connection tests preserve the selected custom model, and the account editor adds a clear-all action for long supported-model lists. Regression coverage verifies that a provider-qualified model survives the complete `/v1/responses` streaming path unchanged.
- **Proxy-list pagination no longer overflows when many pages exist (#452, by @Futureppo).** The page rendered every page-number button in one unbounded row, making the proxy-management card spill horizontally on larger pools. It now uses the shared bounded pagination component, keeps the selected page size in the existing persisted preference, and clamps the current page when filtering, deletion, or a page-size change reduces the number of available pages.
- **Streaming and authentication paths retain less memory, and the operations dashboard now reports real process RSS.** The SSE reader previously copied every single-line event through `bytes.Join`, repeatedly shortened its pooled line-buffer capacity and reallocated on the next chunk, and kept references to completed large events; it now avoids the common extra copy, compacts the existing buffer in place, and clears retained slices while preserving fragmented and multi-line SSE behavior. The authentication-specific uTLS HTTP/2 transport now has read-idle, Ping, and 90-second idle-connection timeouts, closes only connections with no active/reserved/pending streams, and uses compare-and-delete during TTL eviction so a refreshed pool entry cannot be removed by a stale cleanup pass. On Linux, `process_bytes` is now read from `/proc/self/status` `VmRSS` instead of Go’s high-water `MemStats.Sys`, so the dashboard displays current resident memory rather than virtual memory retained by the runtime.
- **The default minimum emulated Codex CLI version is now `0.144.1`.** New installations, newly added database columns, and empty/fallback runtime configurations previously used the obsolete `0.118.0` floor even when the rest of the client fingerprint had moved forward. PostgreSQL, SQLite, bootstrap, frontend, and runtime defaults now agree on `0.144.1`; an existing explicitly stored minimum version remains under administrator control and is not overwritten.

## v2.6.6 - 2026-07-27

### Features

- **Requests without a Codex client fingerprint can be routed into dedicated account groups (#445, by @ekti123456).** Traffic that carries no `x-codex-*` header — third-party gateways, custom clients, anything that isn't the official CLI — can now be confined to a chosen set of account groups, keeping it off the accounts that serve real Codex sessions. Two follow-ups make the isolation actually hold: an **empty allowed-group list on a key** (how most keys are configured) previously aborted the split entirely, so the split group served the no-fingerprint traffic it was meant for *and* kept taking Codex traffic — it now means "every group except the split ones"; and the filter was only wired into the five conversation entry points while store-level authorization had been widened everywhere, so **image requests leaked in both directions** (landing on split accounts regardless of fingerprint, and no-fingerprint image traffic never being confined) — image selection (preferred tier, fallback tier, and the wait-for-account path) plus the alpha search endpoint now apply the same filter. The UI hint spells out the empty-list behavior and, importantly, that the fingerprint is a **client trait any downstream caller can set itself**, not a boundary it is kept out by.
- **The Grok OAuth `client_id` is configurable — as an environment variable and as a hot-applied system setting (#448, by @Story19240).** The official Grok CLI id was a hard-coded constant across eight call sites (browser authorize/exchange, refresh, the SSO→Build conversion chain, the manual device flow); it is now a single configurable value: `GROK_OAUTH_CLIENT_ID` (reusing the existing `grokEnv` convention) or `grok_config.oauth_client_id`, editable from the new **Grok section** of the settings page. Precedence is env var → system setting → built-in id, deliberately in that order: the env var is deployment-level configuration that still holds if the stored value is wrong or the admin UI is misused, and can be corrected without reaching the database. When the env var is set, the settings API reports the override alongside the effective value and the form disables the input and names the value actually in use, so nobody edits a field that cannot take effect. Since a `client_id` ends up in the authorization URL and the token form, values with whitespace or control characters, or longer than 128 bytes, are rejected with a `400` rather than silently normalized to empty — a silent fallback would look like a successful save while the default was still in use. Existing accounts are untouched: refresh uses the `client_id` stored in each account's own credentials, so this only affects authorizations created from now on.

### Fixes

- **HTTP/2 connections and goroutines no longer leak without bound in `utls_chrome` transport mode (#446).** The self-managed uTLS transport never reclaimed its connections: a reporter saw **45k ESTABLISHED connections and 45k goroutines after ~39h with only 3 active requests**, growing until the host ran out of memory. Four independent defects combined. `IdleConnTimeout` was never set on the `http2.Transport` — `NewClientConn` only installs the idle timer when the timeout is non-zero, so `closeIfIdle` never fired and every connection's `readLoop` goroutine pinned its socket and buffers forever, unreachable for GC (now 90s, matching the standard transport, which backstops every other reclaim path). `CloseIdleConnections()` tested `!CanTakeNewRequest()`, but healthy idle HTTP/2 connections answer `true` — the condition skipped precisely the connections that needed reclaiming; it now reclaims connections with no in-flight streams, unlinking under the lock and closing outside it, with a short grace window so a connection handed out but not yet used isn't closed underneath its caller. The pooled-client eviction path called `client.CloseIdleConnections()`, a no-op for uTLS, so every TTL eviction leaked a connection — this hit normal `/responses` traffic, not just auxiliary calls — and now dispatches by transport type. Finally, wham usage/reset-credits, the Codex models manifest, alpha search and subscription sync each built a throwaway `http.Client` per call (constantly, given background probes and clients polling `/models`); they now share pooled clients keyed by purpose + account + proxy + transport mode, with maintenance clients kept separate from the `/responses` pool so a failing background probe can't recycle live request connections. In-flight streams are drained by graceful shutdown rather than a hard close, capped by the new `utls_shutdown_timeout_minutes` setting (default 30 min, range 1–240, out-of-range clamps to the default) since a single streaming turn can exceed 10 minutes (#287). Verified against a live PostgreSQL deployment in `utls_chrome` mode: 210 `/models` requests (41 reaching upstream) left zero upstream `:443` connections after settling, goroutines returned to the 29–30 baseline, and streaming responses completed intact.
- **Usage-log batching can no longer stall on one unwritable row, drop the buffer on shutdown, or grow until OOM (#444, by @ImogeneOctaviap794).** The batched writer landed with three failure modes that all cost money rather than just dashboard data — the same transaction advances the API key scope counters and `api_keys.quota_used`, and budget enforcement reads exactly those persisted counters, so a stuck writer leaves budgeted keys below their limit forever. A failed batch insert was always requeued at the head of the buffer, so a row that can *never* be written (SQLSTATE class 22/23 — overlong value, invalid UTF-8, numeric overflow, constraint violation) stalled the writer permanently; failures are now classified, transient errors (connection loss, timeout, deadlock) requeue exactly as before while data errors **bisect the batch**, write everything writable and drop only the offending rows, never handing back rows already committed. `Close()` called `flushLogs()` after stopping the background flusher, but `flushLogs()` had become batch-at-a-time and relied on that flusher to drain the rest — so everything past the first batch was dropped on graceful shutdown, worst exactly in the high-RPM case the batching targets; it now drains via `FlushUsageLogs()`. Overlong values are clamped to their column width by rune before entering the buffer (widening `varchar(20)` to `varchar(100)` raises the ceiling but doesn't remove the failure mode — `reasoning_effort` / `service_tier` come straight from the request body or the upstream response). And the unbounded buffer is now capped at 20000 entries, dropping oldest first, with the dropped total surfaced on the runtime status page next to the buffer gauge.
- **Grok batch import stays usable at the raised 5000-file cap (#447, by @Futureppo).** The caps went from 500 files / 200 refresh tokens to 5000 without touching the two things that made the old numbers work. The file import ran its whole sequential insert loop under a fixed 60s deadline, so the per-file budget *shrank* as the batch grew — 120ms at the old cap, 12ms at 5000 — and any database hiccup expired the context partway through, reporting `context deadline exceeded` for every remaining file; the deadline now scales as 30s + 100ms per file, capped at 10 minutes so one oversized request can't hold a connection open indefinitely. Wall-clock was the other half: a single request inserting thousands of rows, or refreshing thousands of tokens six at a time, runs far longer than a browser or reverse proxy will wait, with no progress and no way to tell what landed — the credential-file and refresh-token imports are now submitted in **chunks of 200**, merging per-chunk results, showing which chunk is running, and keeping finished chunks visible if a later one fails.
- **Grok requests stop 400-ing at the context-compaction turn (#450).** Codex CLI's compaction turn emits `tools:[]` together with `tool_choice:"auto"`; the xAI upstream hard-validates that combination and answers `400 invalid-argument "A tool_choice was set on the request but no tools were specified."`, while the Codex/ChatGPT upstream tolerates the identical body — so every long Grok session broke at its compaction point, and a `400` isn't retried. The Grok sanitize path now drops `tool_choice` when `tools` is absent or empty (with no tools declared the field has no meaning, so removing it is semantically free). The step sits **after** tool normalization on purpose, which also covers a body the gateway builds itself: dropping the only declared `web_search` tool (`external_web_access:false`) empties `tools` while a `"auto"` choice survives. The normalization stays scoped to Grok — altering the Codex request body would diverge from what the official CLI sends, and there's no evidence that upstream rejects it. Verified live: the `400` reproduces verbatim before the fix, returns `200` with normal text after, and a request with real tools still returns its `function_call` untouched.

## v2.6.5 - 2026-07-25

### Features

- **Per-key usage budgets scoped to individual account groups and accounts (#439).** A key allowed to reach both a premium group and a cheap relay group can now be capped on the premium one alone and fall through to the cheap one once that cap is hit. Budgets are enforced as **scheduling candidate filters** rather than an entry-side reject — the whole point is to keep serving from the other scopes — hooked into the existing `auth.AccountFilter`, which covers the fast scheduler, the slow loop, lazy mode and sticky session reuse in one place. `on_exhausted=skip` (the default) drops that scope's accounts from the request's candidate pool and, when the pool ends up empty, answers `429` naming the exhausted scope instead of the vague `503 no available account`; `on_exhausted=reject` fails the whole key as soon as any scope runs out. Three independent dimensions: **sliding windows** (5h/1d/7d/30d over cost / tokens / requests), **cumulative quotas** that never decay and need an explicit reset, and a **per-scope concurrency cap**. Metering uses `user_billed`, matching the existing key-level cost limits, and unpriced models bill `$0`, which is why the token and request dimensions exist at all. Window data comes from one per-account `usage_logs` aggregation per window (cached 60s, so the cost is independent of how many scopes a key configures) plus an in-process delta so a budget that just ran out isn't overshot for a whole cache cycle; with Redis the delta is also published to per-minute buckets for sibling instances, merging only buckets strictly newer than the snapshot minute since the straddling bucket would double count. Group attribution is computed in Go against live group membership, so moving an account between groups takes effect immediately without denormalizing `group_id` into `usage_logs`. Cumulative quotas are a separate data source — `api_key_scope_counters` is incremented inside the same transaction as the usage-log batch, so the verdict lags one flush exactly like `api_keys.quota_used`, with group rows resolved by a SQL join on `account_group_members` and only keys whose persisted limits declare a cumulative quota accounted for; limits stay in the limits JSON while used amounts live in the table, so the config has exactly one copy. Per-scope concurrency is deliberately a **soft** cap: the filter check and the slot acquisition race, so a burst can exceed it by one or two, and making it hard would mean reworking the retry structure of all six forwarding paths — slots carry a deadline so a missed release self-heals instead of wedging a scope forever, and concurrency ignores `on_exhausted=reject` because transient contention shouldn't fail a request outright. Exhausted scopes are counted per affected request, surfaced in the edit drawer and logged (throttled), since a request silently downgraded to a worse account otherwise reads as an unexplained quality drop. The admin side rejects budgets pointing at a non-existent group or account with `400` instead of dropping them silently, prunes entries when a group is deleted, cleans counter rows when a key is deleted, and warns in the UI when a key's only allowed group carries a `skip` budget — a combination that would disable the key outright.
- **Account groups can be chosen while importing or adding accounts, not only afterwards.** Binding accounts to groups already existed, but only after the fact: you imported 200 accounts, went back to the list, and bulk-assigned them. The group choice is now threaded into the import parameters for the four high-traffic entry points — file import (`txt` / `json` / `json_at` / `at_txt`), pasted RT/ST, pasted AT, and the Grok imports (single add, SSO, refresh token, credential files). Two rules hold everywhere and both are load-bearing: group existence is verified **before** any account is inserted (a typo'd group id is the common failure, and rejecting it while nothing has happened beats leaving half an import unbound — after insertion the only remaining failure is a DB error, reported explicitly as "accounts imported, but group binding failed"), and the runtime account pool is synced after persisting, because groups feed concurrency caps, auto-pause thresholds and which API keys may schedule an account, so skipping the sync would leave the choice inert until the next full reload while the database looks correct. Accounts matched as duplicates — or matched by OAuth identity and merely refreshed — keep their existing groups, so re-importing the same file can't clobber grouping an operator adjusted by hand; each entry point binds only the ids it actually created, and an empty selection means "don't bind", not "clear groups". Binding runs before the SSE `complete` event, since the frontend reloads the list the moment it sees `complete` and would otherwise flash the accounts ungrouped, and Agent Identity entries inside a mixed import file are bound alongside the regular tokens so one import never ends up half-grouped. The frontend reuses `AccountGroupMultiSelect` in the add dialog, the file-import dialog and the Grok import dialogs, remembering the last selection in `localStorage` (batch imports are usually the same set of groups) with the remembered ids pruned against the live group list so a deleted group can't sit in the form and `400` the next import. Not covered yet, deliberately: OAuth authorization and the Grok device-code flow (the choice would have to live in the 30-minute server-side session and be re-verified at exchange time), sub2api pull, OpenAI-relay accounts, and the public self-service portal — external submitters shouldn't pick their own scheduling group, so those stay an admin decision on the accounts list.
- **xAI Grok rates land in billing, the pricing page, and the models.dev sync.** Grok models had no built-in pricing, never appeared on the pricing page, and were invisible to the models.dev sync, so every Grok request was billed at the `$1/$2` fallback rate. Billing gains rules for `grok-4.5`/`4.3`/`4.20`/`4-fast`/`4`/`code-fast-1`/`3-mini`/`3-fast`/`3`/`2`, taken from the models.dev `xai` provider (with xAI's public rates for the two models it doesn't list) — `grok-4.5` is `$2/$6` with `$0.3` cache reads. The long-context threshold also becomes **per-model**: Grok switches tiers at 200K, not OpenAI's 272K, so 200K–272K inputs were charged at the short-tier rate while `grok-4.5`'s long tier is double (`$4/$12`). The pricing page merges in Grok models — the union of account whitelists plus the default set when an account declares none — sorted as their own block after the Codex models, leaving Codex-only deployments unaffected; the models.dev sync reads the `xai` provider alongside `openai` instead of `openai` only, with resellers still excluded so their markups can't overwrite list price. Verified live against a Grok account: the pricing page lists all five exposed Grok models, and a `grok-4.5` request (219 input / 128 cached / 79 output) bills `0.0006944` USD, matching the xAI rate card to the cent.

### Fixes

- **The final `chat.completion.chunk` carries `"delta":{}` again, unbreaking strict-deserializing clients (#440).** The terminal chunk only held `index` and `finish_reason`, so `omitempty` dropped `delta` from the payload entirely. Clients that deserialize each chunk into a strict struct — Rust/serde-based CLIs among them — treat `delta` as a required field and abort the whole turn with `serialization error: missing field 'delta' at line 1 column 150`, which surfaced as an apparently random mid-turn failure rather than a schema mismatch. The final chunk now always emits `"delta":{}`, matching the official OpenAI stream shape. Verified against a live `grok-4.5` account for both the text turn (`finish_reason=stop`) and the tool-call turn (`finish_reason=tool_calls`).

## v2.6.4 - 2026-07-25

### Features

- **The account list can be scoped to one upstream server-side, so the Grok and Codex admin pages stop loading the whole pool.** `GET /accounts` takes an optional `channel=codex|grok`, filtered in SQL on `credentials.upstream_type` (both the SQLite `json_extract` and Postgres `->>` paths, with `codex` meaning "not Grok" so legacy rows that never recorded an `upstream_type` stay on the Codex side). Each page now requests only its own set — the Grok page `channel=grok`, the Codex page `channel=codex` — which cuts both the transferred payload and the per-account enrichment work on large pools; omitting the parameter keeps the previous all-accounts behavior, and the API docs page documents it. Covered by a new `ListActiveByChannel` test.
- **The Grok accounts page gains pagination, sorting, group chips, and a confirmation before pool-wide batch tests.** Client-side pagination replaces the single unbounded list; accounts can be sorted by usage percent (7d/5h), request count, updated time, or clustered by group, each direction-toggling on a second click; rows carry group chips and there's a group filter, with search extended to match group names. Batch connectivity testing now asks first when it would fan out beyond an explicit selection — one dialog for "test the N accounts in the current filter", another for "no selection and no filter, this tests all N Grok accounts" — so a large pool isn't probed by an accidental click.

### Fixes

- **`react-router` bumped to 7.18.1, closing the one new advisory this admin UI can actually reach (GHSA-wrjc-x8rr-h8h6).** Four advisories landed against the pinned 7.17.0; three of them need React Server Components or SSR hydration paths the admin frontend doesn't have, but the backslash open redirect in `<Link>`/`useNavigate` — a CVE-2025-68470 bypass, patched in 7.18.0 — applies to any client-side router. The lock moves to 7.18.1 and the declared floor to `^7.18.0`, so a fresh install can't resolve below the fix.
- **The frontend security gate stops failing on an advisory that can't trigger here, without lowering the bar for real ones.** `GHSA-qwww-vcr4-c8h2` (RSC-mode CSRF bypass) covers `react-router >=7.12.0 <8.3.0` with no patch inside the 7.x line, so every push to `main` failed the scan: npm's own remediation — downgrading to 7.11.0 — would reinstate the open redirect above, and 8.3.0 is a major migration (`react-router-dom` has no 8.x, so it means 12 rewritten import sites, React `>=19.2.7`, and the frontend build image moving from node 20 to 22). Since `npm audit` can only relax its threshold globally — which would wave through genuine high findings — the CI step's bare `npm audit --omit=dev --audit-level=high` is replaced by an equivalent gate that takes a per-advisory allowlist (`frontend/scripts/audit-gate.mjs`). Its single entry records why the RSC path is unreachable in a pure client-side SPA (`BrowserRouter`, no RSC/SSR entry, no server actions) along with a review date; everything else high or critical still fails, an unobtainable audit result fails closed, and allowlist entries that stop matching are reported so they get deleted. The react-router v8 migration is left as a separate, deliberate change.

## v2.6.3 - 2026-07-25

### Features

- **Account groups become a first-class axis in account management and key usage analysis.** The Accounts page gains a **group clustering sort** — a toggleable asc/desc sort mode (in both the toolbar and the table header) that clusters accounts by their group's configured order, so a mixed pool reads as contiguous group blocks instead of an interleaved list. The group multi-select picker now supports **inline group creation**: typing a name that doesn't exist yet offers a create action right in the dropdown (available from the account editor, quick add, and batch import forms), auto-assigning a palette color — no round-trip to the group manager. The picker's dropdown is also portaled so it renders correctly inside dialogs and sheets instead of being clipped by the container. On the analysis side, the API key Token Usage panel's per-account breakdown rows now carry the account's **group chips** (name + color, batch-fetched server-side to avoid N+1 queries) plus usage/group sort controls, so a key's traffic can be read per group at a glance.

## v2.6.2 - 2026-07-24

### Features

- **Proxies can be bound to accounts in bulk, straight from the proxy-pool page.** Beyond round-robin pool assignment, the Proxies page now lets you pick a proxy and open a bind dialog over the whole account pool: search by name/email/ID/status, filter by bound state (unbound / bound to this proxy / bound elsewhere) and account type (Codex/Grok), multi-select with select-all-visible, then **Bind selected** (writes the proxy into each account's `proxy_url`) or **Unbind selected** (clears it), with a per-batch success/failure summary and a bound-accounts count per proxy in the table.

### Fixes

- **In-flight requests are protected from WebSocket pool maintenance — no more same-second batches of 598 on unstable networks (#436).** The v2.6.0 trailing-metadata-frame fix made pooled WS connections genuinely long-lived for the first time, which exposed two latent maintenance paths that could kill connections *with requests still in flight*: a failed 30s heartbeat ping called `session.Close()`, cancelling every pending request on that session at once (under a network stall ≥10s, every pooled connection's heartbeat fails in the same window — the reporter's "batches of 598 with identical timestamps"), and the 30s `evictExpired` sweep closed idle-expired connections/sessions without checking `PendingCount()`, so a long-thinking turn or lost pongs got an active connection reaped mid-stream. v2.5.9 was immune only by accident: the trailing-frame bug killed every connection after each turn, so these paths never ran against live traffic. Now, any connection or session with in-flight requests is off-limits to maintenance: a failed ping removes the connection from the pool (blocking new reuse) but leaves the socket open for the read path to adjudicate — a write-path failure does not prove the read path is dead — and eviction skips busy objects entirely, mirroring the protection the over-age rotation already had. Idle objects keep the old discard semantics. Regression-tested both ways (the new tests fail on the old code), and verified against the real upstream over connection reuse.
- **`/v1/messages` upstream failures are surfaced to the downstream instead of being disguised as clean completions (#435).** When a stream broke mid-turn (or the upstream sent `response.failed` after content had started), the Anthropic translator fabricated a `stop_reason: end_turn` + `message_stop` ending — the downstream gateway or client saw a well-formed, "successful", often-empty message: unbillable-but-billed, undetectable, unretryable. Both paths now emit the protocol's in-stream `event: error` frame instead (the long-orphaned stream-error helper, rewired through the flush writer), so Claude Code and gateway chains recognize the failure and retry on their own. The silent-retry window also widens: stream-commit decisions now use a strict "first real content frame" tracker (decoupled from the loose `first_token_mode` stats setting, which counted structural frames like `response.output_item.added` as first token), and pre-content structural frames stay buffered — a break during the thinking-start phase, exactly where the #436 connection kills used to land, is now retried on another account with the downstream none the wiser. Thinking deltas follow structural frames within milliseconds, so the #207 reasoning-latency guarantee is preserved. Entry-stage rejections (missing `model`, invalid JSON, oversized body, translation failure) finally log to the console with IP and user-agent — previously a rejected client (the reporter's Hermes case) left no trace anywhere. Verified against the real upstream: thinking + text streams keep their exact shape, and the new failure tests fail on the old code.
- **Account form proxy input layout tidied; the proxy-pool selector only renders when the pool has entries.**

## v2.6.1 - 2026-07-23

### Features

- **Key × account cross-usage attribution, from both the account and the API-key views.** A shared pool split across several downstream keys can now be attributed in both directions off the existing `usage_logs` (which already carries both `account_id` and `api_key_id`, so no write-path change). The account usage modal gains a **by-key breakdown** — which downstream keys drove this account and how much (requests / tokens / cost, with a share bar and a requests/tokens/cost metric toggle). The Token Usage panel's per-key rows become **expandable**: opening a row drills into a **by-account breakdown** — which upstream accounts that key called and how much — fetched on demand for the panel's current time range, with multiple rows expandable at once. Backed by a new admin endpoint `GET /usage/api-keys/:id/accounts` and `AccountUsageDetail.by_api_key`.
- **Usage analysis available from the Grok account page.** Grok accounts get the same usage-analysis entry as Codex accounts — a `BarChart3` action opens the shared usage modal (overview / detail / quality tabs plus the by-key breakdown), reusing `AccountUsageModal` with the Codex-only credit-voucher / reset-credit sections hidden via a new `showCreditSettings` prop. The account detail sheet also grows a Grok-aware variant (rate-limit filter, OAuth-vs-API-key fields, Codex-only actions suppressed).

### Fixes

- **`/v1/alpha/search` no longer 400s on `prompt_cache_key` (#433).** Newer Codex clients (`codex_vscode/0.144.5`) include a Responses-style `prompt_cache_key` in the standalone `/alpha/search` request body, but the ChatGPT backend search endpoint has a narrower schema and rejects it with `400 Unknown parameter: 'prompt_cache_key'` — so every request with a search task failed while sibling `/responses` calls (which accept the field) succeeded. The standalone-search handler forwarded the body verbatim; it now sanitizes the body before forwarding, dropping fields the search endpoint doesn't accept. `prompt_cache_key` is a caching hint with no meaning for a retrieval call, so removing it doesn't change search semantics; the strip is kept as a field list so any future incompatible parameter is a one-line addition.

## v2.6.0 - 2026-07-23

### Features

- **Configurable, auditable, gradual-rollout layered Prompt Guard for the V1 APIs (#429, by @ifThink404).** An opt-in gateway-side protection layer inspects V1 requests across `/v1/responses`, `/v1/chat/completions`, `/v1/messages`, the Responses WebSocket, and a new text Realtime WebSocket, to reduce the risk of shared upstream accounts being flagged by the provider's safety systems for jailbreak / prompt-injection traffic. It splits the request into independent origins — current-user prompt, history, system/developer instructions, tool output, tool arguments, attachments, session context — so tool output or app context can't inflate the user's execution score, and separates an **execution score** (the only one that can block/strike) from a **shadow audit score** (observe-only). Detection adds bounded normalization/decoding (URL, HTML, Base64, Hex, ROT13, escapes, compression) under a scan budget, combination rules with evidence binding, strict terminal rules, per-provider profiles (balanced/strict/research), user/IP/session cumulative risk, per-user/-key percentage rollout, an optional semantic sidecar and attachment/intelligence extension points (each independently switchable with timeout/concurrency/cache/circuit-breaker), and SSE/WebSocket output scanning before frames reach the downstream. A signed **NewAPI audit protocol** passes through and verifies user id / real IP / request id / timestamp / HMAC and returns structured strike/ban evidence, backed by an async audit queue. The admin **Prompt Filter** page gains full visual configuration, rule intelligence history/diffs, trigger logs and complete audit context. Every switch — the master toggle, NewAPI policy, strikes, account bans, IP blacklist, and the auxiliary layers — **defaults to off / monitor**, and the recommended path is the documented four-stage grayscale (monitor → warn/small-traffic → targeted enforce → strict terminal + auto-strike). One operational caveat is documented in-app: with output scanning on, `codex_preflight_sse_passthrough` no longer reliably restores original first-frame speed, so the two must be grayscaled together.
- **Grok upstream hardening — cooldown persistence, periodic probe, retry budget, and model registration.** Grok accounts persist per-model cooldowns across restarts, run a configurable background connectivity probe (`grok_config`, default off / 30 min) that rewrites account status, gain a per-account rate-limit retry budget (`grok_config.max_rate_limit_retries`, `0` = follow global), clamp reasoning effort to Grok's supported band (`xhigh`/`max` → `high`, `minimal` → `low`) on the mandatory sanitize path, and — when they declare no model whitelist — auto-register a default Grok model set (`grok-4.5`, `grok-4`, `grok-3-fast`, …) into `/v1/models`. Free-tier `429` responses now surface authoritative token usage and a rolling-24h quota bar instead of a blind 30-minute cooldown.
- **Generic API keys can schedule undeclared Grok accounts via the default model set.** A key with no upstream-channel pin previously could never reach a Grok account that declared no model whitelist (the relay filter treated an empty whitelist as "serves nothing"), so `grok-4.5` returned a permanent 503 even though `/v1/models` advertised it. Undeclared Grok accounts now serve exactly `DefaultGrokModelIDs`, matching what the model list advertises, while non-Grok models still can't leak onto Grok accounts.
- **Proxy-pool selector in the account forms, and OAuth Grok accounts hide the fixed Base URL.** Both the Codex and Grok account add/edit forms grow a "select from proxy pool" dropdown that fills the chosen proxy's URL into the still-editable field (hidden when the pool is empty). OAuth Grok accounts use the official `cli-chat-proxy` endpoint, so the Base URL field now shows only for API Key accounts in both the add and edit dialogs.
- **`/v1/messages` thinking round-trip — reasoning signatures replayed as `encrypted_content`.** On the Anthropic endpoint, the translator now emits a `signature_delta` carrying the upstream reasoning `encrypted_content`, and on the input path rebuilds signed thinking blocks into `reasoning` items, so a multi-turn Claude Code conversation keeps the upstream's encrypted reasoning context instead of dropping it every turn. Verified end to end on both Codex (`gpt-5.5`) and Grok (`grok-4.5`) upstreams.
- **Optional preflight SSE passthrough compatibility switch (#425).** A default-off, hot-reloadable `codex_preflight_sse_passthrough_enabled` restores the legacy behavior of forwarding pre-content metadata events (`codex.rate_limits` / `codex.response.metadata` / `response.metadata`) immediately instead of buffering them until first content — for downstream gateways that treat the first SSE data frame as time-to-first-response. The admin UI warns that enabling it commits HTTP 200 before first content, reverting the pre-content-failure recovery semantics.
- **Optional `first_token_ms` accounting that excludes WS connection-acquire time.** A new setting lets the logged `first_token_ms` subtract the per-attempt WS connection-acquire cost (default off, keeping the original acquire-inclusive figure); the raw value is still `first_token_ms + ws_acquire_ms`.
- **Grok-only bulk clean (banned/error) with custom confirm dialogs.** The Grok accounts page gets scoped clean actions and a plan filter, with dedicated confirm dialogs.
- **OAuth accounts dedupe by workspace identity — `email + workspace_id` (#431, by @jenfonro).** OAuth dedup previously keyed on `email + account_id/chatgpt_account_id/user_id`, which some import sources and legacy backfill polluted with `user-*` / legacy shapes, so accounts under one email couldn't be reliably de-duplicated across (or within) workspaces. Standard Codex OAuth accounts now dedupe on an independent `workspace_id`, parsed from the JWT only when the same token yields both `email` and `chatgpt_account_id`: imports, OAuth authorization, self-service submission, and account restore all match on `email + workspace_id`; RT/session refresh writes the workspace identity back into credentials to backfill bare-RT / legacy accounts; and a one-time `20260722_workspace_identity_v3` data migration backfills missing `workspace_id` from the JWT (active-pool accounts only, only when the JWT email matches, never touching the recycle bin or legacy `account_id`) and merges the resulting `email + workspace_id` duplicates. When `workspace_id` is known it now takes precedence over the `allow_duplicate` flag — an account explicitly imported as a duplicate is still merged if it provably shares a workspace; `allow_duplicate` only preserves duplicates when the workspace can't be identified. Agent Identity, Grok, and OpenAI-Responses accounts are excluded from the workspace migration.

### Fixes

- **Credentials-wrapped Agent Identity `auth.json` now imports (#424).** Account exports from sub2api / axisrelay store the Agent Identity fields flat inside a `credentials` object rather than under an `agent_identity` sub-object, so the import dialog rejected them with "not Agent Identity format" even though every field was present. The parser now unwraps `credentials` before detection (covering the single paste and the batch file upload), and the generic accounts.json importer synthesizes the identity node from flat credentials instead of silently dropping the entry.
- **Grok account proxy edits persist to the database.** `UpdateGrokAccount` only wrote the credentials JSON, but `proxy_url` is a dedicated column — so editing a Grok account's proxy only patched the in-memory store and silently reverted on reload/restart. The edit now persists `proxy_url` (empty clears it).
- **Quota is charged even when usage logging is disabled (#428).** Turning off usage logs no longer skips quota accounting.
- **Streamed tool calls are cached for continuation (#427).** Tool-call output items from a streamed response are now cached so `previous_response_id` continuation replays them correctly.
- **A deleted Codex Agent runtime marks the account banned (#426).** When the upstream reports the agent runtime no longer exists, the Agent Identity account is banned rather than retried into repeated failures.
- **Pooled WebSocket connections survive upstream trailing metadata frames.** A `responsesapi.websocket_timing` trailing frame no longer kills the pooled connection every turn (the first-token regression root cause); idle-frame adjudication keeps the connection alive.
- **Prompt Filter trigger-log badges no longer overflow into the endpoint column.** In the fixed-layout trigger log, a long origin label ("current user prompt") rendered at its natural width and overlapped the adjacent model text; the action-column badges now wrap within their cell.
- **Prompt Guard background database work drains safely on shutdown (#430, by @ifThink404).** The async audit / probe / intelligence tasks introduced with the prompt guard are now lifecycle-managed: on shutdown the database stops accepting new detached work, waits out a grace period, then cancels and waits for every registered task to exit before closing the connection, so no background writer can outlive the SQL connection. This closes the async-write and temp-SQLite-cleanup races that surfaced as flaky CI after #429, and also brings account import/refresh probes, Grok usage/status probes, rule intelligence, CLI-version sync, and model-manifest learning under the same drain discipline.

## v2.5.9 - 2026-07-22

### Features

- **Grok is now a first-class upstream provider (accounts, scheduling, billing, per-key routing).** Alongside Codex/OpenAI, the pool can hold **Grok** accounts and route `/v1/responses`-shaped traffic to xAI. Accounts import three ways — Device Code OAuth (RFC 8628, the same flow the official Grok CLI uses), `auth.json` paste, and raw `xai-…` API key — plus batch file/`sso.txt`/`refreshtoken.txt` importers and an **SSO→Build converter** that drives the xAI device flow with a `grok.com` `sso` cookie to mint an OAuth account with no manual authorization. A dedicated `/accounts/grok` page (table + card layouts) shows a full billing view (per-product usage, on-demand cap, monthly spend). API keys gain a first-class **upstream channel** field (`auto`/`codex`/`grok`) that hard-gates which account pool serves the key; the Dashboard and Usage pages get an All/Codex/Grok segmented filter (backed by a new `usage_logs.channel` column + backfill migration), and the model dropdowns follow the selected channel (admin `/models` now returns `grok_models`, the union of Grok accounts' declared models). Grok accounts persist their weekly/monthly usage across restarts, carry their own error/cooldown mapping (free-quota exhaustion, spend limits, `Retry-After`), and never run the ChatGPT wham/`/responses` probes that would mis-ban them.
- **Codex Agent Identity auth import — sign-per-request accounts with no stored OAuth token.** A new "Agent Identity" import method accepts a Codex `auth.json` in `auth_mode=agentIdentity` form: the account stores only an Ed25519 (PKCS#8) agent private key + `agent_runtime_id`, never an access/refresh token, and every upstream request is dynamically signed. The gateway registers a task with `auth.openai.com/api/accounts/…/task/register` (decrypting the returned task id via X25519/NaCl anonymous box when the endpoint encrypts it), caches and persists the task id, sends `Authorization: AgentAssertion <signed-envelope>` in place of the bearer token, and re-registers + retries once on a `401 invalid_task_id`. These accounts run over HTTP (their per-request signing model doesn't fit the long-lived WS handshake), show a blue "Agent Identity" badge in the account list, and populate their usage bars after import via a signed `/responses` probe. Import works as a single paste, a multi-file batch, or by auto-detection inside the general CLIProxyAPI/Sub2Api JSON import and folder scan (dedup by `agent_runtime_id`); `auth_mode` is accepted in both `agent_identity` and `agentIdentity` spellings.
- **Context-window overflow auto-compaction (#415).** When an upstream turn overflows the model's context window, the gateway can now transparently compact the conversation and retry instead of surfacing the error — an experimental global setting with a per-key switch and a configurable summary retry, including a fix for the loose-mode gap (#310) and the pre-token frame-buffering pitfall.
- **WebSocket busy-session overflow to sibling connections (#413).** A busy WS session now escalates through a tunable ladder — patience wait → spill to a sibling connection → concurrency cap — instead of stalling, gated behind an env valve (default off) and wired through the full settings pipeline and UI; a new `ws_acquire_ms` segment records connection-acquire time in usage logs.
- **wham credits balance and applicable reset credits surfaced.** The wham usage probe now threads the account's `credits.balance` (previously dropped by `ApplyWhamUsage`) and the `applicable_available_count` reset-credit count through to the account view.

### Fixes

- **The `/v1/messages` Claude→Codex model mapping keys now appear in `/v1/models`.** The global Claude model mapping (used to translate `claude-*` model names to Codex models on the Anthropic endpoint) was never added to the served model list, so pulling `/v1/models` returned no `claude-*` entries and downstream clients couldn't discover them. Every non-wildcard mapping "from" key is now included in the model list.
- **Tool call / output `call_id` pairing repaired on the input path (#414).** Clients that trim conversation history (e.g. Copilot) could drop a `function_call` while keeping its output, producing an unpaired `call_id` that the upstream rejected with 400. A bidirectional, stateless repair reconciles the pairing before dispatch.
- **`/v1/messages` alias-injected `reasoning_effort` stripped, and pre-token upstream failures no longer masked (#412).** `applyMessagesModelMapping` now removes the top-level `reasoning_effort` field the alias injection wrote (it 400s on the Responses upstream), and a failure before the first token is surfaced with its real status code instead of being swallowed by the retry path.
- **Large-body input validation no longer O(N²) (#417).** The prompt filter early-exits when disabled and function-name validation is now lazy; input validation on large request bodies is ~5× faster on the benchmark.
- **Bump `golang.org/x/text` to v0.39.0 (GO-2026-5970).** Fixes a reachable "infinite loop on invalid input" advisory in the previously-pinned v0.38.0, called via the uTLS HTTP/2 transport and the prompt-filter text normalization.

## v2.5.8 - 2026-07-19

### Features

- **Per-key image-tool policy gains a third mode: strip-and-forward (#411).** Beyond the existing allow (default) and block (403 on any image-generation touch), an API key can now be set to **strip**: the gateway removes the Codex image-capability declarations from a `/v1/responses` request and forwards it as a plain text request instead of rejecting it. Stripping covers the flat `{"type":"image_generation"}` tool and the namespace form `{"type":"namespace","name":"image_gen"}` in the top-level `tools[]`, the Responses Lite embedded `input[].additional_tools.tools[]` (an emptied carrier item is removed entirely), a `tool_choice` explicitly pointing at `image_generation`/`image_gen` (`"auto"` and other selections are untouched), and the gateway's own auto-injected image tool + bridge instructions (the strip runs after body preparation, so injection is naturally suppressed). All other tools — shell, function, apply_patch, MCP, web search — survive. This serves upstreams (relay channels, account groups, plans) that reject any request merely *declaring* image capability with `403 Image generation is not enabled for this group`, even when the turn is pure text/code: such pools can now keep serving text traffic without a client-side rewriting proxy. The config is `image_generation_policy` (`allow`/`strip`/`block`) on the key's limits, edited as a three-way select in the key editor; the legacy `disable_image_generation` boolean maps to `block`, so existing configurations keep their exact behavior. Standalone `/v1/images/*` endpoints and image-only models remain governed by block. The strip applies uniformly across the HTTP, WebSocket, and compact `/v1/responses` paths; the pre-existing WS anti-stall strip (issue #220) now reuses the same, more complete implementation.
- **Payload rules can match the actually-scheduled account — pro keys backfilling into a plus group no longer force-fast their own pro accounts (#410).** The existing `group_ids`/`group_names` identity gates match the account groups a key is *allowed* to use, which is independent of where a given request is actually routed — so a pro key allowed `[pro, plus]` with a tier-override rule hit the gate on every request, tagging pro-account traffic with `service_tier` overrides meant only for the plus fallback. Rules gain three new gates matched against the account actually selected for the current attempt: `account_group_ids`, `account_group_names`, and `account_plans` (plan values as in the key plan filter: free/plus/pro/prolite/team/k12/go; all three wildcard lists with the usual OR-within/AND-across semantics). The rule identity is derived per attempt after scheduling picks the account (a copy — the base identity is shared across attempts), so a retry that switches accounts re-evaluates the gates against the new account, and the usage log's requested-tier attribution flips with it; a rule setting any `account_*` gate fails closed on paths where no account is resolved. The rule editor exposes the three gates in both the template and custom forms, relabels the old gates as "key-allowed groups" to make the two semantics unmistakable, and — the actual trap behind the issue — the "Fast Group Boost" template now writes `account_group_names` instead of `group_names`, matching what its description always claimed ("requests served by accounts in this group"); re-save an existing template rule to adopt the new semantics. Verified live: with the gate set to the serving account's plan the override lands (`requested_tier=priority`), with a non-matching gate it doesn't.

### Fixes

- **`system`-role messages on the native Responses path are rewritten to `developer` — SDKs no longer 400 with "System messages are not allowed" (#409).** The Codex upstream accepts only the `developer` role for system-style messages. The chat-completions and Anthropic translation paths have always converted `system` → `developer`, but the native `/v1/responses` passthrough forwarded `input[]` items verbatim, so any SDK that builds Responses requests with a `{"role":"system"}` message got the upstream's `invalid_request_error` back. The shared body-preparation pipeline now rewrites the role on message items (both the typed `{"type":"message","role":"system"}` form and bare `{"role":"system"}` items), preserving content untouched; it runs after `previous_response_id` cache expansion (so replayed history is covered too) and before content-part normalization, and therefore applies uniformly to the HTTP, WebSocket, and compact entrypoints. The OpenAI-relay path is deliberately left alone — the official Responses API accepts `system`. Verified live against the real upstream on `gpt-5.4-mini` and `gpt-5.6-sol`, streaming and non-streaming, typed and bare forms: requests complete and the system instruction demonstrably steers the reply.

## v2.5.7 - 2026-07-18

### Features

- **Client and upstream user-agent auditing in usage logs (#403, by @Torrekie).** Each request now records both the inbound client user-agent and the outbound user-agent actually sent upstream, threaded through the HTTP and WebSocket-relay executors and the upstream drain path, so operators can see which Codex CLI / Desktop / SDK version a request came from and what UA the proxy presented to OpenAI. The Usage page surfaces both, with PG and SQLite persistence and migrations.
- **Request-prep phase timings for `/v1/responses` (#405).** The stretch between the handler entry and the first upstream attempt is now instrumented and emitted two ways: an `X-AxisRelay-Phase-Timing` response header (written before the first SSE byte, so it's visible in a packet capture) and a structured `[TIMING]` log line. Segments are `mw` / `read` / `validate` / `prepare` / `schedule` plus `body_kb`; the existing `first_token_ms` picks up from the attempt onward. This closes the attribution gap where a gateway saw a slow first token but axisrelay's `first_token_ms` looked fast. The companion "native fast-path" idea in the issue was measured and dropped — `PrepareResponsesBodyForOwner()`'s full `Unmarshal → map → Marshal` costs only 1–13ms even on 400KB bodies, not worth the regression surface; `validate` is the real prep-phase cost (grows linearly with body size) and is the right target if the prep phase is ever optimized.
- **Self-learning size-based transport router for oversized requests (#404).** Instead of the incremental-WS-continuation the issue proposed (dropped — official Codex CLI packet captures show every turn sends the full input with `store:false` and `previous_response_id:null`, so upstream WS continuation under `store=false` is unverified with counter-evidence, and `response.id` is account-bound which fights pool scheduling/affinity), a `websocketSizeRouter` (`proxy/ws_size_router.go`) records the smallest request-body size seen at each close-1009 (only learning from ≥64KB samples to avoid misclassification and test pollution) and dispatches subsequent requests at ≥95% of that threshold straight to the HTTP upstream — eliminating the "wait for the WS attempt to fail, then serialize HTTP" first-token tail. The learned threshold recalibrates on a 6h TTL; all four 1009 trigger points across both handlers feed the learning. Gated behind a `codex_ws_size_router_enabled` system setting (default on, wired DB→Store→runtime→admin API→Settings UI) with a `CODEX_WS_SIZE_ROUTER=off` escape hatch.

### Fixes

- **Responses Lite protocol marker is preserved across transports — GPT-5.6 requests no longer 400 with `Unknown parameter: 'input[N].namespace'` (#406).** GPT-5.6 (Sol / Terra / Luna) run in Responses Lite, where history items may carry an optional `namespace`. The gateway wasn't carrying the lite signal to the upstream, so the upstream parsed the request as ordinary Responses and rejected the `namespace` field. The HTTP header allowlist now passes `x-openai-internal-codex-responses-lite`, and the WS path writes it into `client_metadata` (`ws_request_header_x_openai_internal_codex_responses_lite`), so the marker survives on both transports (`bc9b78a`). Separately, lite constraints are now normalized before upstream dispatch (`9501dbb`): `reasoning.context` forced to `all_turns` and `parallel_tool_calls` to `false` on both transports, and on the HTTP transport the hosted `image_generation` tool and its bridge text are stripped (WS keeps them so the image bridge can still fall back) — the auto-injected image tool otherwise made every lite request over HTTP 400. Verified live against the real upstream over forced-WS.
- **Payload-rule editor round-trip and `service_tier` override sanitization (#395).** The visual rule editor's edit/delete buttons were `opacity-0` hover-only (invisible on touch) and `formStateFromEntry` silently dropped headers / `not_match` / `exist` / `not_exist` on save; buttons now hide only under `hover:hover` and a `carry` field round-trips the unexposed conditions, with simple rules routed back to their semantic template on edit. The append-prompt / override-prompt / service-tier / effort-map templates now expose the group-name/-id and key-name gates added in #398. And a rule overriding `service_tier` to `flex`/`auto` no longer 400s upstream: handler-layer sanitization (which only lets `priority`/`fast` through) ran *before* payload rules applied, so a rule-injected tier bypassed it — `sanitizeServiceTierForUpstream` now re-runs after rules apply, and the editor's tier input is a whitelist Select.

## v2.5.6 - 2026-07-16

### Features

- **Payload rewrite rules can now target by API key and account group, and usage stats bill the post-rewrite `service_tier` (#395).** The payload rule engine previously matched only on model and inbound headers, so operators who split subscription tiers into account groups and hand out different API keys per group had no way to scope a rule to a key — e.g. "requests using the *fast* key get `service_tier: priority`". Each rule gains four new match gates — `api_key_ids` / `api_key_names` / `group_ids` / `group_names` — all wildcard lists with the same OR-within / AND-across semantics as the existing `models` gate (IDs are matched as stringified values, so `fast*` works on names and `3*` on ids). The request identity (key id/name + the key's allowed account-group ids/names, group names resolved from a new in-memory `Store` cache kept in sync on group create/rename/delete) rides the upstream `context.Context` into `ExecuteRequest` — no signature churn on the WS/relay path — and a rule that sets any identity gate fails closed when the request has no resolved identity (internal / unauthenticated paths). Separately, the usage log now attributes `service_tier` from the body *after* payload rewrite: the four Codex-path handlers (responses / messages / chat-completions / WS) recompute the requested tier via `EffectiveRequestedServiceTier` per attempt before sending, so a rule that overrides `service_tier` is reflected in `RequestedServiceTier` / `BillingServiceTier` (the "requested" billing policy) instead of logging the client's pre-override value. Compact/relay paths keep their existing behavior (they don't apply payload rules). A one-click "fast group" template is added to the rule editor.
- **Self-service account submission portal (#393).** A standalone public page `/account-portal` lets external contributors add their own ChatGPT/Codex OAuth account to the pool without going through an admin: enter a contact email → generate a ChatGPT authorization link → sign in → paste the redirected URL back (the portal parses `code`/`state` client-side, since OpenAI only registers the `localhost:1455` callback that a remote user's browser can't deliver to the server) → the account lands in a **pending-review** state. Submitted accounts are stored with `enabled=false` + a `self-service` tag and are **not** added to the scheduling pool; an admin approves (enable) or rejects (delete) them from a new "pending self-service" review panel on the Accounts page. Approving an account that was never loaded into the runtime pool now pulls it into the scheduler on the spot. The whole flow is gated behind a `public_account_portal_page_enabled` toggle (default off; page and API both 404 when disabled), protected by per-IP rate limiting and email-format validation (no CAPTCHA this round), and never echoes tokens back to the submitter. Accounts also gain a general-purpose `note` field (admin-editable, `PATCH /accounts/:id/note`) that the portal populates with the submitter's contact email as an identifier.

### Fixes

- **Upstream account 403s now rotate to another account instead of being passed straight back to the client (#396).** A Codex upstream 403 is always account-side (`payment_required` / `deactivated_workspace` / `codex_access_restricted` — OAuth / plan / workspace scope, never request-content), and the offending account was already cooled down and hard-excluded on 403 — but 403 was missing from `isRetryableStatus`, so the request stopped immediately and surfaced the raw 403, and clients like Claude Code treated it as their own permission failure and quit, even when the pool still held healthy accounts. 403 is now retryable (bounded by `max_retries`), so the request switches accounts across every endpoint; once retries are exhausted the final error is rewritten to a pool-level `503 account_pool_forbidden` (mirroring the existing 401→503 handling) rather than transparently forwarding the 403. Account cooldown/error semantics for 403 are unchanged.

## v2.5.5 - 2026-07-15

### Features

- **Payload rewrite rules engine, with a dedicated management page (#321/#390/#391).** A declarative rule engine lets the admin rewrite the upstream-bound request body — append or override `instructions` (the system prompt), override `service_tier`, conditionally map reasoning effort (e.g. `medium → high`) — without hanging another rewriting proxy in the chain. Six rule groups (`default`/`default_raw` write-if-missing, `override`/`override_raw` unconditional, `append` string-append, `filter` field-delete) are gated by match conditions that must all hold: model wildcards, header wildcards, JSON-path equality (`match`/`not_match`) and path existence (`exist`/`not_exist`). Rules apply once at the top of `ExecuteRequest` — before the WS/HTTP fork — so all four inbound protocols (responses / chat / anthropic / client WS) are covered uniformly; image-generation requests are skipped to protect the image bridge's constructed `instructions`. A protected-field blacklist (`model`/`stream`/`input`/`store`/`prompt_cache_key`/`type`) rejects rewrites that would desync scheduling, billing, or session isolation (model rewriting stays with the existing model-mapping feature). Rules persist in system settings (PG + SQLite), hot-apply on save with 400 on invalid JSON or protected fields, and `GET /api/admin/settings/observed-instructions` records the most recent client-passthrough `instructions` as a reference sample. The editor lives on a new sidebar page `/payload-rules` with a full visual mode — stat tiles, per-group color-coded rule cards with match-gate chips, one-click templates for the four typical rules, a custom-rule form with a model multi-select (portal-positioned dropdown with search, wildcard input, keyboard navigation) — two-way synced with a JSON source mode. The UI warns that overriding `instructions` replaces the official Codex CLI prompt (which can affect its tool-calling behavior) and invalidates the upstream prompt cache, so rule text should stay stable.
- **Configurable prompt-filter hardening and NewAPI audit integration (#389, by @ifThink404).** The prompt filter gains strict terminal rules, text normalization, composite matching, cumulative risk scoring, input *and* output stream scanning across the chat / responses / Anthropic paths (SSE and WebSocket alike, with correct output buffering and fragmented-event scanning), and a prompt-intelligence history with hardened auto-add validation. A NewAPI-facing audit channel persists configurable audit secrets and policy settings, verifies signed user/IP headers (the V1 signature covers method, path, and a body digest, with replay protection), and enforces repeat-offender bans using per-key cache leases for risk and offense counters. The raw advanced-JSON controls are replaced by a localized admin UI with secret generation/replacement flows, rule comparison/history, a regex preview dialog, in-app usage guides for both Prompt Filter and Payload Rewrite, and Nginx buffering guidance; settings and secret updates are atomic, and strict-keyword false positives are corrected.
- **Public Image Studio portal for API-key users (#369).** A standalone `/image-studio` UI backed by `/api/image-studio` endpoints lets teammates generate and edit images with their own API key, without admin-console access — per-key isolation, a gallery reference picker, and a polished studio layout.
- **Dashboard pool runway (#383).** Turns the existing limit-pressure forecast into an ops-facing runway metric (~N hours / 1 day+ / under 1h) with a dashboard card, action hints, and a show/hide toggle (local preference). Forecast math lives in `frontend/src/lib/poolRunway.ts` (real long-window length, skip absent 5h samples); the accounts recovery panel reuses the same runway copy.

### Fixes

- **Inbound request bodies are decompressed per `Content-Encoding` — zstd bodies from new Codex clients parse correctly (#373/#388).** Codex Desktop/CLI 0.144.2+ under a `requires_openai_auth` + bearer-token configuration compresses the `/v1/responses` body with zstd; the gateway never decompressed inbound bodies, so the compressed bytes were parsed as plaintext JSON — a large body failed local validation with "Field 'model' is required", while a small one could slip past validation on literal residue inside the zstd frame and leak compressed bytes upstream ("The 'None' model is not supported"). A new `RequestBodyDecompressor` middleware (after `RequestSizeLimiter`, before `BodyCacheMiddleware`) decodes zstd / gzip / x-gzip / br / deflate (zlib-wrapped with a raw-flate fallback), unwinds stacked encodings in RFC 9110 reverse order, and rewrites the raw-body cache, `c.Request.Body`, and `Content-Length` while stripping `Content-Encoding` so all downstream validation and forwarding sees plaintext. A decompressed size beyond `MaxRequestBodySize` returns 413 (decompression-bomb protection), a corrupt stream returns 400, and unknown encodings pass through untouched.
- **Compaction turns are exempt from the first-token watchdog (#381/#387).** A compaction round must ingest the entire history before emitting its first compaction output item, so its first frame can legitimately exceed a short configured first-token timeout (e.g. 30s) — and because that round's `encrypted_content` is bound to the original account, the timeout-triggered cross-account retry was near-certain to keep failing until attempts were exhausted and the session was dead. `firstTokenTimeoutForRequest` now returns 0 (watchdog off) when the request body carries `compaction_trigger`, wired into the Responses relay and Codex paths plus the inbound WS path; ordinary requests keep the configured threshold, and a genuinely hung turn is still bounded by the client's own timeout.
- **The 5h usage window is treated as optional (#382/#384).** Some accounts' upstream usage reports only the 7d window, but the gateway assumed 5h always exists. Stale 5h state is now cleared only after authoritative 7d-only observations, while empty or partial responses preserve existing 5h snapshots and unrelated unauthorized / error / disabled / newer rate-limit states; cooldown and source ordering stay safe across the WHAM, Responses, Anthropic, images, persistence, and runtime-cache paths. Status badges and the account detail sheet name the specific affected window, and the UI hides an absent 5h usage/cost instead of showing misleading placeholders.
- **HTTP/2 upstream connections send keep-alive PINGs so dead connections are evicted.** Direct Codex/OpenAI HTTP connections negotiate HTTP/2 via ALPN, but `http2.Transport` defaults to `ReadIdleTimeout=0` (no keepalive PING), so a connection silently severed by an intermediate proxy/NAT looked alive on both ends — a request landing on it hung until the OS TCP retransmission timeout (minutes), surfacing as extreme first-token latency. Both HTTP/2 direct transports (the standard one via `http2.ConfigureTransports`, and the uTLS-fingerprint one's hand-built `http2.Transport`) now set `ReadIdleTimeout=15s` / `PingTimeout=15s`. WebSocket relays already have full Ping/Pong keepalive and are unaffected.
- **Image-edit reference-image cap relaxed from 10 to 16 (#275).** The 10 was a defensive value from an earlier review pass, unrelated to upstream capability; the official Images API allows 16 reference images for gpt-image edits, and this proxy's responses image channel was verified to accept 16 `input_image` items end-to-end. Multipart, JSON, and Image Studio all reference the same constant, so the relaxation applies everywhere at once.

## v2.5.4 - 2026-07-14

### Fixes

- **WebSocket handshake 401s are restored to their real status code — visible in usage logs and triggering account cooldown (#378).** In forced-WS deployments an upstream 401 was completely invisible: a handshake-stage HTTP 401 was wrapped into a plain-text transport error, so usage logs only ever showed 598/transport (or the failure was masked by a silent retry), the account never received an unauthorized cooldown nor an auth probe, and a dead-token account stayed in the scheduling pool being redialed forever — the only place a 401 could surface was the HTTP-only `/v1/responses/compact` endpoint, producing the misleading "401s all concentrate on compact" pattern. The dial error now carries a structured `HandshakeHTTPError` (status code / headers / normalized JSON body; `Error()` text unchanged so connection-test consumers are unaffected), and the WS executor converts a handshake 401 into a real-status HTTP response so the existing non-2xx path takes over: the usage log records 401 with the upstream error message, `applyCooldownForModel` applies the unauthorized cooldown (`missing_scope` carve-out included), and retry moves to the next account. Non-401 handshake statuses (403/429/503) deliberately keep their transport semantics — 503 handshake rate-limiting still uses sticky retry. Mid-connection close-1008 auth death keeps its existing async `VerifyAccountAuthAsync` probe.
- **Newer Responses 429 cooldowns stay authoritative over stale in-flight successes (#376, by @Torrekie).** Under `ignore_usage_limit_status` (#343), a completed Responses success clears a usage/rate-limit cooldown — but a turn accepted *before* the quota boundary could finish streaming *after* another request had already observed `429 usage_limit_reached`, and its stale completion unconditionally reopened the account that newer upstream evidence had just limited. Each attempt's start time is now threaded through the direct relay, Codex HTTP/upstream-WS, and downstream-WS paths, and `ConfirmResponsesAvailableSince` only clears the cooldown when the successful request started after `LastRateLimitedAt`; an in-flight Working response still finishes on its account, and a genuinely-after-the-limit success still restores availability.
- **Settings/scheduling batch fixes (#374/#377).** Numeric settings inputs switch to a draft-input component so intermediate states aren't force-normalized by `parseInt || 1` while typing; the WS connection pool converges idle connections to each account's dynamic concurrency limit with per-account serialized dialing (many short sessions no longer each strand an idle connection); the one-shot WS→HTTP downgrade on close 1009 retains the already-acquired account lease instead of releasing it and re-running the scheduler; CORS allows the `X-AxisRelay-Affinity-Key` header; `GET /api/admin/settings` returns `RetryIntervalMS`/`TransportRetryPolicy`.
- **`/models/sync` honors the global ProxyURL (#371/#379).** `SyncOfficialCodexModels` used a bare `http.Client` and always dialed the official models page directly, ignoring the configured global proxy — the only upstream-facing link that did. It now takes a `proxyURL` and reuses `newCodexStandardTransport` (same fall-back-to-direct-on-bad-proxy semantics as the CLI-version and pricing syncs), with the admin handler passing `store.GetProxyURL()`; a fake-proxy CONNECT test proves the traffic actually routes through the proxy.
- **Account-maintenance requests route through Resin (#372/#380).** With Resin configured, AI requests went through the reverse proxy but account upkeep didn't: the wham usage query, reset-credit list, reset-credit consume, and the subscription-expiry query (#360) all dialed chatgpt.com directly — every account sharing the host's egress IP. A shared `resinMaintenanceTarget` helper now applies the same trio the executor mainline uses (reverse-proxy URL rewrite, per-account Resin client pool, `X-Resin-Account` header) to all four call sites; with Resin disabled, behavior is byte-identical (wham keeps the uTLS-fingerprint transport, the subscriptions endpoint keeps its uTLS Chrome fingerprint).

## v2.5.3 - 2026-07-12

### Features

- **Per-group base concurrency and account scheduler-priority sorting (#363/#364).** Account groups gain a **per-account base concurrency** override: set once on the group and every member inherits it (an account belonging to multiple groups takes the smallest positive value), with an explicit account-level override still winning and unset falling back to the global setting before the health-tier dynamic shrink applies. The value is edited inline on the group and hot-applied without a restart; FastScheduler recomputes affected members immediately. The account list adds a **scheduler-priority column and sort** (click to toggle asc/desc) so operators can see and order accounts by the priority introduced in v2.5.2, plus a quick "set groups" action that replaces an account's group membership wholesale (selecting none clears it) without touching its other settings.
- **Automatically consume expiring reset credits (#367).** OpenAI grants accounts a limited number of manual rate-limit reset credits that themselves expire; a credit left unused past its expiry is simply lost. A new opt-in background sweep (default off) scans schedulable paid accounts every 5 minutes and, when a credit is within a configurable lead window of expiring (Settings → lead minutes, default 60), consumes it via `wham/rate-limit-reset-credits/consume` so the reset actually lands before the credit evaporates. Consumption is per-account locked and deduplicated by handled-credit id (retained 8 days) so the same credit is never double-spent across scans, and it respects the existing reset-credit cooldown. Toggling the switch or changing the lead time wakes the loop to rescan immediately.

### Fixes

- **Renewed accounts no longer show "subscription expired", and the authoritative expiry is synced from upstream (#360).** Subscription expiry was sourced solely from the JWT `chatgpt_subscription_active_until` claim, which stays stuck at the pre-renewal value long after a user renews; `wham/usage` was measured to carry no subscription-expiry field at all, so the refresh-time sync added in #300 never actually did anything. Two layers fix it. (1) **Stale-value cleanup:** an authoritative paid `plan_type` (a truly-expired plan degrades to `free` upstream) is mutually exclusive with "subscription expired", so a past expiry under a paid plan is treated as a stale leftover and cleared — uniformly across the wham probe, the `/responses` `x-codex-plan-type` header sync, the RT refresh path, and the import seed, all of which now also refuse to write such a stale value back; a genuinely expired `free` account keeps its expiry for display. (2) **Authoritative sync:** a new best-effort path fetches the real `active_until` from the ChatGPT web endpoint `/backend-api/subscriptions` using a uTLS Chrome fingerprint + browser headers (a plain TLS fingerprint is blocked by Cloudflare), triggered after a successful wham probe only when a paid account's expiry is missing / past / within 7 days, throttled to once per account per 6h; when `account_id` holds a polluted `user-...` value it falls back to the workspace UUID from the access-token JWT. Also completes the token-refresh fallback so a subscription field missing from the `id_token` is recovered from the `access_token`.
- **Relay compaction triggers stay on the SSE path (#361).** `stream=true` `compaction_trigger` requests on relay-only pools are kept on the Responses SSE path (rather than being diverted to the non-stream compact endpoint), while compact-aware global and account model mappings are retained; the existing non-stream compact-endpoint fallback is preserved, with protocol regression coverage added.

## v2.5.2 - 2026-07-11

### Features

- **Per-account scheduling priority (#358).** Each account gains a scheduler priority (-100 to 100, default 0), editable in the account scheduler drawer or via `PATCH /accounts/:id/scheduler` (`scheduler_priority`, null resets; hot-applied without restart). Higher-priority accounts are scheduled **strictly first** — lower priorities only serve when every higher-priority account is unavailable (cooldown, paused, concurrency-saturated) — while accounts at the same priority keep competing on the existing health-tier + dispatch-score rules. Typical use: give official OAuth accounts a positive priority (or relay/API-key channels a negative one) so relays act purely as fallback, i.e. "use up the official accounts before touching the relay". The value is stored in the account credentials JSON (no schema migration); FastScheduler mode gets the same semantics via priority-descending segments inside each health bucket (round-robin preserved within a segment). Default 0 keeps existing deployments byte-identical.
- **Codex CLI standalone web search passthrough — `POST /v1/alpha/search` (#359).** Newer Codex CLI (e.g. 0.144.1) with `web_search = "live"` (or `--search`) calls a dedicated search endpoint instead of the Responses hosted tool; the gateway previously had no such route and the CLI's built-in web search 404'd. The endpoint (registered under `/v1`, prefix-less, and `/backend-api/codex` alike) now forwards to the ChatGPT backend's standalone search using a schedulable ChatGPT OAuth account's credentials — request body and response (including upstream 4xx/5xx status codes) pass through verbatim so the CLI sees real error semantics, UA/Version follow the same outbound-client-header resolution as `/responses`, and the same uTLS transport is used. Search is a model-driven retrieval turn, so the upstream timeout is 120s. Relay-only pools fast-fail with 503 (the endpoint only exists on the ChatGPT backend). Verified live against the real upstream with the codex-rs `SearchCommands` request shape (`commands.search_query[].q`): 200 with full `output` + `encrypted_output`.

### Fixes

- **Anthropic path preserves `max` reasoning effort for gpt-5.6 mappings (#357).** Translating Anthropic `/v1/messages` `output_config.effort` into Responses `reasoning.effort` used the model-agnostic normalizer, which unconditionally clamps `max` to `xhigh` — so Claude Code requests mapped to `gpt-5.6-sol` etc. lost `max`. The translation now passes the mapped Codex model into the same model-aware normalizer the `/v1/responses` path uses: gpt-5.6+ keeps `max`, older models still clamp to `xhigh`, all other efforts unchanged.

## v2.5.1 - 2026-07-11

### Fixes

- **Compact model-mapping regression: synthesized aliases no longer hijack generic wildcard rules, and mapping targets are stripped of `-openai-compact` before reaching the upstream (#350 regression).** v2.5.0's compact mapping resolves the endpoint-qualified `<model>-openai-compact` alias before the base model, but the alias *synthesized* for clients that sent a plain base name participated in all rule matching, with three fallouts. (1) Rules keyed on the suffixed name — which operators had copied from relay-side compact route markers (e.g. `gpt-5.6-sol-openai-compact` in a channel's model list) and which were dead letters before v2.5.0 because the suffix was stripped before matching — suddenly activated and forwarded the suffixed name verbatim upstream; relays fail to resolve it as a literal model, and compaction died with `stream disconnected before completion: stream closed before response.completed`. (2) Generic wildcard rules (`gpt-*`, `*`) matched the synthesized alias and outranked the base model's exact rule. (3) An identity suffixed→suffixed rule could knock an account that only lists the suffixed marker out of the compact pool entirely (503 no available account). Now a synthesized alias only matches rules explicitly keyed with the `-openai-compact` suffix (including `*-openai-compact` wildcards); any matched mapping target has the suffix stripped before entering the upstream body — the upstream call is always `/v1/responses/compact`, where the alias convention never applies; and the compact account filter and per-attempt rewrite share the same resolver. Verified via an A/B scenario matrix against the pre-#350 build: every configuration is restored to its pre-v2.5.0 upstream behavior, while v2.5.0's intended feature — suffixed-key rules that apply only to compaction — keeps working. Usage stats also now display the base model for suffixed requests: a bare suffix strip no longer renders as a mapping arrow (`gpt-5.6-sol-openai-compact → gpt-5.6-sol` simply shows as `gpt-5.6-sol`), which also keeps pricing lookups on the clean base name.

## v2.5.0 - 2026-07-11

### Features

- **Model pricing management promoted to a dedicated page, with models.dev sync.** Per-model billing prices used to be buried in a settings drawer; they now live on their own **Model Pricing** sidebar page. Each model's input / cached / output / priority / long-context unit prices can be custom-overridden or synced from a JSON source, with priority `custom > synced > code default` (an unset field falls back to the code default, so partial overrides work). The sync source **auto-detects two formats** — the project's flat `pricing.json` and models.dev's `api.json` (`provider → models → cost`, mapping the 272K long-context tier to the long-context fields; only the `openai` provider is read, and an alias whose canonical key collides with another model never overwrites the exact entry). Preset buttons switch between the project default source and models.dev in one click; a fake/garbage sync source is never written. The pricing list is ordered newest-model-first, with the gpt-5.6 sol/terra/luna trio pinned to the top and terra priced independently.
- **Per-API-key "Disable image generation" switch (#356).** A per-key toggle (off by default) that blocks image generation for that key: image-only models (`gpt-image-*`), the `/v1/images/generations` and `/v1/images/edits` endpoints, and any request carrying or requesting the `image_generation` tool all return 403. This lets a key that never wants images avoid policy-violation bans and image-data bandwidth, without touching keys that do.
- **Recycle bin K12 filter and credential batch export.** The account recycle bin gains a K12 plan filter and can export the selected or currently listed soft-deleted accounts as CPA-format JSON and an email TXT list via a new `POST /accounts/recycle-bin/export` endpoint.
- **DeviceCheck attestation header passthrough (#355).** Upstream `openai/codex` added an `x-oai-attestation` (Apple DeviceCheck) header on the ChatGPT Codex request paths, generated by the signed macOS desktop app and verified server-side against Apple. It is currently a soft signal — plain CLI / non-macOS clients legitimately never send it — so the proxy takes the only correct stance: forward the header verbatim when a downstream real Codex client supplies it (on both the HTTP and WebSocket OAuth Codex paths, never on the API-key relay path), and **never fabricate it** (a forged token fails Apple verification and is a worse fingerprint than absence). Operators with a real token source can still inject it per account via the existing custom-header mechanism. The plumbing is ready should the upstream ever make attestation mandatory.
- **Built-in model list retires sub-5.3 models.** Models below 5.3 are removed from the built-in list (keeping `gpt-5.3-codex-spark`), and the same filter is applied to the upstream manifest passthrough so retired slugs don't reappear.

### Fixes

- **Idle upstream WebSocket connections are no longer misjudged alive — fixing pre-first-token retries, dropped streams, and 598s (#349).** In forced-WS mode a connection returned to the pool had no reader, so gorilla/websocket never processed the upstream's keepalive `Ping`/`Pong`/`Close` control frames while idle. The upstream declared the connection dead (`close 1011 keepalive ping timeout`) and queued a Close frame, but the local state stayed `Connected` and the write-only Ping probe still "succeeded" — so the dead connection was handed to the next request, whose first read immediately hit the queued Close. That surfaced as slow first tokens (silent retries), and consecutive dead-connection reuse exhausted retries into `status 598` / "stream disconnected before completion" (609 keepalive-timeouts in a single reported instance). Each physical connection now runs a **permanent read pump** — the sole reader — so control frames are handled in real time (Ping is auto-answered), a queued Close or read error flips connection state immediately, and business frames are delivered to the request consumer through a bounded queue with per-request leases. The liveness probe no longer writes a Ping (which is exactly what produced the false positive): it trusts the pump's real-time state and only checks lease/queue cleanliness, and a connection with recent inbound activity skips the ping-pong roundtrip entirely so a hot reuse doesn't pay an extra upstream RTT. Verified end-to-end (PG+Redis, `gpt-5.6-sol` reasoning `max`, `/v1/responses`) that 75s/150s idle-reuse windows and concurrent load succeed with a stable ~0.3s first event and zero keepalive-timeouts.
- **WebSocket connections rotate at 50 minutes to dodge the upstream 60-minute lifetime cap (#346).** The upstream enforces a 60-minute per-connection lifetime; past it, `response.create` is rejected with `websocket_connection_limit_reached` while the Ping probe keeps succeeding, so age can't be inferred from probing. Connections are now proactively rotated — destroyed while idle — at 50 minutes (10-minute headroom so in-flight streams finish), and the lifetime error frame marks the connection dead so it can't keep poisoning subsequent requests, including continuation-affinity ones.
- **Continue-thinking no longer inflates the context window until it overflows (#353).** The continue-thinking fold stacked **every** round's reasoning (including the account-bound `encrypted_content`) into the folded response's `output` — a single response could carry N reasoning blocks instead of the normal one. A client echoing that N× history back on the next turn overflowed the model's context window (`400 context_length_exceeded` on the first round), and on cross-account replay each of the N encrypted blobs was rejected by the newly scheduled account, spamming the `encrypted_content` strip-and-retry path. The folded downstream output now keeps full `encrypted_content` only for the **final** round and strips it from earlier rounds (their summary text is retained; those rounds' encrypted context already did its job during folding and produced the final answer). The change touches only the client-facing output — the `replayTail` sent upstream for same-account continuation is unchanged, so the folding mechanism itself is unaffected. `CODEX_CONTINUE_KEEP_ALL_ENCRYPTED=1` restores the old behavior. A single response that natively emits multiple reasoning blocks (no folding) keeps all of them.
- **`gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna` are billed at their distinct official prices (#344).** The three gpt-5.6 variants carry different official rates; they were all folding onto gpt-5.4's price. Each now bills separately (sol `$5/$30` with cache-write, terra `$2.5/$15`, luna `$1/$6`), with priority tiers derived as the standard 2× where the upstream doesn't publish an explicit one.
- **Model mapping now applies to remote compaction requests (#350).** Aggregating clients append an `-openai-compact` suffix to compact requests, but the suffix was stripped **before** model mapping ran, so a mapping configured against the compact alias never matched; and body-signal compact promotion mapped the model twice. Compact requests now resolve the endpoint-qualified alias first (falling back to suffix-strip + normal mapping), map exactly once, and account routing accepts the synthesized compact alias so an account isn't wrongly judged unable to serve it. Billing is unaffected (it follows the effective model).
- **Compaction history is distinguished from a compaction trigger (#347).** The body-signal detector scanned `input` recursively and treated durable `compaction` / `context_compaction` history items (which carry `encrypted_content` and belong in later turns) as new compaction triggers — polluting usage stats with `compact=true` and promoting ordinary requests to the compact chain on relay-only pools (returning compaction output instead of a normal message). Only a bare top-level `compaction_trigger` item now counts as a trigger; durable history items ride through the normal `/v1/responses` path.
- **Codex client identity completed for restricted relays (#351).** Some third-party Responses relays that only accept the official Codex client also validate the request body's `client_metadata.x-codex-installation-id`; a UA-only rewrite was rejected with `403 codex_access_restricted`. A per-account `codex_client_metadata_mode` (`auto` / `always` / `off`, default `auto`) now injects a deterministic (UUIDv5, stable across restarts) installation id and retries once, but **only** on that exact `403 + codex_access_restricted`, then remembers the relay's requirement keyed by account + base URL. Default `auto` leaves the first request identical to before, so ordinary Codex accounts and the compact path are unaffected.
- **Responses outcomes can be the authority on account availability (#343).** New opt-in `ignore_usage_limit_status` (global, with per-account override): when enabled, 5h/7d usage percentages become display-only, an account isn't locally cooled down by a usage snapshot alone, and availability is decided by real Responses results — a genuine 429 still cools the account, and a confirmed Responses success clears that usage cooldown (WHAM is demoted to metadata and can't clear it). Accounts that ignore usage limits are also skipped by the full-usage auto-cleaner, so a snapshot that's now merely informational can't soft-delete them.
- **Anthropic empty content-block fields are preserved (#339).** Strict Anthropic-stream clients (e.g. OpenCode) require `content_block.thinking` / `content_block.text` to be a string even when empty, but Go's `omitempty` dropped `thinking: ""` / `text: ""` from `content_block_start`, so the client rejected the stream with `expected string, received undefined` before any delta arrived. A custom marshaler now keeps the empty field for the matching block type while leaving `tool_use` blocks unaffected.
- **Batch account tag/group edit is per-field opt-in (#348).** The batch metadata editor used to union all selected accounts' tags and groups and write them back to every account, so changing one field silently overwrote the other. Each field now has an independent switch: a disabled field keeps each account's current value (it isn't sent), and only an enabled field is uniformly applied (empty clears it). Both switches off disables save.
- **SQLite transactions use immediate locking, eliminating concurrent-write "database is locked" (#353-adjacent).** A deferred transaction takes a read lock first and only upgrades to a write lock at the UPDATE, and that upgrade returns `SQLITE_BUSY` immediately — `busy_timeout` doesn't cover lock upgrades. The DSN now sets `_txlock=immediate` so a write transaction acquires the write lock at `BEGIN` and concurrent writers queue on `busy_timeout` instead of erroring; this also removes a rare `database is locked (517)` flake between an account update and its async event insert.
- **WebSocket bad-handshake errors carry upstream detail.** A failed WS handshake now surfaces the upstream HTTP status, key headers, and raw JSON body instead of a bare `websocket handshake failed: <code>`, so connection-test failures on relay/OAuth accounts are diagnosable.

## v2.4.9 - 2026-07-10

### Fixes

- **Reserved `collaboration.*` tools pass through untouched — gpt-5.6 multi-agent no longer 400s (#342).** GPT-5.6 multi-agent v2 declares reserved tools such as `collaboration.spawn_agent`, whose schema the upstream requires to match its official configuration verbatim. The proxy applied its generic function-tool schema sanitization (`stripUnsupportedSchemaKeys` drops `pattern`/`minItems`/`format`/…, plus `required` normalization and array-items defaults) to *every* function tool, so it mutated the reserved tool's schema too — the upstream then rejected the request with "Invalid Value: 'tools'. Function 'collaboration.spawn_agent' is reserved for use by this model and must match the configured schema", and the stream disconnected before completion; switching to gpt-5.5 (whose client doesn't send collaboration tools) worked around it. A reserved-namespace check (`collaboration.` prefix, matching both flat Responses and nested Chat-Completions tool shapes) now passes such tools through verbatim in both `normalizeResponsesFunctionTools` and the schema-sanitize loop — no flattening, no default descriptions, no schema stripping. It is not model-gated, since the reserved namespace should be preserved for any model. Verified end-to-end that the reserved tool name and structure reach the upstream unchanged; note that acceptance still requires the account to be entitled for multi-agent (unentitled tiers are rejected upstream on the generic tool-name check, independent of this fix).

## v2.4.8 - 2026-07-10

### Features

- **Codex client emulated-version auto-sync from `openai/codex` releases.** When the upstream gates a new model behind a minimum client version (gpt-5.6 requires 0.144+, rejecting older fingerprints with "requires a newer version of Codex"), the only fix used to be bumping the built-in `latestCodexCLIVersion` constant and cutting a release. The emulated version is now fetched from `openai/codex` releases (`releases/latest`, which naturally excludes prereleases) and applied to the outbound UA / manifest `client_version` — no release needed to follow an upstream gate. The runtime uses `max(built-in constant, synced version)`, so a stale or malformed remote value can never downgrade the fingerprint. Settings → "Codex client version sync" gains a **Sync now** button (shows the current emulated version), a **scheduled auto-sync** toggle (default on), and a **configurable interval** (hours, 1–720, default 12); the background loop reads the toggle and interval live so changes apply without a restart. `CODEX_DISABLE_CLI_VERSION_SYNC=1` is a hard off-switch that overrides the toggle. New model gates can now be followed with one click.
- **Reasoning effort `max` passes through for gpt-5.6+.** Verified against the upstream: gpt-5.6 and later accept `reasoning.effort=max` and echo it back, while older models (gpt-5.4/5.5) reject it with "Invalid value: 'max'". Effort normalization is now model-aware — `max` is forwarded as-is for gpt-5.6+ (including `-sol`/`-terra`/`-luna` variants) and clamped to `xhigh` for everything else (and on paths without model context), across the Chat/Responses/relay preprocessors and the settings-configured reasoning-effort model aliases. The frontend effort selector gains a `max` option.

### Fixes

- **Built-in Codex CLI fingerprint bumped `0.142.3` → `0.144.1`, unlocking the gpt-5.6 series.** The upstream's new minimum-client-version gate rejected requests carrying the older emulated version. The built-in floor is raised to the current stable `rust-v0.144.1`; the single constant flows through the UA profile pool, default UA, probes, and manifest `client_version` fallback (the auto-sync feature above keeps it current going forward). Verified: gpt-5.6-sol requests recover to 200.
- **`agent_message` input items no longer 400 (#341).** Newer Codex CLI / Desktop (multi-agent v2) replay `agent_message` history items into `input[]` when continuing a session, which the entry-point validator rejected with "Invalid input type 'agent_message'", stalling the conversation. The upstream accepts the type natively (verified), so it joins the input-type whitelist alongside the earlier `compaction`/`shell_call`/`mcp_call` allowances.
- **`client_metadata` is stripped from compact request bodies (#340).** Newer Codex clients carry a `client_metadata` field (used for engine fingerprint signals) in plain `/v1/responses` bodies, which the normal upstream accepts — but the compact endpoint rejects it with 400 "Unknown parameter: 'client_metadata'". v2.4.7's body-signal compact promotion was the first path to send a plain-request-shaped body to the compact endpoint, exposing this. Both compact preparers (ChatGPT and relay) now strip `client_metadata` alongside `include`/`store`/`stream`; the plain (non-compact) path keeps forwarding it so downstream engine-fingerprint recognition is unaffected.

## v2.4.7 - 2026-07-09

### Features

- **Codex models manifest passthrough + automatic model learning.** Codex CLI and the Codex desktop app refresh their model picker from the provider's `GET /models?client_version=...` (custom provider mode) or `GET /backend-api/codex/models` (chatgpt_base_url mode), expecting the ChatGPT manifest format (`{"models":[{slug,...}]}`). axisrelay only served the OpenAI-compatible list, so Codex clients silently fell back to their local cache — the model picker froze on whatever existed the day the provider was configured, and new models never appeared. The manifest is now proxied verbatim (body + ETag, `If-None-Match`/304 supported) using a schedulable ChatGPT OAuth account's credentials: clients always see the account's real, current model entitlements, including capability metadata (`prefer_websockets`, `truncation_policy`, input modalities) that the OpenAI list can't carry. `client_version` acts as the dispatch fingerprint — requests without it keep the OpenAI-style list unchanged. On top of that, unknown slugs in a fetched manifest are learned into the model registry asynchronously (strictly add-only: existing rows — including admin-disabled ones — are never touched, absent models are never deleted since a manifest reflects one account's entitlements), so a brand-new upstream model passes request-side model validation as soon as any Codex client refreshes its picker, with no manual sync or release needed.
- **Continue-thinking: optional auto-continuation for truncated Codex reasoning (#335, #338).** New system settings `codex_continue_thinking_enabled` (default off) and `codex_continue_max_rounds` (default 8, range 1–32). When enabled, the `/v1/responses` streaming path detects the `518n−2` reasoning-token truncation fingerprint, re-sends the current turn's reasoning (ids stripped, `encrypted_content` preserved, tagged as commentary) back upstream on the same account, and folds multiple upstream rounds into a single downstream SSE response — sequence numbers and output indices are renumbered end-to-end, usage is rebuilt from the single-response perspective, and per-round real consumption is logged separately for billing. Disabled, the path is byte-for-byte identical to before.
- **Reasoning effort: `none`/`minimal` pass through, `ultra` pre-wired.** The upstream accepts `none/minimal/low/medium/high/xhigh`, but the proxy's whitelist only knew `low..xhigh` and silently clamped everything else to `high` — quota-saving `none`/`minimal` requests got maxed out instead. Both are now forwarded as-is, and an `ultra` tier is pre-registered (settings, reasoning-effort model aliases, frontend selectors) so an upcoming model tier can be used without a code change; `max`→`xhigh` mapping is unchanged. Usage-log effort badges are color-coded on a cool-to-hot palette across all tiers.
- **Built-in `gpt-5.6-sol`/`gpt-5.6-terra`/`gpt-5.6-luna` fallback.** The gpt-5.6 series (Sol/Terra/Luna) that appeared on the official site is pre-registered in the built-in model list, so it becomes requestable the moment the upstream opens access — marked `APIKeyAuthAvailable=false` like gpt-5.5 (ChatGPT-account models). Pricing is unpublished and bills via the default fallback until official rates land.
- **Output tokens-per-second column in usage logs.** Usage table and mobile cards show output throughput (output tokens ÷ generation seconds). When a first-token time is recorded it is excluded from the window — closer to pure decode speed, so TTFT doesn't drag the rate down; failed or empty-output requests show `-`.
- **Admin UI overhaul (#336).** Mobile navigation gets primary tabs plus a "more" sheet with dense tables converted to cards on small screens; theme settings gain follow-system mode, light/dark swatches, and one-click reset; the accounts page gets clickable KPIs, status chips, pinned row actions, and a floating detail drawer; API keys becomes a product-console surface (filterable KPIs, search/sort, `last_used_at` from usage logs, one-time create reveal, optimistic updates); settings layout unifies control sizes and pins the docs TOC while scrolling.

### Fixes

- **Body-signal compact requests on relay-account pools are promoted to the compact chain.** Newer Codex clients (remote compact v2) embed the session-compaction trigger as an input item (`type: "compaction_trigger"`) in a plain `POST /v1/responses` instead of calling `/v1/responses/compact`. ChatGPT OAuth accounts' native upstream accepts that form directly (verified: returns compaction output items), so passthrough is kept — such requests are pinned to official accounts whenever one is available, preserving the native SSE response shape. But a relay (OpenAI Responses API) account's plain `/v1/responses` generally rejects the trigger or returns a non-compaction response, leaving the client stuck with "expected exactly one compaction output item, got 0"; when no official account is available (relay-only pools), the request is now promoted at handler entry to the `ResponsesCompact` chain, hitting the relay upstream's `/v1/responses/compact` — the compact chain's own `stream` stripping ensures the JSON response isn't misparsed by streaming logic into account-rotation retries.
- **Namespace-style `image_gen` tool declarations no longer get the hosted image tool stacked on top.** Codex's /image skill declares image generation as `{type:"namespace", name:"image_gen"}` (top-level `tools[]` or nested in Responses Lite `input[].additional_tools`), which the auto-injection logic didn't recognize — so those requests got a hosted `image_generation` tool *and* bridge instructions injected on top, and the bridge text explicitly assumes the `image_gen` namespace is absent: duplicated tooling with self-contradicting instructions. Both injection decision points (ChatGPT and OpenAI relay paths) now detect the namespace form and pass such requests through untouched.
- **Asymmetric `arguments` types on replayed input items no longer 400 (#330).** The upstream requires `function_call.arguments` to be a JSON *string* but `tool_search_call.arguments` to be an *object*; clients or the `previous_response_id` cache replaying a previous turn's output items verbatim tripped "Invalid type for 'input[N].arguments'" — only in conversations that had used tool search. A normalizer now fixes both directions after cache expansion; the input-type whitelist also gains upstream-supported types (`shell_call`, `apply_patch_call`, `mcp_call`, …) so the proxy doesn't reject legal replays before the upstream sees them, and the call-id continuation cache covers `shell_call`/`apply_patch_call`.
- **Multi-line upstream WS error frames are no longer truncated in SSE.** An upstream error frame's `error` object can be pretty-printed JSON with real newlines; embedding it verbatim into a single-line `data:` SSE event cut everything after the first newline — logs, DB, and clients all saw just `..."status_code":400,"error":{`. Error objects are now compacted to one line (`json.Compact`), with a final safety-net compaction before WS→SSE writes.
- **Removed the upstream HTTP client's 10-minute total timeout (#287).** `http.Client.Timeout` spans the entire request lifecycle including body streaming, so long streaming turns (complex tasks can exceed 10 minutes) were cut off mid-transfer. The standard transport now matches the uTLS path's staged control: `Timeout: 0` with lifecycle owned by the request context (downstream disconnect cancels), a new 5-minute `ResponseHeaderTimeout` to catch upstreams that never answer, and unchanged dial timeout and stream-stall detection.

### Build

- **Go builder image bumped to `golang:1.26.5-alpine`.** Container builds pick up the 1.26.5 stdlib security fixes.

## v2.4.6 - 2026-07-08

### Features

- **Configurable retry interval and sticky same-account retry for transport errors (#331).** Two new system settings refine retry behavior; defaults preserve the old behavior exactly. `retry_interval_ms` (0–30000, default 0 = immediate) inserts a wait between retry attempts — covering transport errors, upstream non-200 retries, pre-first-token stream-break transparent retries, and WS silent retries across all proxy paths — and aborts immediately if the client disconnects mid-wait (the account's concurrency slot is released before sleeping). First-token-timeout retries skip the extra wait since they already burned a full timeout. `transport_retry_policy` (`rotate` default / `sticky`) changes what happens on connection-level failures (network blips, proxy node switches): in sticky mode the request retries on the *same* account — no hard exclusion, session affinity kept, and no `ReportRequestFailure`, so a local network hiccup no longer burns through the whole account pool while marking every account unhealthy. Upstream HTTP status errors (5xx/401/429) still rotate, since those are per-account verdicts. Both settings live next to "max retries" in the admin settings page.

### Fixes

- **Streaming requests that fail before the first token now return real HTTP errors instead of 200 (#318, reported and first-fixed by @JrCx7scC).** When the upstream terminated a stream with `response.failed` (e.g. `context_length_exceeded`) before any token, axisrelay wrapped the failure into a 200 SSE stream (plus `[DONE]` on the chat path), so billing middle layers treating axisrelay as an upstream saw a normal-looking success with no usage and billed their users by *estimated input tokens* — "upstream rejected with 0 output, user billed for the full input". The original PR's direction was right but didn't survive the wire: the post-loop flush committed the 200 header before the error JSON could be sent (gin's `Flush()` calls `WriteHeaderNow()` even on an empty buffer), so the fix additionally guards the epilogue flush on "wrote any body", overrides the pre-set SSE Content-Type, and gates the error return on an explicit abort flag. Applied across Responses/ChatCompletions streaming, the OpenAI Responses direct branch, and inbound WebSocket — where there is no HTTP status to fix, so non-retryable pre-first-token failures now send a structured `error` frame and close with a semantic close code (429→1013, other 4xx→1008, 5xx→1011) instead of relaying the raw failure frame and ending the turn as if it succeeded. Retryable failures keep the existing silent-retry/relay contract.
- **wham 401 no longer unilaterally bans an account — the `/responses` probe is the final arbiter (#328).** codex_at (bare AT import) accounts can be permanently 401 on the wham endpoint (token missing a workspace claim) while real `/responses` traffic works fine; the usage probe treated that 401 as dead credentials and put the account into a 24h unauthorized cooldown — re-banning it after every manual reset, or flapping banned/unbanned every probe round when fallback probing was on. A wham 401 is now a sentinel: no failure report, no ban. When fallback is available the `/responses` probe delivers the auth verdict (200 recovers / 401 bans); in wham-only mode the 401 is just logged, leaving genuinely dead tokens to be cooled down by real-traffic 401s.
- **Duplicate-account merge race could soft-delete both accounts.** After a merge, the async probe reload on the surviving account could trigger a reverse merge before the losing row was soft-deleted — the reverse pass merged old→new, then the outer pass soft-deleted the new row too, losing the account entirely (reproduced deterministically in CI where the fake probe returns instantly). Merges are now serialized under a mutex and the losing row is soft-deleted before the probe reload, so concurrent or recursive merges always see each other's results.
- **Online update hardening (#327, #329).** After an in-place update the restart re-exec could still launch the *old* binary — `os.Executable()` resolves `/proc/self/exe`, which after the atomic swap points at the deleted old inode; restart now execs the explicit new binary path. The frontend no longer caches the whole update-info blob (a stale `has_update=true` survived the update and kept showing "new version available" when current == latest); only the remote release fields are cached and the comparison is recomputed against the running bundle version on every check. The update/restart UI also gains client-side timeouts and a "refresh manually" hint when the restarted process doesn't answer within 90s.
- **Usage stats "source account" column shows email again.** A v2.4.5 frontend change made the account *name* take precedence, so unnamed AT-imported accounts all rendered as their placeholder name (`at-account-1`) instead of their email identity; the order is restored to email → name → ID on both the usage and operations views.

### Security

- **Bumped `quic-go` to v0.59.1 and `golang.org/x/crypto` to v0.52.0.** The quic-go bump fixes GO-2026-5676 (HTTP/3 QPACK trailer-expansion memory exhaustion), flagged by govulncheck as reachable through the actual call path; the x/crypto bump clears a Dependabot alert.

## v2.4.5 - 2026-07-07

### Features

- **Per-account model mapping for OpenAI Responses accounts (#325).** Each OpenAI Responses (relay API-key) account can now carry its own model alias map, so different relay channels can expose different model names while routing to the same upstream — e.g. mapping a channel-specific alias onto the upstream's real model name per account, on top of the existing global model mapping. Follow-up hardening: mapping keys and values both run through `ValidateModelName` (length + charset) before being accepted, since mapped source aliases flow into `/v1/models` responses, model validation, and usage logs — arbitrary strings are rejected instead of injected.
- **Connection-test content supports multi-line random pick and variable templates (#320).** Building on #317's configurable single test sentence, the test content setting now accepts multiple lines: each probe randomly picks one non-empty line, and `{{...}}` placeholders in the picked line are expanded before sending — `{{time}}`/`{{date}}`/`{{datetime}}`/`{{timestamp}}`/`{{rand}}`/`{{rand:min-max}}` (min>max auto-swaps; unknown variables are kept verbatim). This removes the "every account in a deployment sends the same probe sentence" fingerprint. Single-line configs behave exactly as before; all three test entrances plus the usage probe share the same rendering.
- **Reset-credit expiry details in the account usage dialog (#322).** The "manual quota reset" section now fetches the per-credit breakdown from the upstream's zero-cost reset-credits list endpoint and shows each credit's expiry time, sorted soonest-first with credits expiring within 7 days highlighted — so you can see not just how many resets are left but when each one becomes worthless, and burn the soonest-expiring ones first. The authoritative available count from the detail endpoint also recalibrates the locally cached number. Detail fetch failures degrade gracefully: the count and the reset button keep working, only the expiry list shows an "unavailable" hint.
- **Content-derived session seeds: multiple users behind one shared API key now stick to accounts per-conversation.** When a single API key is used as a channel for a whole downstream site, sessionless requests all collapsed onto the same key-derived affinity key — every user squeezed onto one upstream account and account switching was all-or-nothing per key. A content-derived seed (sha256 over conversation-stable fields: model + instructions + tools + system/developer + first user message) now slots in between explicit session signals and the per-key fallback, so each conversation converges onto its own sticky account and the pool load spreads out. Requests continuing via `previous_response_id` skip the seed (their input changes every turn and would drift), the seed only participates in local routing (never sent upstream in the default isolation mode), and the session-binding table gains a 65536-entry soft cap with expired-entry sweeping.

### Fixes

- **Forced-WS + shared single API key no longer bleeds context across users.** With `codex_force_websocket` on and one API key shared by multiple end users, user 2's turn could get stitched onto user 1's conversation: the reused stateless connection's handshake headers (`Session_id`/`Conversation_id`) were frozen to the *first* request's cache key and never updated, so an upstream that keeps per-connection conversation state saw every later user under the first user's session identity (same family as #268/#308). Stateless reused connections now send *no* session identity in the handshake at all — per-request isolation rides entirely on the per-request `prompt_cache_key` in the frame body; explicit-session and per-api-key modes are unchanged. A `CODEX_WS_STATELESS_ONESHOT=1` escape hatch disables slot reuse entirely (one connection per request, hard isolation, at the cost of per-request handshakes). Continuation affinity is also fixed: `previous_response_id` context lives only inside the connection that produced the response, so continuation requests now return to their original connection (response→connection bindings with TTL and a 4096 cap, guarded by account + API-key ownership + connection-pointer checks) instead of intermittently hitting "previous response not found" on a random slot.
- **`previous_response_id` context cache is isolated per downstream API key.** The response cache was keyed globally by `response_id` and expansion never checked ownership, so any request presenting (guessing, or accidentally reusing) someone else's response id would get that user's full conversation items injected into its input — a cross-key context leak. Cache entries are now namespaced by owner (`key:<id>`/anon) across all three write/expand paths (HTTP streaming + non-streaming, WS-native ingress, translator inline expansion); an owner mismatch is treated as a plain cache miss.
- **DB failures and upstream account 401s are no longer misreported as client 401s (#323).** Two layers of "server/account-side trouble" surfaced to clients as "your API key is invalid": (1) API-key resolution collapsed "key not found" and "DB failure" (connection exhaustion, timeouts) into the same result, so under load a PG "too many clients" turned into 401 `invalid_api_key` — resolution is now three-state, real DB errors return 503 and skip the `AUTH_FAILED` audit log so credential-attack alerting stays clean; (2) an upstream account's revoked OAuth token, after retries were exhausted, passed through as a raw 401 — non-`missing_scope` upstream 401s are now rewritten to a 503 pool-level error (`account_pool_unauthorized`), clearly distinct from client auth failure. Account-side handling was already correct (401 accounts get banned + cooled down); this fixes only the status-code leak to clients.
- **Free/team monthly-window accounts label their long usage window "30d" instead of "7d" (#324).** v2.4.4 taught the backend to recognize monthly (~30 day) rate-limit windows, but the frontend still hard-coded the long-window label as "7d" — misleading for free and team accounts whose long window really is a month. The account list usage bars, billed column, and scheduler board badges now render the real window length (`30d` for monthly, actual day count for other lengths, `7d` fallback when unknown). Also fixes the 7d billed-cost window start, which was hard-coded to `reset − 7 days`: for a monthly window that start date landed in the future, so `billed_7d` was stuck at 0.

## v2.4.4 - 2026-07-05

### Features

- **One-click online update from the admin console (#319).** The version popover's "new version" hint gains an "Update now" button: the backend (`GET`/`POST /api/admin/system/update`) picks the release asset matching the running OS/arch, downloads it from GitHub only (https allowlist re-validated on every redirect hop, 200MB cap), verifies SHA256 via the release asset digest or `SHA256SUMS.txt`, atomically swaps the binary (keeping a `.backup` for manual rollback, restoring the old binary if the swap fails mid-way), and re-execs in place on Unix; the frontend then polls until the new version answers and reloads. Dev/non-semver builds and Windows are marked unsupported instead of pretending to update, and release/Docker builds now embed the version via ldflags so the backend reports it accurately. Follow-up: container environments (docker/podman/k8s, detected via `/.dockerenv`, `/run/.containerenv`, `/proc/1/cgroup`) get an explicit warning that an in-container update is reverted when the container is recreated and image pull is the right upgrade path — the update itself stays available for temporary hot-fixes.
- **Configurable connection-test content (#317).** The prompt sent by single/batch/recycle-bin connection tests and the usage/plan-sync fallback probes was hard-coded to `"hi"` — an easily fingerprintable traffic signature shared by every axisrelay deployment. It is now a system setting (Settings → test content, auto-saved, defaulting to `"hi"`), applied uniformly across all five test/probe paths.
- **Per-account custom upstream headers (#316).** Each account can carry a set of custom headers (create/edit/import/scheduler flows all supported) that are applied to outgoing upstream HTTP and WebSocket requests after the standard headers, so they can also override them — the primary use case being `Chatgpt-Account-Id` to pin a multi-workspace team login onto a specific workspace. Header names/values are validated (RFC token charset, no CR/LF, 64 headers / 8192-char values max) and stored in the existing credentials JSON (no migration); accounts without custom headers behave exactly as before. Follow-up fix: the wham usage/reset-credit probes and Codex invites now use the *effective* workspace id (custom-header override first) instead of the OAuth-decoded one, so when a header reroutes traffic to another workspace, the quota bars, auto-pause, and smart pacing measure the workspace the traffic actually lands on; and wham identity write-back is skipped for overridden accounts so the override can't pollute the OAuth dedup identity.

### Fixes

- **Team monthly (30-day) windows are no longer misclassified as 7d — smart pacing stops over-throttling team accounts.** The usage-window classifier only had 5h/7d slots, but a team plan's second rate-limit window is actually monthly (~2592000s), and it was being stuffed into the 7d slot. Smart pacing computed its natural burn rate against a hard-coded 7 days, making the allowed pace ~4.3× too strict for monthly windows and needlessly throttling team accounts. Windows within a 28–31 day tolerance are now recognized as monthly (`IsMonthlyWindowSeconds` as the single source of truth, on both the wham and passive-header paths), the real window length feeds the pacing math, and the account list labels the window "30d" instead of "7d". Known limit: the window length lives in memory only, so between a restart and the first probe (seconds) pacing falls back to the 7d default.
- **Rate-limit cooldowns and 5h/7d window resets trigger an immediate wham probe when their countdown hits zero.** Usage probes were driven only by the periodic background sweep (default 2 min), so when a cooldown or window reset expired, the account flipped back to "available" instantly but its usage bar kept showing the stale pre-reset value until the next sweep. A boundary timer now stays armed on the earliest upcoming reset/cooldown boundary across all accounts and fires a zero-cost wham probe the moment it passes (with a 2s lag for clock skew), re-arming for the next boundary; cooldown/reset writers wake it lock-safely, and the periodic sweep remains as a fallback.

## v2.4.3 - 2026-07-04

### Features

- **API keys can restrict which account plans they dispatch to (multi-select).** Each API key's advanced limits gain an "Account plans" allowlist: pick one or more of `free` / `plus` / `pro` / `prolite` / `team` / `k12` / `go`, and the key will only ever be scheduled onto accounts whose plan matches one of them (e.g. a key limited to `plus` + `team` never touches free-tier accounts). Empty means no plan restriction. The filter is applied at the account-selection stage — the same layer as the existing group allowlist, with which it combines using AND (an account must satisfy both the plan and group constraints when both are set) — rather than rejecting the request after a pick, so a plan-restricted key simply behaves as if only the matching accounts exist. Matching is by raw lowercased `plan_type`, identical to the Accounts page plan filter, so `pro` and `prolite` stay distinct. Stored in the existing `limits` JSON column (no database migration), and because a plan-only restriction now counts as an access constraint, such keys always resolve their full metadata from the database and stay in sync on each request. The scheduler is only rebuilt when the allowlist actually changes, so the auth hot path isn't disturbed.

## v2.4.2 - 2026-07-04

### Features

- **Import accounts by pasting a full ChatGPT Session JSON (#314, #315).** The Add Account dialog gains a "ChatGPT Session" tab and the Import dialog gains a "paste text" button, so you can paste the raw JSON from `chatgpt.com/api/auth/session` and import in one step instead of manually splitting out `accessToken`/`email`. JSON parsing now accepts camelCase keys (`accessToken`/`idToken`/`planType`), flattens nested `user`/`account` objects (`user.email`→email, `user.name`→name, `user.id`/`account.id`→identity, `account.plan_type`→plan), and reads `expires`/`expired`/`expires_at` as either a numeric or string timestamp. Existing TXT/JSON/sub2api formats are unaffected — the new fallbacks only add information where a field was previously empty — and session imports flow through the normal importer, so they inherit identity dedup and the new/updated counting below.
- **Optional smart pacing spreads remaining quota evenly across a usage window (issue #312).** When enabled, if an account is burning a 5h/7d window faster than an even pace (used% running ahead of elapsed-time%), its dynamic concurrency is scaled down to a sustainable rate so the remaining quota lasts until the window reset instead of hitting the limit early. Pacing is per-account (each account paces on its own window, so the pool is smoothed as a whole), reuses the existing guard-band concurrency machinery (no new background task — it rides the usage snapshots already being refreshed), takes the stricter of the 5h/7d ratios, and never drops below a configurable floor. Off by default; Settings adds an enable toggle, a window selector (5h+7d / 5h / 7d), and a minimum-concurrency floor. Only engages when a valid usage+reset signal exists.

### Fixes

- **Credit accounts skip 5h/7d usage-window rate limiting (#313).** `credit_enabled + credit_skip_usage_window` accounts are meant to bypass usage-window limits, but response-header sync and the WHAM probe still marked them `rate_limited` whenever a 5h/7d window hit 100%, so a credit account was excluded from local scheduling despite the flag. A shared `SkipsUsageWindowLimits()` gate now records the usage snapshot but skips the local rate-limit marking (`MarkUsage7dRateLimited` / premium 5h) for such accounts, while real cooldown/error paths (401, existing cooldowns, genuine upstream failures) are preserved.
- **codex_at accounts merge into an existing same-identity account after the identity probe; import progress separates new vs updated.** codex_at tokens (`at-...`) carry no decodable JWT, so their OAuth identity is unknown at insert and only backfilled later by the WHAM probe — the post-probe merge the RT path already performs was missing here, so re-adding a codex_at could leave a duplicate. Manual codex_at adds now trigger a probe, and once it backfills email+account_id the importer re-checks for a same-identity account and merges into it (soft-deleting the new row, preserving usage stats). Separately, import/add now counts an "updated" bucket distinct from "new": hitting an existing identity only refreshes its credentials and no longer counts as a new add (fixing the success+duplicate double-count where re-importing the same accounts showed a nonzero "new"), and a single pasted access token now streams a progress bar too.
- **A 5h/7d window reset now triggers an immediate verification probe.** After a window's reset time passes an account looks "available" again, but it may actually be banned upstream — and the post-reset refresh was gated behind the usage `maxAge`, leaving up to ~10 minutes in which a bad account kept getting scheduled and failing. `NeedsUsageProbe` now fires a probe promptly when the reset boundary is crossed and the snapshot predates it (no `maxAge` delay), probing once per boundary; a real 401 then marks the account unauthorized.
- **A WebSocket upstream `close 1008 (policy violation)` now triggers async auth verification.** On the forced-WS transport a token that is actually invalid is rejected by the upstream as a `close 1008` rather than an HTTP 401, so it was classified as a generic transport failure — the account only lost health points, was never cooled down, and kept being scheduled and failing (the real `Unauthorized` was visible only on the HTTP transport). A WS upstream close now asynchronously runs a zero-cost WHAM probe for that account: a real 401 marks it `unauthorized` (matching the HTTP path), while a content-policy/transient 1008 leaves a healthy account untouched. Throttled to once per 30s per account.

## v2.4.1 - 2026-07-03

### Features

- **Bare RT imports now auto-merge into an existing same-identity account, preserving usage stats.** Scenario: an account was first imported via AT, then its bare RT was imported later. A bare RT carries no identity (it is not a JWT), so import-time dedup necessarily lets it through, and the identity only becomes known after the first refresh exchanges it for an AT — previously the flow stopped there, leaving two accounts (AT + RT) for the same identity in the pool. After a successful refresh the importer now re-checks for an identity duplicate: on a hit, the new credentials are merged into the existing account (writing the refresh token upgrades it to auto-renewable — RT takes highest priority) and the newly inserted row is soft-deleted. The merge only updates credential/identity keys — `codex_*` usage snapshots are untouched and the surviving account keeps its ID, so usage statistics and per-account request history are fully preserved. Copies imported with "allow duplicate add" are skipped.
- **AT batch add gains a progress bar.** `POST /accounts/at?stream=true` streams per-token SSE progress (aligned with the RT/ST path); the frontend automatically switches to streaming when more than one AT is pasted.

### Fixes

- **AT imports dedup by `user_id` identity — duplicate accounts no longer pile up.** Two stacked holes let the same account enter the pool repeatedly: (1) a personal-plan AT's JWT may carry no workspace `chatgpt_account_id`, only a `user_id` — identity dedup required email+account_id, so it fell back to raw access-token comparison, and since ATs rotate, a re-imported newer AT never matched the stored one; (2) the wham probe used to backfill `user_id` into the `account_id` field when the workspace id was missing, polluting the dedup key so even workspace-bearing JWTs never matched. JWT parsing now extracts `user_id` (with `chatgpt_user_id` fallback), the identity key is email + (account_id or user_id), the wham backfill no longer writes user_id into account_id, and `FindActiveAccountByOAuthIdentity` matches the `user_id` key as well as legacy polluted `account_id` values. Covers both the add dialog and TXT/JSON file imports.
- **Data migration v2 merges existing user_id-shaped duplicate accounts on startup.** The identity aliases now include the `user_id` key, and the dedupe migration reruns once to merge stock duplicates (both correctly-keyed `user_id` rows and rows whose `account_id` was polluted by the old wham backfill) — no manual cleanup needed; losers are soft-deleted into the recycle bin with audit events.
- **Copies imported via "allow duplicate add" are exempt from identity dedup and migration merges.** Forced duplicates (e.g. the same account with different proxies) were indistinguishable from accidental ones, so the dedupe migration would have merged away copies the user kept on purpose. Forced imports are now tagged `allow_duplicate=true` in credentials across all four add/import paths; the dedupe migrations skip tagged rows, and identity matching skips them too so a later normal import updates the primary account instead of writing new credentials into a forced copy. Note: only copies imported after this version carry the tag — force-imported duplicates from v2.3.9/v2.4.0 will still be merged by the v2 migration.
- **`context_length_exceeded` no longer triggers account-rotation retries (#310).** When the upstream returned `context_length_exceeded` as a `response.failed` frame (no explicit status code), the error-code string matcher didn't recognize it, fell through to the default 500 classification, and the request was transparently retried across up to 3 accounts — each attempt necessarily failing again while charging an innocent account a server-failure health penalty. Deterministic client errors (`context_length`/`context_window`, `string_above_max_length`, `model_not_found`, `unsupported_*`) are now classified as 400: no retry, no account penalty, and the error passes through to the client (which is the party that must shorten the input). The HTTP and WebSocket pre-first-token retry decisions share this classification.

## v2.4.0 - 2026-07-02

### Features

- **K12 plan classification and badge in the account list (#307).** The plan filter gains a K12 tab and k12 accounts get a green plan badge. The three separately hard-coded premium-plan checks on the frontend (accounts page, quota distribution chart, rate-limit recovery chart) are unified into a single `isPremiumUsagePlan` aligned with the Go side (including k12/edu/education/go), so k12 accounts now render the 5h usage bar and participate in the 5h quota distribution / rate-limit recovery charts.

### Fixes

- **WebSocket relay no longer reuses a connection whose stream was cut short — eliminating cross-session response mixing (#308).** When the downstream write failed (broken pipe / client disconnect) or the request context was canceled mid-stream, the read loop treated it as a normal end and `Close()` returned the connection to the pool — while the upstream was still pushing the remainder of that response on the same connection. The next request that reused it received the previous user's response frames. The release decision is now a whitelist: a connection is returned to the pool only when the stream was consumed to an explicit terminal boundary (`response.completed` / `response.failed` / upstream error frame) with no anomaly; any early exit (downstream write failure, context cancellation, upstream close, handshake failure with an unread stream) discards the connection. The ctx-cancel watcher also closes the pipe before tearing down the WS response so the client sees the cancellation instead of a spurious read error.
- **k12/edu and other paid plans are now covered by premium 5h rate-limit semantics (#306, #309).** `isPremium5hPlan` only recognized plus/pro/team, so after a k12 account hit 429 and was put on cooldown, the periodic wham usage probe judged it "not a premium plan" and proactively cleared the cooldown — the account flipped back to "available" while still being rate-limited upstream (the "shows available but still 429" report). All paid plans that carry a rolling 5h window (k12/edu/education/go, falling back to `IsPlusOrHigherPlan`) are now included; the plan gate errs broad on purpose since the rate-limit state additionally requires an observed 5h window at 100%.
- **k12 plans fully aligned with team semantics (#282).** k12 is an education team workspace whose quota applies per workspace, but several plan checks missed it: k12 accounts got no scheduler score bias (+50 like team) and effectively sank to the bottom of the pool ordering; 429 cooldown inference fell into the unknown-plan conservative default instead of team's 5h/7d dual-window detection; and the frontend "unsampled" check only looked at 7d usage, so k12-style workspaces that report only a 5h window were stuck showing "unsampled" forever — it now treats data in either window as sampled.
- **First-run setup no longer rejects Docker bridge sources (#199).** The bootstrap endpoint's IP allowlist (introduced in v2.1.4) only accepted loopback or `BOOTSTRAP_ALLOWED_CIDR`, but with Docker port mapping the host browser's source IP is the bridge address (e.g. `172.17.0.1`), so the most common local Docker deployment was blocked from the initialization page with a 403. Private and link-local sources are now allowed by default when `BOOTSTRAP_ALLOWED_CIDR` is unset (public IPs are still rejected; the endpoint remains rate-limited, audited, and only active while no admin secret is configured). Setting `BOOTSTRAP_ALLOWED_CIDR` makes the configured CIDRs authoritative (loopback always allowed), and the new `none` value restores the strict loopback-only mode. The 403 message now includes self-service guidance, and `.env.example` documents the variable.

## v2.3.9 - 2026-07-01

### Features

- **Prompt-filter review supports multiple Moderations API keys (#289).** Low-tier OpenAI accounts only get 50,000 TPM on the Moderations endpoint, so a single key can't keep up with high volume. The prompt-filter review key is upgraded from a single value to a key pool: multiple keys separated by newlines/commas are round-robined (package-level atomic cursor) to spread the quota, and on a `429`/`401`/`403`/`5xx`/network error the call automatically switches to the next key, only handing off to `FailClosed` once every key has failed. Reuses the existing TEXT column to store multiple lines (no DB migration), and the frontend becomes a multi-line textarea showing the configured key count. Single-key configuration behavior is unchanged.
- **Compact endpoint auto-strips the `-openai-compact` model suffix.** Aggregating gateways like newapi append an `-openai-compact` suffix to `/responses/compact` requests (e.g. `gpt-5.4-openai-compact`). The suffix is now stripped before compact model mapping and validation, so the newapi channel keeps that naming while axisrelay treats it as the base model (`gpt-5.4`) internally and forwards upstream, avoiding an "unsupported model" rejection. Only applies to `/v1/responses/compact` — plain `/responses` and the global model mapping are untouched.
- **Per-account request count limit.** Added a per-account cumulative request-count cap; accounts that reach the limit drop out of scheduling.
- **Stronger account import/add dedup with optional force-duplicate.** AT imports are deduplicated by OAuth identity, and re-importing valid credentials clears an error/banned (401) state to auto-unban the account; single-account RT/ST adds — which previously had no dedup at all — now dedup by raw credential too. Added an "allow duplicate add" toggle (off by default) that skips dedup and forces a new record, wired through both single-add and batch-import paths. The batch-import dialog gains a unified proxy input (the backend already accepted `proxy_url`; the frontend now sends it).
- **Hardened Codex client identification strategy.** Added strict official-client identification so a UA merely carrying a codex token no longer triggers an automatic compatibility upgrade; improved Codex client version parsing to cover more official UAs and trailer scenarios; added an engine-fingerprint signal model plus client allow/deny-list data structures and validation logic, with unit tests covering strict/legacy behavior, version boundaries, and policy validation.
- **Improved Codex custom tool event compatibility.** Extended compatibility handling for Codex custom tool incremental events (including custom deltas).
- **Removed the usage-reset radar module (`/admin/subscriptions`).** Deleted the Codex Reset Radar implementation on both ends, including the backend `admin/reset_radar.go` with its tests/routes/handler status fields and the frontend Subscriptions page, route, nav item, API, types, and copy.

### Fixes

- **Support socks5/http proxies with special characters in the password (#293).** When a proxy password contained URL-reserved characters like `#` or `?`, both the frontend `new URL` and the backend `url.Parse` failed to parse it, so adding `socks5://user:pass@ip:port` reported "please enter a valid proxy address" and couldn't dial even if added. Added `security.ParseProxyURL` (standard parse first, IPv6-compatible; on failure, percent-encodes the userinfo and retries, then uniformly validates scheme/host/port range), wired into all three dialer parse sites; the frontend validation switches to `new URL` with a lenient regex fallback so special-character passwords are no longer wrongly rejected.
- **Injected image tool no longer forces plain requests off WebSocket (#304, #288).** The WS→HTTP downgrade decision changed from "does `tools[]` contain an `image_generation` tool" to "is there real image-generation intent": HTTP is only forced for image-only models, `tool_choice=image_generation`, top-level image params, or natural-language image-generation intent, while ordinary requests stay on WS and the injected tool is stripped on the WS path via `stripResponsesImageGenerationTool`. The `chat/completions` path uses the same decision, gaining natural-language intent detection along the way.
- **Sync subscription expiry after a manual account refresh (#300).** After a renewal, the token JWT's `chatgpt_subscription_active_until` lags, so relying on token refresh alone kept an account's validity stuck at its stale creation-time value. A successful refresh now also fires a zero-cost wham probe to sync the subscription expiry and usage from the server's authoritative data, so both single and batch refreshes update the validity immediately.
- **Rate-limit cooldown and usage snapshot now track official window resets automatically.** Fixed two opposite-direction state desyncs: after usage is reset early upstream the cooldown wasn't cleared (`probeUsageViaWham` now re-evaluates against wham's authoritative window data even while rate-limited and proactively calls `ClearCooldown` when no longer limited); and after a rate limit expired the usage bar stayed at its old value (`NeedsUsageProbe` now fires a single zero-cost wham refresh when the 5h window reset time has passed but the snapshot still shows pre-reset high usage, guarded by `maxAge` to prevent repeated probing).
- **Fixed engine-fingerprint signal matching boundaries.** The fingerprint signal type is now trimmed before matching so a valid config with surrounding whitespace still hits; `header_exact` now iterates the original header keys with case-insensitive matching; added regression tests for signal types and non-canonical header keys.
- **Prevent Codex User-Agent from falling back to the Go default (#299).** Guards against the upstream Codex request's User-Agent unexpectedly reverting to Go's default value.

### Security

- **Bumped `golang.org/x/image` to v0.43.0.** Clears a security-scan alert.

### Build

- **Container `go mod download` uses the goproxy.cn mirror.** Speeds up dependency fetching during container builds.

## v2.3.8 - 2026-06-20

### Fixes

- **Codex 专属 `at-...` Access Token 不再按 JWT 解码，并可通过 WHAM 自动补齐账号身份。** `at-...` 现在会识别为 `codex_at` 类型，导入/添加时不会再尝试按 JWT 解析；账号列表的 AT 徽章会显示为 `codex_at`。对于已经导入但缺少邮箱或 `account_id` 的 codex_at 账号，后续单账号用量刷新或后台 wham 用量探针会把 WHAM 返回的 `email` / `account_id` / `user_id` 写回运行时和数据库，无需重新导入。

## v2.3.7 - 2026-06-19

### Features

- **Per-account on-demand usage refresh button (accounts).** Added `POST /api/admin/accounts/:id/usage/refresh`, which synchronously runs `ProbeUsageSnapshot` (preferring the wham endpoint — zero quota cost, no test conversation) and returns the latest 5h/7d usage. The usage column now shows a refresh icon next to each progress bar (wired into the desktop table, mobile cards, and personal mode) that re-pulls that account's bars instantly, with a spinner and failure toast. Also fixes progress bars not refreshing after a test connection: accounts that just finished a test are now force-scheduled for a delayed re-pull even when they already have usage data (e.g. showing 100%), bypassing the "has data = fresh" check in `needsUsageReload`.
- **Editable Prompt Filter extra rules.** The prompt-filter "extra rules" can now be edited from the UI (add/update/remove) instead of being config-only, with full validation feedback on the edit form.
- **Sync WHAM subscription expiry.** The subscription expiry time is now parsed from the WHAM usage response and used to refresh `subscription_expires_at` in both the runtime and the database via the wham probe, with added time-format compatibility and persistence-consistency tests.

### Fixes

- **Codex invite dropdown no longer hides disabled/abnormal but credential-usable accounts (#281).** Relaxed `isCodexInviteCandidate` to match the backend: only relay / AT-only accounts are excluded, dropping the `enabled`/`locked`/`status` filters, since `SendCodexInvite` only requires an access token and does not check those fields — otherwise accounts that were merely paused from scheduling or temporarily abnormal (but still credential-usable) were hidden. The account picker also gains status dots + disabled/locked/banned/error badges on items and the selected account, a light warning when an abnormal-but-usable account is selected, and full keyboard navigation (↑↓ to move, Enter to confirm, Esc to close, highlight scrolls into view).
- **"Normal" account card count vs. filter mismatch yielding an empty list.** The "normal accounts" card counts as `total − abnormal − rate-limited` (folding `refreshing` and similar non-abnormal/non-rate-limited states into "normal"), but clicking the "normal" filter applied an extra hard `status ∈ {active, ready}` constraint that excluded `refreshing`/`cooldown` states. With many accounts refreshing this produced a card showing "40k+ normal" that opened to an empty list. Both paths now use the same health semantics (abnormal > rate-limited > normal): the "normal" filter is `not abnormal && not rate-limited`, matching the card exactly.
- **Transient failures no longer force admin logout (#admin auth).** `checkAuth` previously cleared `admin_key` and forced re-login on any `catch` or `!res.ok`, so any network blip / service restart / 5xx during the 30s polling loop logged the user out and required re-entering the key. The key is now cleared only on a genuine 401 (invalid key); under transient network/5xx failures an existing key optimistically stays logged in and the next poll self-corrects.
- **Improved Prompt Filter hit-log context display.** Refined how prompt-filter match context is captured and rendered in the hit logs for clearer surrounding context.

## v2.3.6 - 2026-06-18

### Features

- **API key self-service usage portal `/key-usage` (#271).** Added a public, login-free `/key-usage` page where a carpool/shared-key user can paste their own API key and view that key's usage (totals, model breakdown, recent logs) without admin access. The API key management page now surfaces the portal address with copy and open shortcuts, and each key has a shareable direct link, backed by a dedicated public usage endpoint that only exposes the data for the presented key.
- **Per-key usage reset (#271).** A single API key's accumulated usage can now be reset (`reset_quota`) when editing the key, zeroing just that key's counters without minting a new key — so monthly re-accounting for shared/carpool keys no longer requires recreating keys.
- **5h guard-band slowdown concurrency (#270).** Added a configurable "guard band" before the 5h usage auto-pause threshold: as an account's remaining 5h quota enters the band (default 5 percentage points), its scheduler dispatch score is progressively penalized and its dynamic concurrency is capped to a configured ceiling (default 1), giving accounts a soft landing instead of slamming into the hard auto-pause. Both the band width and the guard concurrency are configurable from Settings (with global defaults), and disabling either turns the slowdown off.

## v2.3.5 - 2026-06-17

### Features

- **Per-request upstream isolation by default (#268).** For requests without an explicit session, the upstream identity key (`prompt_cache_key` + session/conversation id) was previously derived deterministically from the downstream API key, so different requests sharing one API key collapsed onto the same upstream session id and leaked context across requests. The upstream identity is now decoupled from local connection reuse and account affinity: by default each sessionless request gets a unique upstream identity (`isolated` mode), while the 8-slot WS connection pool still routes by a stable API-key-derived pool key (preserving reuse and 503 handshake-throttle resistance) and account affinity is unchanged. Set `CODEX_REQUEST_ISOLATION_MODE=per-api-key` to restore the old shared-cache behavior.
- **Active in-flight request indicator (accounts).** The account list status column now shows a blue pill with a breathing dot and the live in-flight request count (`active_requests`), visible only when greater than zero, to surface which accounts are busy and how concurrent.
- **Display `chatgpt_account_id` (team workspace id).** The account list now shows the `chatgpt_account_id` (the team-plan workspace id decoded from the access-token JWT) under the email in monospace, so multiple workspaces under the same login email can be told apart.
- **Hardened + optimized OpenAI active reset-credits flow.** The wham/usage and reset/consume calls now go through the uTLS Chrome-fingerprint transport (matching the `/responses` gateway) to reduce Cloudflare blocks; consume reuses a single `redeem_request_id` as an idempotency key so a retry after refresh no longer burns an extra reset, a per-account mutex removes the check→consume TOCTOU race, a 401 auto-refreshes the token and retries once, and the post-reset wham round-trip is moved to the background to shorten the response path. Successful resets are written to `account_events` audit records.
- **`CODEX_TRUSTED_PROXIES` env to configure trusted reverse proxies.** PR #265 disabled Gin's default trusted proxies but stripped the real client IP behind reverse proxies; AxisRelay now trusts loopback and common private networks by default for same-host/Docker WAF deployments, and operators can tighten or disable this via `CODEX_TRUSTED_PROXIES` (comma/space-separated IP/CIDR list, or `none` to disable). Invalid entries fail fast at startup.
- **Localized upstream reset error codes.** When an active reset is rejected by upstream (e.g. `rate_limit_not_resettable` on business/credits-only plans), known codes are translated to a Chinese explanation with the original upstream JSON appended; unknown codes fall back to the raw upstream text, and empty bodies fall back to the status code.
- **1h metrics sampling granularity.** The 1h time range now samples at 1-minute granularity across 60 buckets.
- **Account card layout refinements.**

### Fixes

- **Discard broken upstream WS connections instead of reusing them.** After an upstream WS error (close 1006/1009/1011, broken pipe, unexpected EOF), `WsResponse.Close()` only removed the pending entry and returned the connection to the pool without closing the underlying socket, so the broken connection was misjudged reusable (a CLOSE_WAIT probe Ping still succeeds, a false positive) — causing slow first tokens / dropped streams and leaking fds in CLOSE_WAIT. Read errors now mark the connection broken, and `Close()` distinguishes normal completion (release for reuse) from abnormal termination (discard: close the socket and remove it from the pool via `CompareAndDelete`). Refs #267.
- **Refresh reset credits independent of usage freshness.** The reset-credits count is only refreshed by the wham/usage probe, but probing was gated on usage-snapshot freshness; since normal `/responses` traffic bumps that snapshot via response headers (which don't carry reset credits), busy accounts looked fresh and never got a wham probe. Reset-credit probe time is now tracked separately, and a zero-cost wham-only probe is allowed during rate-limited cooldown and premium 5h limits.
- **Disabled Gin's default trusted proxies (#265).**
- **Codex invite account dropdown only showed one account.** The account combobox pre-filled and filtered by the selected account's email, so opening it matched only that account; it now shows all eligible Codex OAuth accounts when opened and filters by text only while the user is actively typing.

## v2.3.4 - 2026-06-16

### Features

- **Account health visualization and "personal mode".** The account list/cards gain a "health status" bar (time-bucketed coloring based on recent request success/failure), the dashboard adds a system-level health overview between the top cards and usage stats, and the time-range selector is promoted to a page-level shared control. The accounts page adds a "pool mode / personal mode" toggle — personal mode renders accounts as richer two-column cards (avatar, equal-height alignment, icon+text action area), with the initial mode auto-selected by pool size on first upgrade.
- **Active reset credits for Codex (#249).** The account usage dialog, list badges, desktop table, and card action areas can all view the remaining "active reset count" and reset it in one click (with confirmation, refreshing after consuming 1), reusing the existing wham upstream path instead of the generic passthrough.
- **Prompt-filter review key (#257).** After a local rule match, an optional review model (OpenAI/Anthropic-compatible moderation) can re-check the request to reduce false positives; supports base_url / model / timeout / fail-closed configuration.
- **Blocked requests record full content and are queryable (#259).** Blocked requests now record the full request content in "prompt filter → logs" (no longer just a 500-char preview, truncated at 32K) and are included in search; in usage stats, cyber_policy errors can be clicked to view the full triggering request content.
- **Local token-counting compatibility endpoints (#238).** Added `POST /v1/messages/count_tokens` and `POST /v1/responses/input_tokens`, which return a local `input_tokens` estimate without forwarding upstream or consuming quota, so clients like LiteLLM no longer get a 404 when probing token-counting endpoints.
- **Codex referral invite feature (#260).** Added a Codex referral invite UI and batch invite capability, plus an improved account picker (#264).
- **Deduplicate accounts by OAuth identity (#262).** Imported/added accounts are deduplicated by OAuth identity to avoid the same account entering the pool twice.
- **Account bulk-update API (#263).** Added a bulk account update endpoint supporting batch scheduling and other config changes.
- **Edit OAuth account auth config and re-authorize UI (#250).** Supports editing an existing OAuth account's auth config and re-authorizing from the frontend.
- **External async image task API (#254).** Added an external async image-generation task API.
- **Backend streaming forwarding performance (#252).** Optimized the streaming forwarding path performance.

### Fixes

- **Streaming cyber_policy penalties not recorded (#258).** cyber_policy bans are delivered as `response.failed` (HTTP 200) in Codex streaming responses, but previously only non-2xx errors were written to `prompt_filter_logs`, so streaming-path cyber_policy was invisible in the prompt-filter logs; recording is now added across the 4 streaming failure paths.
- **Failed image request usage not counted (#239).** Fixed error image-request records not showing in usage stats.
- **`/v1/messages` cached-token double billing (#253).** Anthropic usage now deducts cache-hit tokens to avoid double-billing the cached portion.
- **Claude Code tool argument sanitization correction (#251).** Fixed compatibility issues caused by overly broad tool argument sanitization.
- **Upgraded vite to fix npm audit high severity (#261).** Frontend dependency upgrade fixing an npm audit high vulnerability.

## v2.3.3 - 2026-06-13

### Features

- **Cloud storage links for image generation (#240).** When S3-compatible cloud storage is configured and a client requests `response_format=url`, `/v1/images/generations` now uploads each generated image to object storage and returns a time-limited (1h) presigned link instead of a base64 data URL, falling back to the data URL on any upload or signing failure so the API never hard-fails on storage misconfig. Uploaded images are registered into the admin gallery via a lazily created synthetic job + asset record, so they appear in gallery/history and are cleaned up (DB row + backing object) on delete.
- **API key token-limit unit selector (#234).** The API key 5h/7d token limit fields now offer a unit selector (token / K / M / B) so large quotas can be entered and displayed in readable units instead of raw token counts.

### Fixes

- **Request forwarding error handling and success accounting (#246).** Hardened the OpenAI Responses `ttftGuard` call site with explicit nil-safe wrapping, handled OpenAI/Codex compact response-body read failures while preserving the final 502, synced usage headers on Codex compact read failure and reported request success on the happy path, and rebuilt the Anthropic stream error SSE payload via `json.Marshal`.
- **WebSocket relay stability (#247).** Improved `wsrelay` executor, manager, and session handling to keep upstream WebSocket relay connections stable.
- **Function `tool_choice` shape normalized.** A `tool_choice` object missing `type` but carrying a `function` object or top-level `name` is now normalized to `type: "function"` with a flattened `name`, matching OpenAI SDK convention so the upstream no longer rejects the request.
- **Missing encrypted reasoning content (#235).** `missing_required_parameter` errors on a `*.encrypted_content` param are now recognized alongside `invalid_encrypted_content`, so requests with absent encrypted reasoning content are retried instead of failing.
- **5h usage freshness split from 7d (#241).** The 5h usage snapshot now persists its own `codex_5h_usage_updated_at` timestamp instead of overwriting the shared `codex_usage_updated_at`, so 5h and 7d freshness no longer clobber each other.
- **Usage logs page size up to 500 (#244).** The usage logs endpoint now accepts a page size of 500.
- **Read pages sanitizer narrowed (#245).** Tightened the Read pages sanitizer scope.

### Features

- **Group filter: ungrouped and exclude modes (#229).** The account group filter now supports an "ungrouped" quick option and per-group tri-state filtering (only / hide / off), so accounts outside chosen groups can be isolated, selected in bulk, and assigned to a group quickly. The groups column is also shown by default in the table view.

### Fixes

- **`gpt-5.3-codex-spark` no longer receives the default image tool (#230).** The Responses translator auto-injected the hosted `image_generation` tool (plus bridge instructions) into ordinary text requests; spark rejects hosted image tools, so text-only spark requests failed on the HTTP upstream path. The default injection is now skipped for spark while explicitly supplied image tools are still honored.
- **Tools without a `type` field no longer fail requests (#219).** Tool definitions missing `type` (or with `type: null`) were forwarded verbatim and the upstream rejected the whole request with 400 `Unsupported tool type: None`. Function-shaped tools (a nested `function` object or a top-level `name`) are now treated as `type: "function"` per OpenAI SDK convention on both the Chat Completions and Responses paths; unrecognizable typeless tools are dropped instead of failing the request.
- **Usage logs are requeued when a flush fails (#233).** Failed usage-log batches are put back at the front of the buffer and retried on the next flush instead of being dropped, and usage-log inserts plus API key quota updates now run in a single transaction on both SQLite and PostgreSQL.
- **Reduced timer allocations while waiting for Redis token refresh (#231).** `WaitForRefreshComplete` now reuses a single ticker and timeout timer instead of allocating a new timer every 200ms poll (previously up to ~150 timers per 30s wait).

### Performance

- **Request bodies are read once and reused (#232).** `RequestSizeLimiter` caches the body it already reads, and the body-cache middleware plus all JSON proxy handlers reuse it, cutting up to ~2/3 of duplicated request-body buffer allocations on hot paths.

## v2.3.1 - 2026-06-11

### Features

- **Group-level and global auto-pause thresholds (#227).** Usage auto-pause thresholds can now be set globally (Settings) and per account group (group dialog), resolved with account > group > global priority. Account-level settings win when present, otherwise the smallest non-zero group threshold applies, falling back to the global default. Changes take effect immediately without a restart, and out-of-range values are rejected consistently across all three levels.
- **API key concurrency limits (#226).** Each API key can cap its concurrent in-flight requests via `limits.max_concurrency`. Requests over the cap receive 429 with a descriptive message across all proxy entrances (responses, compact, chat completions, Anthropic messages, images, WebSocket turns). Unset or 0 means unlimited.
- **Account group multi-select binding (#217, #222).** Account group assignment now uses a searchable multi-select dropdown instead of assigning new groups to all accounts.

### Fixes

- **WebSocket prompt cache restored (#202).** Since v2.2.7, requests without an explicit session identifier got a random per-request `stateless-` ID on the WS upstream path, which leaked into `prompt_cache_key` and the WS handshake session headers — so the upstream prompt cache never hit in WS mode (HTTP was unaffected). WS now injects a deterministic cache key derived from the downstream API key (matching HTTP behavior), restoring cache hits (~86% of input tokens on repeated prefixes in live testing).
- **WebSocket connection reuse under high RPM.** Sessionless requests previously opened a new WS connection per request; sustained load (~200 RPM) triggered upstream handshake throttling (`bad handshake` → 503). Such requests now reuse warm connections from a per-(account, cache key) slot pool (8 slots, falling back to one-shot connections when all slots are busy). Live testing at 200 RPM went from 128/200 success to 200/200 with zero handshake failures and roughly halved latency.
- **Codex CLI compact v2 input items (#224).** `/responses` no longer returns 400 `Invalid input type 'compaction_trigger'` for Codex CLI compact v2 payloads.
- **Usage probed immediately after OAuth account add.** Newly added OAuth accounts get an immediate usage probe instead of waiting for the next cycle.
- **Account table scrolling.** Adjusted account table scrolling behavior in the admin UI.

## v2.3.0 - 2026-06-10

### Features

- **Account recycle bin.** Soft-deleted accounts now land in a recycle bin instead of vanishing. Accounts can be restored to the runtime pool without a restart, test-connected before restoring (429/usage-limit results are recorded as rate-limited rather than failed), batch-tested with an optional auto-restore-on-pass toggle, filtered by plan type and searched with pagination, and permanently purged behind a typed confirmation enforced on the API as well.
- **Client IP in usage logs.** Usage records now capture the requesting client IP and display it in the usage views.

### Fixes

- **Relay accounts on `/v1/messages` and `/v1/chat/completions` (#181).** Both endpoints now schedule OpenAI Responses (relay API-key) accounts and forward over HTTP like `/v1/responses`, instead of returning 503 "No available accounts" once Codex OAuth accounts were rate-limited on relay-only deployments.
- **Image generation no longer routed over WebSocket (#220).** Requests carrying an `image_generation` tool (including the auto-injected one) are forced onto HTTP, and requests that do go over WS get the auto-injected image tool, its `tool_choice`, and bridge instructions stripped, so image generation can no longer stall WS streams on large base64 payloads.
- **Image SSE keep-alive.** Image generation SSE streams now stay alive through long upstream pauses, preventing idle disconnects mid-generation.
- **Account rate-limit state sync.** Rate-limit state observed by usage probes and WHAM checks is synced back to runtime account state so scheduling and status displays stay consistent.
- **Account status summary counts.** Admin account status summary counters now align with the account list filters.

### Security

- **Code-scanning hardening.** Local image storage rejects keys containing path separators on write and `..` traversal segments on read/delete (legacy relative refs keep working); remote-migration and sub2api import URLs must be complete http/https URLs with a host; SVG logo minification strips comments to a fixed point so `<!--` cannot survive sanitization.

## v2.2.9 - 2026-06-09

### Features

- **Account proxy validation (#212).** Added proxy test controls across account add/edit, OAuth generation, and OpenAI Responses account forms, backed by an admin proxy test endpoint that reports reachability, latency, IP, and location.
- **Usage range and reset-time display.** Dashboard usage cards now label and query the selected time range instead of always showing "today", and account quota reset labels now display precise seconds with full timestamps available in tooltips.

### Fixes

- **WebSocket message-too-big fallback (#214).** Upstream WebSocket `close 1009` / `message too big` failures before the first downstream token now fall back to HTTP for the same request instead of rotating accounts through another WS attempt, with logs classified as `message_too_big`.
- **CPA/sub2api JSON import (#215).** JSON imports now avoid treating repeated or conflicting ChatGPT account IDs as the same account when credentials differ, fixing failed or collapsed imports from CPA/sub2api export files.
- **Batch account test state updates.** Batch connection tests now update account status consistently with single-account tests, including `response.failed`, unauthorized, generic upstream errors, and usage-limit results; successful tests still recover stale banned/cooldown state.
- **Quota auto-pause scheduling (#216).** Accounts that reached configured usage thresholds remain out of scheduling while their quota window is still active, and stale reset timestamps are ignored so reset/cooldown state does not linger incorrectly.

## v2.2.8 - 2026-06-08

### Features

- **Account usage analytics.** Expanded the account usage details modal with Overview, Details, and Quality views, range-aware totals, richer model distribution controls, and quality signals including error rate, retry count, average first-token latency, P95 response time, streaming ratio, and compact ratio.
- **Batch account import workflow.** Refresh Token batch import now has progress feedback and kicks off usage sampling so newly imported accounts do not require a separate manual batch test before usage data becomes visible.
- **First-token mode setting (#207).** Added a system setting for strict vs loose first-token detection, making TTFT behavior configurable while keeping thinking/pre-output events from being counted incorrectly in strict mode.
- **Usage visibility controls (#203, #209, #210).** Usage stats now support additional time ranges, full token-number display, compact request markers, and dedicated WS/Fast request badges.

### Fixes

- **Batch account test consistency (#194).** Batch tests now match single-account tests more closely by using the WHAM usage preflight and parsing real SSE terminal events from a short request; `response.failed`, missing output, and `usage_limit_reached` are no longer treated as successful account recovery.
- **Forced WebSocket usage logs (#205, #210).** Requests forced through upstream WebSocket now persist and display the `via_websocket` marker correctly, with Fast tier shown as its own request badge.
- **Responses compact and quota handling (#174, #201).** OpenAI Responses API-key accounts can forward `/v1/responses/compact`, and nested `usage_limit_reached` errors are handled consistently for cooldown and sampling.
- **Billing tier policy persistence (#206).** `billing_tier_policy` now stays aligned across frontend save behavior, backend normalization, and database persistence.
- **OAuth authorization UX.** OAuth authorization links can be regenerated and copied reliably, including HTTP admin deployments where the Clipboard API may be unavailable.
- **Image and WS routing stability (#198).** Explicit image generation requests bypass forced WS routing, and `gpt-5.3-codex-spark` scheduling/model filtering is hardened.

## v2.2.7 - 2026-06-05

### Features

- **WebSocket silent retry controls (#195).** Added Codex upstream WS settings for hiding upstream errors, silently retrying pre-first-token upstream failures, and capping silent retries (`codex_ws_hide_upstream_errors`, `codex_ws_silent_retry_enabled`, `codex_ws_silent_max_retries`). These settings are available in the admin UI and persist through both Postgres and SQLite.
- **WHAM-only usage probe controls.** Added runtime/admin controls so usage probes can rely on the zero-cost WHAM endpoint without falling back to `/responses` probes when that is preferred.

### Fixes

- **WS upstream failure handling (#195).** Retryable upstream WS failures before the first token, including usage-limit, 429, 5xx, read errors, timeouts, and EOFs, now stay server-side while axisrelay switches accounts and rebuilds the upstream WS connection. If retries are exhausted, clients receive a unified friendly message while the original upstream error remains in backend logs and usage records.
- **Responses routing and model hardening (#198).** Hardened `/v1/responses` routing for recent Codex models including `gpt-5.3-codex-spark`, widened local plan gating so stale local `plan_type` records do not incorrectly block real upstream calls, and improved WS/TTFT handling around response payload content.
- **OpenAI Responses compact routing.** OpenAI Responses API accounts added with `base_url` + `api_key` can now use `/v1/responses/compact`; compact bodies are normalized for that upstream path instead of being sent through the ChatGPT-only compact route.
- **Usage-limit detection.** `usage_limit_reached` is now recognized even when wrapped inside `response.error`, `response.status_details`, or upstream 5xx-shaped payloads, so exhausted accounts are treated as quota-limited consistently.
- **Accounts toolbar wrapping.** Improved the admin accounts toolbar layout so search/filter/action controls wrap cleanly on narrower viewports.
- **Security scan recovery.** Updated React Router to a patched release and raised the Go toolchain directive to `1.26.4`, clearing the failing frontend npm audit and backend govulncheck jobs.

## v2.2.6 - 2026-06-03

### Features

- **Codex upstream WebSocket mode.** Added an opt-in `codex_force_websocket` system setting that routes Codex upstream traffic over a persistent WebSocket long-connection (reusing the `wsrelay` connection pool), reducing per-request TLS/handshake overhead to better match the official CLI. Disabled by default; when off, requests keep using the existing HTTP path. A downstream `https://.../v1/responses` (or `/v1/chat/completions`) HTTP POST is transparently forwarded as a `wss://` upstream connection.
- **Idle WS keepalive.** Added `codex_ws_keepalive_enabled` and `codex_ws_keepalive_interval_sec` (default 60s) to keep idle upstream WebSocket connections alive with background Ping frames. Keepalive never opens new connections and never sends business frames, so it consumes zero account quota. Disabled by default.
- **WS request badge.** The Usage log now shows a `ws` badge between the status code and model for requests that went over WebSocket.
- **Dedicated WebSocket settings card.** WS-related settings are split out of the crowded scheduling card into their own full-width "WebSocket (Codex Upstream)" card in the admin UI.
- **Bundled Codex CLI bump.** Updated the bundled Codex CLI version from `0.128.0` to `0.136.0` for a more faithful fingerprint and access to newer models.

### Fixes

- **WS upstream error passthrough.** Upstream WebSocket error frames are now relayed to the client as a `response.failed` SSE event preserving the original error, instead of being turned into a low-level read error that surfaced as a mysterious empty response / 32-90s hang. Any upstream error (rate limit, unsupported model, invalid parameter) is now visible to the client.
- **WS transport robustness.** Fixed the `wsrelay` dialer copy dropping `NetDialContext` (TCP KeepAlive never took effect); added 64KB read/write buffers with a shared write-buffer pool for large 48-91KB upstream frames; replaced the fixed 10ms busy-connection spin with exponential backoff plus a max-wait timeout; raised the read timeout from 60s to 120s so long-reasoning turns are not falsely disconnected; and log a warning when a WS path silently falls back to HTTP.
- **TLS session resumption.** Standard and uTLS transports now share a `ClientSessionCache`, so reconnects use TLS resumption (1-RTT) to cut cold-connection cost. Bumped `MaxIdleConnsPerHost` and `IdleConnTimeout`, and switched the HTTP server to explicit `ReadHeaderTimeout`/`IdleTimeout` with graceful shutdown (keeping `WriteTimeout` at 0 so streaming responses are never cut off).
- **Account lookup O(N) → O(1).** The store now keeps a `DBID → account` index, so session-affinity hot paths and all `FindByID` lookups no longer scan the account slice linearly.

## v2.2.5 - 2026-06-02

### Features

- **Codex model registry and reasoning models (#165).** Added system-setting controls for upstream-synced base models and reasoning-effort variants so model list entries can be configured from the admin UI.
- **Codex model redirect mapping (#189, #190).** Added a dedicated Codex model redirect map that can route downstream model names to another Codex model while preserving request reasoning effort.
- **Account quota auto-pause controls.** Added 5h/7d usage-threshold auto-pause controls with global defaults, per-account disables, edit-account controls, and batch editing.
- **Compact request visibility.** Usage logs now detect compact requests and show a compact badge alongside request model/reasoning information.
- **Email domain account tags (#191).** Account management now derives email domains, supports domain filtering with banned/total stats, searches by domain, and lets users show or hide domain badges.
- **Configuration examples.** Updated Codex config examples to cover the current model mapping and reasoning-effort setup.

### Fixes

- **Service-tier billing semantics (#183).** Split requested, actual, and billing service-tier handling so billing policy stays explicit when upstream reports a different actual tier.
- **SQLite and admin billing stability (#185, #186).** Stabilized account billing window reads and SQLite access paths so large account pools do not turn simple admin/API-key queries into transient 503s.
- **Account status and probe handling.** Unauthorized account errors are recorded more consistently, and batch account rendering has less unnecessary work at scale.
- **Refresh token reuse handling.** Reused refresh tokens are now treated as non-retryable credential errors instead of entering avoidable retry paths.

## v2.2.4 - 2026-05-28

### Features

- **Scheduler warm-tier bypass (#176).** Added a scheduler option to skip warm-tier demotion so selected accounts can stay in the healthy scheduling lane.
- **API Reference image previews.** The Try it panel now renders image responses directly when `/v1/images/generations` or `/v1/images/edits` returns `b64_json`, image URLs, or Responses-style image output, while still keeping the raw JSON visible.
- **Long-context billing details (#178).** Usage cost tooltips now expose when long-context pricing is active (`input_tokens > 272,000`) and show the actual input, output, and cache-read unit prices used for the request.
- **Root changelog.** Moved the project changelog to the repository root so release notes and documentation links point to a stable location.

### Fixes

- **WebSocket response continuity (#182).** Fixed WS mode context loss so `previous_response_id` continuation remains connected across `/v1/responses` WebSocket turns.
- **First-token timeout retries (#172).** First-token timeouts now retry by account-pool round and clear transient unavailable markers after the pool has been tried, reducing false "no available account" failures for small pools.
- **Banned-account test recovery.** Successful account tests now recover stale banned/error state instead of leaving the account marked unhealthy after the probe succeeds.
- **Bounded batch account tests.** Batch account tests now keep their execution bounded so duplicate or repeated test requests do not destabilize account state handling.
- **Version popover clipping.** The version popover is rendered through a portal so sidebar overflow no longer clips the latest-version panel.

## v2.2.3 - 2026-05-27

### Features

- **Usage reset radar.** New `/subscriptions` page consolidates the Codex Reset Radar summary, recent RSS events, and a reset-time hook. When a window-close signal is detected the backend clears stale cooldown/usage caches and re-tests every account so the pool reflects the new reset boundary immediately.
- **Streaming batch operations in admin.** Account batch refresh/test/enable/disable/lock/reset now stream per-account progress events (`success/banned/rate_limited/failed`) to the admin UI instead of waiting for the full operation to complete.
- **Compact usage number setting.** Added a system setting to render token counts with K/M units in the usage table for easier reading at scale.
- **Card view for account management.** Desktop accounts page gains a table/card view toggle (up to 5 cards per row on `xl` screens). Choice is persisted in `localStorage`.
- **Status badge error tooltip.** Hovering an `unauthorized` or `error` status badge now surfaces the full upstream error message in a popover, matching the usage log status code tooltip style.
- **Anthropic `speed: fast` forwarding (#170).** Anthropic-style `speed: fast` requests now map to the Codex priority tier upstream so fast clients get fast tokens end-to-end.

### Fixes

- **Version popover always clickable.** The sidebar version badge now opens the popover even when the GitHub latest-version lookup is still pending or blocked; a "checking…" hint is shown until the remote tag arrives.
- **First-token timeout and scheduler races.** Hardened the proxy path so first-token timeouts and concurrent scheduler races no longer collapse into a spurious "no available account" 503.
- **`/responses` WebSocket ingress.** WebSocket clients hitting `ws://host/v1/responses` are now accepted; the prior 404/101 misclassification has been fixed. Setting `CODEX_UPSTREAM_TRANSPORT=ws` no longer reports the connect handshake as an unknown error.
- **Anthropic content preservation + deactivated probe flagging.** Anthropic-shaped responses keep their original content blocks; deactivated accounts are clearly flagged in probe state instead of silently appearing healthy.
- **Wham window classification.** Usage window classification now uses `limit_window_seconds` rather than field position, so free-tier accounts no longer have a 7d window misclassified as 5h.

## v2.2.2 - 2026-05-26

### Features

- **First-run setup and admin auth polish.** Added setup guidance for unconfigured deployments, improved the admin authentication flow, and added a frontend logout path.
- **Runtime status API and page.** Added machine-readable runtime checks for service, database, cache, usage log writer, probes, account pool, image storage, and admin auth.
- **Background media customization.** Added configurable background image/video support, realtime glass opacity/blur controls, and raised MP4 dynamic wallpaper uploads to 40MB while keeping image uploads capped at 20MB.
- **Quick-start configuration options.** Added fast-mode and reasoning-effort snippets for supported client templates in the usage docs.
- **Issue templates.** Added structured Chinese and English GitHub issue templates for bugs, ideas, UI feedback, deployment help, and questions.

### Fixes

- **Request body sizing for wallpapers.** Raised the default request body limit to 48MB so 40MB MP4 background uploads can pass multipart overhead safely.

# Changelog — iteration/may-2026-v2

Dates: 2026-05-13 to 2026-05-20. 17 commits.

## Features

- **Credit quota support (#141).** Added `credit_enabled` and `credit_skip_usage_window` flags to the accounts table. Credit-marked accounts skip usage-window penalties in the scheduler. Managed via `PATCH /api/admin/accounts/:id/credit`.

- **Scheduler mode (#133).** Added `scheduler_mode` system setting with two modes: `round_robin` (default, weighted by dispatch score) and `remaining_quota` (prioritize accounts with lowest usage percent). Configurable from Admin Settings page.

- **5h/7d windowed USD cost display.** Replaced the single total-cost column with a windowed billing view. Each account now shows `billed_5h` and `billed_7d` fields aligned with the account's usage-reset boundaries. This reflects actual spending, not estimated token costs.

- **Image-to-image in Image Studio (#135, #136).** The admin Image Studio now supports image-to-image generation via `POST /api/admin/images/edit-jobs`, accepting reference image URLs or data URIs. Added text-to-image and image-to-image tabs in the frontend.

- **Billing model expansion.** Added pricing for gpt-5.5-pro and gpt-5.4-pro families. Implemented long context (>272K tokens) premium pricing for gpt-5.5, gpt-5.5-pro, gpt-5.4, gpt-5.4-pro with automatic detection. Fixed gpt-4o and gpt-4o-mini cache-read pricing.

## Fixes

- **GPT-5.5 pricing corrected.** Updated standard-tier billing from old values to $5.00/M input / $30.00/M output (priority: $12.50/M / $75.00/M), matching current official pricing.

- **SSE stream isolation.** Prevented SSE response mixing when retrying across accounts, using `c.Writer.Written()` as the retry guard instead of a package-level flag.

- **Usage logging for image errors.** Added usage-log emits for read-error paths in image generation, ensuring billing records are not silently dropped on stream failures.

- **Model mapping initialization.** Restored `modelMapping` init that was accidentally removed during the scheduler_mode refactor.

- **Credit field Scan order.** Fixed PostgreSQL `Scan` argument ordering for credit fields that was causing silent zero-values.

- **Round 2 review fixes.** Addressed Haiku review findings including api.ts syntax cleanup, billing test corrections, and several CRITICAL/HIGH issues from automated review.

## Security

- **SQLite default binds to localhost.** SQLite compose files (`docker-compose.sqlite.yml`, `docker-compose.sqlite.local.yml`) now bind ports to `127.0.0.1` by default. Previously they bound to `0.0.0.0`, exposing the service on all interfaces. Standard (PostgreSQL) compose files retain the `0.0.0.0` default.

- **BIND_HOST env var.** Added `BIND_HOST` environment variable support to control the HTTP listen address across all deployment modes. Documented in `.env.example`, `.env.sqlite.example`, and `CONFIGURATION.md`.

## Breaking Changes

- **SQLite compose port binding.** SQLite deployments upgrading from a previous version that relied on external access via the default compose configuration must now explicitly set `BIND_HOST=0.0.0.0` in `.env` or override the port binding in the compose file. All other behavior remains backwards-compatible.
